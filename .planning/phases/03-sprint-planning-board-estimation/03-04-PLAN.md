---
phase: 03-sprint-planning-board-estimation
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/data/sprint-repository.ts
  - src/data/backlog-repository.ts
  - src/domain/types.ts
  - server.ts
  - src/App.tsx
  - src/components/SprintBoard.tsx
  - src/components/CompletionDateDialog.tsx
  - src/components/SprintClosePanel.tsx
  - src/components/SortableList.tsx
autonomous: true
requirements:
  - SPRT-03
  - SPRT-04
user_setup: []

must_haves:
  truths:
    - "D-15: User can view a sprint board mapped to backlog status states with Backlog, In Progress, and Done columns"
    - "User can move sprint items between board columns by drag-and-drop"
    - "D-16: Moving an item to Done asks for real completion date and saves completed_at"
    - "D-17: Moving an item out of Done clears completed_at without confirmation"
    - "D-18: User can manually order items within each board column and persist that order"
    - "D-19: User can close a sprint explicitly with a Close sprint action"
  artifacts:
    - path: "src/components/SprintBoard.tsx"
      provides: "Three-column sprint board"
      exports: ["SprintBoard"]
    - path: "src/components/CompletionDateDialog.tsx"
      provides: "Prompt for real completion date when moving to Done"
      exports: ["CompletionDateDialog"]
    - path: "src/components/SprintClosePanel.tsx"
      provides: "Close sprint action and warnings"
      exports: ["SprintClosePanel"]
    - path: "server.ts"
      provides: "Board status/order and close-sprint endpoints"
      exports: []
  key_links:
    - from: "src/components/SprintBoard.tsx"
      to: "/api/sprints/:id/board"
      via: "fetch and persist board state"
      pattern: "fetch.*/board"
    - from: "src/components/SprintClosePanel.tsx"
      to: "/api/sprints/:id/close"
      via: "close selected sprint"
      pattern: "fetch.*/close"
---

<objective>
Build the sprint board and close-sprint behavior. Users can move sprint items across Backlog/In Progress/Done, capture the real completion date for Done, manually order each column, and close a sprint.

Purpose: This completes the daily sprint management workflow and preserves the data Phase 4 needs for velocity and analytics.
Output: Board UI, board persistence API, completed_at handling, and close sprint action.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sprint-planning-board-estimation/03-CONTEXT.md
@.planning/phases/03-sprint-planning-board-estimation/03-RESEARCH.md
@.planning/phases/03-sprint-planning-board-estimation/03-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add board repository and API methods</name>
  <files>src/data/sprint-repository.ts, src/data/backlog-repository.ts, server.ts</files>
  <read_first>
    src/data/sprint-repository.ts
    src/data/backlog-repository.ts
    server.ts
    src/domain/types.ts
  </read_first>
  <action>
    Add SprintRepository.findBoard(sprintId) returning sprint items joined with backlog fields grouped/sortable by backlog status and sprint_items.board_order.
    Add SprintRepository.updateBoard(sprintId, updates) where updates include backlog_item_id, status ("backlog" | "in_progress" | "done"), board_order, and optional completed_at.
    In BacklogRepository, add updateStatus(id, status, completed_at?) that updates status and completed_at together. Moving out of done should set completed_at to null.
    Add endpoints:
    - GET /api/sprints/:id/board
    - PUT /api/sprints/:id/board with body { items: [{ backlog_item_id, status, board_order, completed_at? }] }
    Validate status values and completed_at date format.
  </action>
  <acceptance_criteria>
    - GET /api/sprints/:id/board returns items ordered by column/status and board_order.
    - PUT /api/sprints/:id/board updates backlog status and sprint board_order.
    - completed_at is required by API when setting an item to done.
    - completed_at is cleared when status is changed from done to another status.
    - Invalid status or date format returns 400.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Board state can be read and persisted through the API.</done>
</task>

<task type="auto">
  <name>Task 2: Build completion date dialog</name>
  <files>src/components/CompletionDateDialog.tsx</files>
  <read_first>
    src/domain/types.ts
    .planning/phases/03-sprint-planning-board-estimation/03-CONTEXT.md
  </read_first>
  <action>
    Create CompletionDateDialog component. It opens when a board move targets Done. It asks "Quando este item foi concluido?" with a date input defaulting to today's date. It has Confirm and Cancel actions. Confirm returns the selected YYYY-MM-DD date. Cancel aborts the board move.
  </action>
  <acceptance_criteria>
    - Dialog asks for real completion date.
    - Date input defaults to current local date.
    - Empty/invalid date cannot be confirmed.
    - Cancel leaves the item in its previous column.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Completion date prompt is available.</done>
</task>

<task type="auto">
  <name>Task 3: Build three-column sprint board</name>
  <files>src/components/SprintBoard.tsx, src/App.tsx, src/components/SortableList.tsx</files>
  <read_first>
    src/components/SortableList.tsx
    src/components/SprintPlanningWorkspace.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Create SprintBoard component for a selected sprint. It fetches GET /api/sprints/:id/board and renders three columns: Backlog, In Progress, Done. Use dnd kit to move items within and across columns.
    When moving to Done, open CompletionDateDialog. Only persist the move if the user confirms a date.
    When moving out of Done, clear completed_at in the persisted update without confirmation.
    Persist board_order manually per column using PUT /api/sprints/:id/board.
    In App.tsx, show SprintBoard for the selected sprint, either below planning workspace or in a simple sub-section of the Sprints tab.
  </action>
  <acceptance_criteria>
    - Board has Backlog, In Progress, and Done columns.
    - Sprint items render in the column matching backlog status.
    - User can drag items between columns.
    - Moving to Done asks for completion date.
    - Moving out of Done clears completed_at.
    - User can reorder items inside each column.
    - Board state persists after refresh.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Sprint board supports status moves, completion dates, and ordering.</done>
</task>

<task type="auto">
  <name>Task 4: Add close sprint endpoint and UI</name>
  <files>src/data/sprint-repository.ts, server.ts, src/components/SprintClosePanel.tsx, src/components/SprintBoard.tsx</files>
  <read_first>
    src/data/sprint-repository.ts
    server.ts
    src/components/SprintBoard.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Add SprintRepository.closeSprint(id) that sets status='closed', closed_at to current ISO timestamp, and updated_at.
    Add POST /api/sprints/:id/close. It should return the updated sprint. It should not delete or mutate sprint items.
    Create SprintClosePanel showing a "Fechar sprint" button. Before closing, show non-blocking warnings for unestimated items and items not in Done, but allow closure after user confirms with window.confirm.
    After close, disable planning add/remove and board moves for that sprint unless executor chooses a clearly labeled "closed" read-only state.
  </action>
  <acceptance_criteria>
    - POST /api/sprints/:id/close sets status closed and closed_at.
    - Close action preserves sprint items, estimates, statuses, and completed_at values.
    - UI warns about unestimated or unfinished items before closing.
    - Closed sprint appears read-only in planning/board UI.
    - Closed sprint can still be viewed.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Sprint can be explicitly closed and viewed read-only afterward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser board operations to API | Status, completion dates, and closed sprint state affect future analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | Marking Done without real completion date | mitigate | API requires completed_at when status is done |
| T-03-12 | Tampering | Mutating closed sprint | mitigate | UI read-only state; API should reject board/membership mutations for closed sprint if practical |
| T-03-13 | Repudiation | User changes Done date later | accept | v1 stores current state only; full audit log deferred |
| T-03-14 | Tampering | Invalid completion date format | mitigate | Zod date format validation |
</threat_model>

<verification>
1. Run `bun run build`.
2. Run `bun run dev`.
3. Open a selected sprint with items.
4. Move one item from Backlog to In Progress; refresh; verify it remains.
5. Move one item to Done; enter a real completion date; refresh; verify it remains Done with completed_at stored.
6. Move the Done item back to In Progress; verify completed_at clears.
7. Reorder items inside a column; refresh; verify order persists.
8. Close the sprint; verify status closed and UI becomes read-only.
</verification>

<success_criteria>
- User can view and manage a three-column sprint board.
- User can move items between columns via drag-and-drop.
- Done transition captures real completion date.
- Moving out of Done clears completion date.
- Manual board ordering persists.
- User can close a sprint and preserve data for Phase 4.
</success_criteria>

<output>
Create `.planning/phases/03-sprint-planning-board-estimation/03-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Components:**
- `SprintBoard`
- `CompletionDateDialog`
- `SprintClosePanel`

**Routes:**
- `GET /api/sprints/:id/board`
- `PUT /api/sprints/:id/board`
- `POST /api/sprints/:id/close`
