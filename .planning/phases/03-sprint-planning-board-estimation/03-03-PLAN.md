---
phase: 03-sprint-planning-board-estimation
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - package.json
  - bun.lock
  - src/data/backlog-repository.ts
  - src/data/sprint-repository.ts
  - server.ts
  - src/components/SortableItem.tsx
  - src/components/SortableList.tsx
  - src/components/SprintBacklogPicker.tsx
  - src/components/SprintScopePanel.tsx
  - src/components/BacklogList.tsx
autonomous: true
requirements:
  - BACK-03
  - SPRT-02
  - SPRT-04
user_setup: []

must_haves:
  truths:
    - "D-05: Drag-and-drop supports global backlog prioritization, adding eligible items to a sprint, and ordering items within a sprint"
    - "D-08: User can prioritize backlog items via drag-and-drop and persisted priority updates automatically"
    - "D-06: User can drag eligible stories/bugs into a sprint"
    - "D-07: Dragging epics/features into a sprint is prevented with a clear explanation"
    - "Persisted priority/order updates survive refresh"
  artifacts:
    - path: "package.json"
      provides: "dnd kit dependencies"
      contains: ["@dnd-kit/core", "@dnd-kit/sortable", "@dnd-kit/utilities"]
    - path: "src/components/SortableList.tsx"
      provides: "Reusable accessible sortable list wrapper"
      exports: ["SortableList"]
    - path: "server.ts"
      provides: "Reorder endpoints for backlog and sprint items"
      exports: []
  key_links:
    - from: "src/components/BacklogList.tsx"
      to: "/api/backlog/reorder"
      via: "persist global priority order"
      pattern: "fetch.*/api/backlog/reorder"
    - from: "src/components/SprintScopePanel.tsx"
      to: "/api/sprints/:id/items/reorder"
      via: "persist sprint item order"
      pattern: "fetch.*/items/reorder"
---

<objective>
Add drag-and-drop prioritization and sprint ordering. Users can reorder the global backlog, drag stories/bugs into a sprint, and reorder sprint scope, with persisted priority/order updates.

Purpose: This satisfies BACK-03 and the drag-and-drop parts of the sprint planning workflow.
Output: dnd kit integration, reusable sortable components, reorder APIs, and drag-enabled backlog/sprint planning UI.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sprint-planning-board-estimation/03-CONTEXT.md
@.planning/phases/03-sprint-planning-board-estimation/03-RESEARCH.md
@.planning/phases/03-sprint-planning-board-estimation/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dnd kit dependencies</name>
  <files>package.json, bun.lock</files>
  <read_first>
    package.json
    .planning/phases/03-sprint-planning-board-estimation/03-RESEARCH.md
  </read_first>
  <action>
    Run `bun add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`. Keep the lockfile update. Do not add a different drag-and-drop library unless dnd kit installation fails, in which case document the failure in the summary and use the smallest maintained alternative.
  </action>
  <acceptance_criteria>
    - package.json lists @dnd-kit/core.
    - package.json lists @dnd-kit/sortable.
    - package.json lists @dnd-kit/utilities.
    - bun.lock is updated.
  </acceptance_criteria>
  <verify>
    <automated>bun install && test -d node_modules/@dnd-kit/core && test -d node_modules/@dnd-kit/sortable</automated>
  </verify>
  <done>dnd kit dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Add reorder repository methods and API endpoints</name>
  <files>src/data/backlog-repository.ts, src/data/sprint-repository.ts, server.ts</files>
  <read_first>
    src/data/backlog-repository.ts
    src/data/sprint-repository.ts
    server.ts
  </read_first>
  <action>
    Add BacklogRepository.reorder(items: { id: string; priority: number }[]) that updates priority in a transaction or loop with parameterized queries.
    Add SprintRepository.reorderItems(sprintId, items: { backlog_item_id: string; sprint_order: number }[]) that updates sprint_order for items in a sprint.
    Add routes:
    - PUT /api/backlog/reorder with body { items: [{ id, priority }] }
    - PUT /api/sprints/:id/items/reorder with body { items: [{ backlog_item_id, sprint_order }] }
    Validate arrays with Zod. Reject empty arrays with 400. Ensure sprint item reorder only touches rows in the requested sprint.
  </action>
  <acceptance_criteria>
    - /api/backlog/reorder updates priority and returns { updated: N }.
    - /api/sprints/:id/items/reorder updates sprint_order and returns { updated: N }.
    - Invalid payloads return 400.
    - Reorder methods use parameterized queries.
    - Backlog root/child reads reflect updated priority order after refresh.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Reorder APIs persist backlog and sprint ordering.</done>
</task>

<task type="auto">
  <name>Task 3: Create reusable sortable components</name>
  <files>src/components/SortableList.tsx, src/components/SortableItem.tsx</files>
  <read_first>
    src/components/BacklogList.tsx
    src/components/SprintScopePanel.tsx
    node_modules/@dnd-kit/core/package.json
    node_modules/@dnd-kit/sortable/package.json
  </read_first>
  <action>
    Create SortableList and SortableItem wrappers around dnd kit. Include PointerSensor and KeyboardSensor with sortableKeyboardCoordinates. Support vertical list sorting. Expose callbacks for onReorder(ids: string[]) and optional disabled ids.
    Keep the component styling neutral so BacklogList and SprintScopePanel own their row visuals.
  </action>
  <acceptance_criteria>
    - SortableList uses DndContext, SortableContext, PointerSensor, KeyboardSensor.
    - Keyboard sorting is enabled.
    - SortableItem uses useSortable and CSS.Transform.toString.
    - Components are generic enough for backlog, sprint scope, and board columns.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Reusable sortable components are available.</done>
</task>

<task type="auto">
  <name>Task 4: Enable drag-and-drop backlog prioritization</name>
  <files>src/components/BacklogList.tsx</files>
  <read_first>
    src/components/BacklogList.tsx
    src/components/SortableList.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Update BacklogList to allow drag-and-drop reorder for the currently displayed backlog list. On reorder, compute priorities so the first visual item receives the highest priority. Call PUT /api/backlog/reorder and refresh.
    If nested children are visible, keep reordering scoped to the currently rendered sibling/root collection to avoid corrupting hierarchy. Add a short comment explaining this scope.
  </action>
  <acceptance_criteria>
    - User can reorder backlog items via drag-and-drop.
    - Reordered priority persists after page refresh.
    - Reordering does not move an item across hierarchy parent boundaries.
    - Existing edit/delete actions still work.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Backlog priority can be reordered with drag-and-drop.</done>
</task>

<task type="auto">
  <name>Task 5: Enable drag-and-drop sprint add and sprint ordering</name>
  <files>src/components/SprintBacklogPicker.tsx, src/components/SprintScopePanel.tsx</files>
  <read_first>
    src/components/SprintBacklogPicker.tsx
    src/components/SprintScopePanel.tsx
    src/components/SortableList.tsx
    src/data/sprint-repository.ts
  </read_first>
  <action>
    Update SprintBacklogPicker and SprintScopePanel so users can drag eligible stories/bugs from available backlog into the sprint and reorder sprint items. It is acceptable to keep an Add button as a fallback.
    If a non-eligible item appears in the drag source because of future UI changes, reject it client-side and show a concise message; the API already rejects it server-side.
    Persist sprint order through PUT /api/sprints/:id/items/reorder after drop/reorder.
  </action>
  <acceptance_criteria>
    - Stories and bugs can be dragged into the sprint.
    - Sprint items can be reordered by drag-and-drop.
    - Add button fallback still works.
    - Non-eligible type drop shows an explanatory message and does not create sprint membership.
    - Reordered sprint scope persists after refresh.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Sprint planning supports drag-and-drop add and order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser drag operations to API | Client-supplied order must not corrupt unrelated rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | Reorder payload modifies unrelated sprint items | mitigate | Update with sprint_id filter and validate item ownership |
| T-03-09 | Tampering | Client bypasses epic/feature drag restriction | mitigate | Server-side addItem check remains authoritative |
| T-03-10 | Denial of Service | Very large reorder payload | mitigate | Validate array and keep app local/small; optionally cap payload length to visible list size |
</threat_model>

<verification>
1. Run `bun install`.
2. Run `bun run build`.
3. Run `bun run dev`.
4. Reorder backlog items; refresh; verify order persists.
5. Drag a story/bug into a selected sprint; verify it appears in sprint scope.
6. Reorder sprint scope; refresh; verify order persists.
7. Try to add an epic/feature through API; verify 400.
8. Verify keyboard drag/reorder is at least operational through dnd kit keyboard sensor.
</verification>

<success_criteria>
- Backlog priority can be changed by drag-and-drop.
- Sprint scope can be assembled by drag-and-drop.
- Sprint scope order can be changed by drag-and-drop.
- All ordering persists to SQLite.
- Mouse and keyboard interactions have supported dnd kit sensors.
</success_criteria>

<output>
Create `.planning/phases/03-sprint-planning-board-estimation/03-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Components:**
- `SortableList`
- `SortableItem`

**Routes:**
- `PUT /api/backlog/reorder`
- `PUT /api/sprints/:id/items/reorder`

**Dependencies:**
- `@dnd-kit/core`
- `@dnd-kit/sortable`
- `@dnd-kit/utilities`
