---
phase: 03-sprint-planning-board-estimation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
  - 02-03
files_modified:
  - src/data/sprint-repository.ts
  - src/domain/types.ts
  - server.ts
  - src/App.tsx
  - src/components/SprintPlanningWorkspace.tsx
  - src/components/SprintBacklogPicker.tsx
  - src/components/SprintScopePanel.tsx
  - src/components/SprintCapacitySummary.tsx
autonomous: true
requirements:
  - SPRT-02
  - EST-01
  - EST-02
user_setup: []

must_haves:
  truths:
    - "D-01: Selected sprint opens a two-column planning workspace with backlog on the left and sprint scope on the right"
    - "D-06: Only stories and bugs can be added to a sprint"
    - "D-07: Epics/features are prevented from sprint membership with a short explanation"
    - "D-12: Unestimated items can enter the sprint with a warning and do not count toward totals"
    - "D-03: Planning workspace shows story point totals and work-day/capacity totals side by side"
    - "D-02: Capacity overage warns but never blocks saving sprint scope"
  artifacts:
    - path: "src/data/sprint-repository.ts"
      provides: "Sprint membership add/remove/read methods"
      exports: ["SprintRepository"]
    - path: "server.ts"
      provides: "REST endpoints for sprint membership"
      exports: []
    - path: "src/components/SprintPlanningWorkspace.tsx"
      provides: "Two-column sprint planning UI"
      exports: ["SprintPlanningWorkspace"]
    - path: "src/components/SprintCapacitySummary.tsx"
      provides: "Capacity and estimate totals with non-blocking warnings"
      exports: ["SprintCapacitySummary"]
  key_links:
    - from: "src/components/SprintPlanningWorkspace.tsx"
      to: "/api/sprints/:id/items"
      via: "fetch sprint scope"
      pattern: "fetch.*/api/sprints"
    - from: "src/components/SprintCapacitySummary.tsx"
      to: "/api/capacity"
      via: "fetch capacity by sprint dates"
      pattern: "fetch.*/api/capacity"
---

<objective>
Build the two-column sprint planning workspace. Users select a sprint, browse eligible backlog stories/bugs, add and remove items, and see point/day/capacity totals with warnings.

Purpose: This plan turns sprint records and estimates into an actual planning workflow.
Output: Sprint membership API and a usable planning workspace with advisory capacity summary.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sprint-planning-board-estimation/03-CONTEXT.md
@.planning/phases/03-sprint-planning-board-estimation/03-RESEARCH.md
@.planning/phases/03-sprint-planning-board-estimation/03-01-PLAN.md
@.planning/phases/02-squad-capacity-engine/02-02-PLAN.md
@.planning/phases/02-squad-capacity-engine/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sprint membership repository methods</name>
  <files>src/data/sprint-repository.ts, src/domain/types.ts</files>
  <read_first>
    src/data/sprint-repository.ts
    src/data/backlog-repository.ts
    src/domain/types.ts
  </read_first>
  <action>
    Extend SprintRepository with addItem(sprintId, backlogItemId), removeItem(sprintId, backlogItemId), findAvailableBacklogItems(sprintId), and getSprintTotals(sprintId).
    addItem must look up the backlog item and reject item types other than story or bug. It should assign sprint_order to max existing sprint_order + 1 and board_order to max board_order for the item's current status + 1.
    removeItem must delete membership and then update the backlog item's priority so it returns to the top of the backlog per context D-09.
    findAvailableBacklogItems should return stories and bugs that are not already in the sprint, ordered by priority DESC then created_at DESC.
    getSprintTotals should sum only estimated sprint items: total_story_points, total_estimate_days, unestimated_count, total_items.
    Update domain/types.ts with SprintPlanningTotals and SprintBacklogCandidate if useful.
  </action>
  <acceptance_criteria>
    - addItem rejects epics/features.
    - addItem prevents duplicates via UNIQUE constraint handling.
    - removeItem returns removed item to top backlog priority.
    - findAvailableBacklogItems returns stories and bugs only.
    - getSprintTotals ignores null estimates and reports unestimated_count.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "addItem" src/data/sprint-repository.ts && grep -c "getSprintTotals" src/data/sprint-repository.ts</automated>
  </verify>
  <done>Sprint membership logic exists in the repository.</done>
</task>

<task type="auto">
  <name>Task 2: Add sprint membership API endpoints</name>
  <files>server.ts</files>
  <read_first>
    server.ts
    src/data/sprint-repository.ts
    src/domain/types.ts
  </read_first>
  <action>
    Add endpoints:
    - GET /api/sprints/:id/items returns sprint items joined with backlog fields ordered by sprint_order.
    - GET /api/sprints/:id/available-backlog returns eligible stories/bugs not already in the sprint.
    - POST /api/sprints/:id/items with body { backlog_item_id } adds an item.
    - DELETE /api/sprints/:id/items/:itemId removes an item.
    - GET /api/sprints/:id/totals returns point/day/unestimated totals.
    Return 400 with a clear message when item type is not story/bug. Return 404 for missing sprint or item.
  </action>
  <acceptance_criteria>
    - GET /api/sprints/:id/items returns array.
    - GET /api/sprints/:id/available-backlog excludes epics/features and already selected items.
    - POST /api/sprints/:id/items adds a story or bug.
    - POST /api/sprints/:id/items rejects epics/features with 400.
    - DELETE /api/sprints/:id/items/:itemId removes membership.
    - GET /api/sprints/:id/totals returns total_story_points, total_estimate_days, unestimated_count, total_items.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Sprint membership API is available.</done>
</task>

<task type="auto">
  <name>Task 3: Build two-column planning workspace</name>
  <files>src/App.tsx, src/components/SprintPlanningWorkspace.tsx, src/components/SprintBacklogPicker.tsx, src/components/SprintScopePanel.tsx</files>
  <read_first>
    src/App.tsx
    src/components/SprintList.tsx
    src/components/BacklogList.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Create SprintPlanningWorkspace accepting selected sprint and callbacks. Layout: left column SprintBacklogPicker, right column SprintScopePanel.
    SprintBacklogPicker fetches /api/sprints/:id/available-backlog and lists eligible stories/bugs with title, type, story_points, estimate_days, and an Add button.
    SprintScopePanel fetches /api/sprints/:id/items and lists selected sprint items with title, status, story_points, estimate_days, warning for missing estimate, and Remove button.
    In App.tsx, when a sprint is selected from SprintList, render SprintPlanningWorkspace below or beside the list.
  </action>
  <acceptance_criteria>
    - Selected sprint opens a two-column planning workspace.
    - Left column shows eligible backlog stories/bugs.
    - Right column shows items already in the sprint.
    - Add button moves item from available list to sprint list.
    - Remove button removes item from sprint and refreshes both lists.
    - Items missing story_points or estimate_days show a warning.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Two-column planning workspace is functional with add/remove flow.</done>
</task>

<task type="auto">
  <name>Task 4: Add capacity and estimate summary</name>
  <files>src/components/SprintCapacitySummary.tsx, src/components/SprintPlanningWorkspace.tsx</files>
  <read_first>
    src/components/SprintPlanningWorkspace.tsx
    src/domain/types.ts
    .planning/phases/02-squad-capacity-engine/02-03-PLAN.md
  </read_first>
  <action>
    Create SprintCapacitySummary. It receives sprint dates and totals, fetches /api/capacity?start_date={start_date}&end_date={end_date}, and displays:
    - total story points
    - total estimated squad work days
    - available capacity from Phase 2 result, using total_final_hours if present, else total_real_hours, converted to days using a clear display note if needed
    - unestimated item count
    - warning when estimated days exceed available capacity
    If /api/capacity is unavailable or returns an unexpected shape, show a non-blocking "capacity unavailable" message and keep point/day totals visible.
  </action>
  <acceptance_criteria>
    - Summary shows story points and work-day totals side by side.
    - Summary fetches capacity by selected sprint start/end dates.
    - Overage displays a warning but does not block add/remove actions.
    - Missing estimates display an item count warning.
    - Capacity API failure does not break the planning workspace.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Planning summary shows points, days, capacity, and warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to Bun API | Sprint membership changes affect planning state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Adding epics/features to sprint | mitigate | Repository and API reject non-story/non-bug item types |
| T-03-06 | Tampering | Duplicate sprint membership | mitigate | UNIQUE(sprint_id, backlog_item_id) and API error handling |
| T-03-07 | Denial of Service | Capacity endpoint missing while Phase 2 incomplete | mitigate | UI handles unavailable capacity as non-blocking |
</threat_model>

<verification>
1. Complete or verify Phase 2 capacity endpoints exist.
2. Run `bun run build`.
3. Run `bun run dev`.
4. Create a sprint and select it.
5. Add an estimated story and an unestimated bug to the sprint.
6. Verify totals show points/days and unestimated warning.
7. Verify capacity warning appears if estimated days exceed capacity.
8. Remove an item and verify it appears back in available backlog near the top.
</verification>

<success_criteria>
- User can select backlog stories/bugs to include in a sprint.
- User can remove items from a sprint.
- Epics/features cannot be added directly.
- Planning view shows points, work days, capacity, and warnings.
- Capacity warnings do not block planning.
</success_criteria>

<output>
Create `.planning/phases/03-sprint-planning-board-estimation/03-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Components:**
- `SprintPlanningWorkspace`
- `SprintBacklogPicker`
- `SprintScopePanel`
- `SprintCapacitySummary`

**Routes:**
- `GET /api/sprints/:id/items`
- `GET /api/sprints/:id/available-backlog`
- `POST /api/sprints/:id/items`
- `DELETE /api/sprints/:id/items/:itemId`
- `GET /api/sprints/:id/totals`
