---
phase: 03-sprint-planning-board-estimation
plan: 01
type: execute
wave: 1
depends_on:
  - 02-03
files_modified:
  - src/data/schema.ts
  - src/domain/types.ts
  - src/data/sprint-repository.ts
  - src/data/backlog-repository.ts
  - server.ts
  - src/App.tsx
  - src/components/SprintForm.tsx
  - src/components/SprintList.tsx
  - src/components/BacklogForm.tsx
  - src/components/BacklogList.tsx
autonomous: true
requirements:
  - SPRT-01
  - EST-01
  - EST-02
user_setup: []

must_haves:
  truths:
    - "D-04: User can create, edit, view, select, and delete sprints from a dedicated sprint list with a new sprint action"
    - "D-14: Story points use the closed Fibonacci set 1, 2, 3, 5, 8, 13, 21"
    - "D-10: Work-day and story point estimates are stored on stories and bugs only"
    - "D-11: Estimate days mean squad work days, not calendar days"
    - "D-13: Epics and features do not accept direct estimates, but can show aggregate direct and indirect child estimates"
    - "Sprint status supports planned, active, and closed states for later close-sprint behavior"
  artifacts:
    - path: "src/data/schema.ts"
      provides: "SQLite schema for sprints, sprint_items, and backlog estimate/completion fields"
      contains: ["sprints", "sprint_items", "story_points", "estimate_days", "completed_at"]
    - path: "src/domain/types.ts"
      provides: "Sprint and estimation domain types"
      exports: ["Sprint", "NewSprint", "UpdateSprint", "SprintItem", "NewSprintItem", "FIBONACCI_POINTS"]
    - path: "src/data/sprint-repository.ts"
      provides: "Repository for sprint CRUD and sprint membership reads"
      exports: ["SprintRepository"]
    - path: "src/data/backlog-repository.ts"
      provides: "Estimate update and aggregate estimate helpers"
      exports: ["BacklogRepository"]
    - path: "server.ts"
      provides: "REST endpoints for sprint CRUD and backlog estimate updates"
      exports: []
    - path: "src/components/SprintForm.tsx"
      provides: "Sprint create/edit form"
      exports: ["SprintForm"]
    - path: "src/components/SprintList.tsx"
      provides: "Sprint list with select/edit/delete actions"
      exports: ["SprintList"]
  key_links:
    - from: "server.ts"
      to: "src/data/sprint-repository.ts"
      via: "import and instantiate"
      pattern: "new SprintRepository"
    - from: "src/App.tsx"
      to: "/api/sprints"
      via: "fetch GET/POST/PUT/DELETE"
      pattern: "fetch.*/api/sprints"
    - from: "src/components/BacklogForm.tsx"
      to: "/api/backlog/:id/estimate"
      via: "estimate fields are submitted for stories and bugs"
      pattern: "story_points|estimate_days"
---

<objective>
Create the sprint and estimation foundation. The app gains persistent sprints, typed sprint status, estimate fields on executable backlog items, API endpoints, and a Sprint list UI. This plan makes Phase 3's data model real before building the planning workspace, drag-and-drop, and board.

Purpose: Sprint planning needs durable sprint records and estimates before users can assemble sprint scope.
Output: SQLite schema, TypeScript types, repositories, REST routes, Sprint tab/list/form, and backlog estimate editing for stories/bugs.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sprint-planning-board-estimation/03-CONTEXT.md
@.planning/phases/03-sprint-planning-board-estimation/03-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/02-squad-capacity-engine/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schema and domain types for sprints and estimates</name>
  <files>src/data/schema.ts, src/domain/types.ts</files>
  <read_first>
    src/data/schema.ts
    src/domain/types.ts
    .planning/phases/03-sprint-planning-board-estimation/03-CONTEXT.md
  </read_first>
  <action>
    In src/data/schema.ts, extend initSchema with:
    1) ALTER TABLE guards for backlog_items columns: story_points INTEGER nullable, estimate_days REAL nullable, completed_at TEXT nullable. Use try/catch or SQLite-safe migration checks so existing databases do not fail if columns already exist.
    2) CREATE TABLE IF NOT EXISTS sprints: id TEXT PRIMARY KEY, goal TEXT NOT NULL, start_date TEXT NOT NULL, end_date TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'planned' CHECK(status IN ('planned','active','closed')), closed_at TEXT, created_at TEXT DEFAULT CURRENT_TIMESTAMP, updated_at TEXT DEFAULT CURRENT_TIMESTAMP.
    3) CREATE TABLE IF NOT EXISTS sprint_items: id TEXT PRIMARY KEY, sprint_id TEXT NOT NULL REFERENCES sprints(id) ON DELETE CASCADE, backlog_item_id TEXT NOT NULL REFERENCES backlog_items(id) ON DELETE CASCADE, sprint_order INTEGER NOT NULL DEFAULT 0, board_order INTEGER NOT NULL DEFAULT 0, created_at TEXT DEFAULT CURRENT_TIMESTAMP, UNIQUE(sprint_id, backlog_item_id).
    In src/domain/types.ts, add:
    - export const FIBONACCI_POINTS = [1, 2, 3, 5, 8, 13, 21] as const.
    - type StoryPoint = typeof FIBONACCI_POINTS[number].
    - SprintStatus = "planned" | "active" | "closed".
    - Sprint, NewSprint, UpdateSprint, SprintItem, NewSprintItem interfaces.
    - Add story_points: StoryPoint | null, estimate_days: number | null, completed_at: string | null to BacklogItem.
    - Add story_points?: StoryPoint | null, estimate_days?: number | null to NewBacklogItem and UpdateBacklogItem.
  </action>
  <acceptance_criteria>
    - schema.ts creates sprints and sprint_items tables with the exact relationship constraints.
    - schema.ts adds backlog_items story_points, estimate_days, and completed_at safely for existing databases.
    - domain/types.ts exports SprintStatus, Sprint, NewSprint, UpdateSprint, SprintItem, NewSprintItem, and FIBONACCI_POINTS.
    - BacklogItem includes nullable story_points, estimate_days, and completed_at.
    - Story point values are restricted to 1, 2, 3, 5, 8, 13, 21 at the type/API layer.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Schema and domain types support sprints and estimates.</done>
</task>

<task type="auto">
  <name>Task 2: Add SprintRepository and backlog estimate helpers</name>
  <files>src/data/sprint-repository.ts, src/data/backlog-repository.ts</files>
  <read_first>
    src/data/backlog-repository.ts
    src/data/schema.ts
    src/domain/types.ts
  </read_first>
  <action>
    Create src/data/sprint-repository.ts with class SprintRepository, constructor(db: Database). Implement findAll() ordered by start_date DESC, findById(id), create(sprint: NewSprint), update(id, changes: UpdateSprint), delete(id), and findItems(sprintId) returning sprint items joined with backlog item fields ordered by sprint_order ASC.
    In src/data/backlog-repository.ts, add updateEstimate(id, story_points, estimate_days) that only persists estimates on items whose type is story or bug. If the item type is epic or feature, throw a clear Error.
    Add aggregateEstimate(rootId) that uses recursive descendants to sum story_points and estimate_days for child items of type story or bug, ignoring null estimates.
  </action>
  <acceptance_criteria>
    - SprintRepository exports all CRUD methods listed above.
    - SprintRepository.create validates/generates id with crypto.randomUUID() and returns the created row.
    - SprintRepository.update sets updated_at.
    - findItems returns sprint membership plus backlog fields.
    - BacklogRepository.updateEstimate rejects epics/features.
    - BacklogRepository.aggregateEstimate sums all direct and indirect child stories/bugs.
    - All SQL uses parameterized placeholders for user-provided values.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "class SprintRepository" src/data/sprint-repository.ts && grep -c "updateEstimate" src/data/backlog-repository.ts && grep -c "aggregateEstimate" src/data/backlog-repository.ts</automated>
  </verify>
  <done>Sprint repository and estimate helpers are available.</done>
</task>

<task type="auto">
  <name>Task 3: Add sprint and estimate REST endpoints</name>
  <files>server.ts</files>
  <read_first>
    server.ts
    src/data/sprint-repository.ts
    src/data/backlog-repository.ts
    src/domain/types.ts
  </read_first>
  <action>
    In server.ts, import and instantiate SprintRepository. Add Zod schemas:
    - sprintSchema: goal string min 1, start_date and end_date strings matching YYYY-MM-DD, optional status enum planned/active/closed.
    - estimateSchema: story_points enum/number restricted to 1,2,3,5,8,13,21 nullable optional; estimate_days positive number nullable optional.
    Validate end_date >= start_date for sprints.
    Add routes:
    - GET /api/sprints
    - POST /api/sprints
    - GET /api/sprints/:id
    - PUT /api/sprints/:id
    - DELETE /api/sprints/:id
    - PUT /api/backlog/:id/estimate
    - GET /api/backlog/:id/aggregate-estimate
    Return JSON errors with 400 for validation failures and 404 for missing ids.
  </action>
  <acceptance_criteria>
    - GET /api/sprints returns an array.
    - POST /api/sprints creates a sprint and returns 201.
    - PUT /api/sprints/:id updates goal/date/status.
    - DELETE /api/sprints/:id deletes a sprint and cascades sprint_items.
    - Invalid date ranges return 400.
    - PUT /api/backlog/:id/estimate accepts only Fibonacci story points or null.
    - PUT /api/backlog/:id/estimate rejects epics/features with 400.
    - GET /api/backlog/:id/aggregate-estimate returns summed story_points and estimate_days.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; PID=$! &amp;&amp; sleep 2 &amp;&amp; curl -s -X POST -H "Content-Type: application/json" -d '{"goal":"Sprint 1","start_date":"2026-06-01","end_date":"2026-06-12"}' http://localhost:3000/api/sprints | grep -c "Sprint 1" &amp;&amp; curl -s http://localhost:3000/api/sprints | grep -c "Sprint 1" &amp;&amp; kill $PID</automated>
  </verify>
  <done>Sprint CRUD and estimate endpoints are available.</done>
</task>

<task type="auto">
  <name>Task 4: Add Sprint tab, sprint form, and sprint list UI</name>
  <files>src/App.tsx, src/components/SprintForm.tsx, src/components/SprintList.tsx</files>
  <read_first>
    src/App.tsx
    src/components/SquadMemberForm.tsx
    src/components/SquadMemberList.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Create src/components/SprintForm.tsx with controlled fields for goal, start_date, end_date, and status. Use existing form styling patterns. Validate goal and date range before submit.
    Create src/components/SprintList.tsx rendering existing sprints with goal, date range, status, and actions: Select, Edit, Delete. Delete must use window.confirm.
    In src/App.tsx, add a "Sprints" tab. Manage sprint state, selected sprint id, editing sprint, loading, and CRUD handlers using /api/sprints.
  </action>
  <acceptance_criteria>
    - App.tsx has a Sprints tab.
    - SprintForm can create and edit sprint goal, start date, end date, and status.
    - SprintList displays all sprints.
    - Selecting a sprint stores a selected sprint in App state for later plans.
    - Creating, editing, and deleting a sprint refreshes the list.
    - UI copy is concise Portuguese where user-facing.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Sprint list and form UI are functional.</done>
</task>

<task type="auto">
  <name>Task 5: Add estimate editing to backlog UI</name>
  <files>src/components/BacklogForm.tsx, src/components/BacklogList.tsx, src/App.tsx</files>
  <read_first>
    src/components/BacklogForm.tsx
    src/components/BacklogList.tsx
    src/App.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Update BacklogForm to show story_points and estimate_days fields only when type is story or bug. Story points must use a select with blank plus 1,2,3,5,8,13,21. Estimate days must use a number input with min 0 and step 0.5.
    For epic/feature, hide estimate inputs and show a small note that estimates aggregate from child stories/bugs.
    Update BacklogList to display point/day estimates for stories/bugs and aggregate totals for epics/features when available from GET /api/backlog/:id/aggregate-estimate. Keep this lightweight; do not block the plan if aggregate display is a simple async fetch per visible root item.
  </action>
  <acceptance_criteria>
    - Story and bug forms show story point and days fields.
    - Epic and feature forms do not allow direct estimate editing.
    - Story point select contains only blank, 1, 2, 3, 5, 8, 13, 21.
    - Empty estimate values are allowed and persist as null.
    - BacklogList shows estimates or "Sem estimativa" for executable items.
    - Epics/features show aggregate totals from descendants when available.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Backlog UI supports Phase 3 estimate semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to local Bun API | Sprint and estimate input crosses into SQLite persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Estimate values outside Fibonacci set | mitigate | Zod validation and UI select restrict story_points |
| T-03-02 | Tampering | Direct estimates on epics/features | mitigate | Repository rejects non-executable item types |
| T-03-03 | Denial of Service | Invalid sprint date ranges | mitigate | Validate YYYY-MM-DD and end_date >= start_date |
| T-03-04 | Information Disclosure | Sprint data exposed over local API | accept | Local-only app and corporate trust boundary per PROJECT.md |
</threat_model>

<verification>
1. Run `bun run build`.
2. Run `bun run dev`.
3. Create a sprint with goal, start date, and end date; verify it appears in the Sprints tab.
4. Edit the sprint goal; verify the list updates.
5. Create or edit a story and set story points 5 and days 2; verify the values persist after refresh.
6. Try setting an invalid story point through the API; verify 400.
7. Try setting an estimate on an epic through the API; verify 400.
</verification>

<success_criteria>
- User can create, edit, view, select, and delete sprints.
- Stories and bugs can carry story point and work-day estimates.
- Epics/features aggregate estimates but cannot receive direct estimates.
- Existing backlog CRUD continues to work.
- Data persists to SQLite across restart.
</success_criteria>

<output>
Create `.planning/phases/03-sprint-planning-board-estimation/03-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Classes:**
- `SprintRepository` (src/data/sprint-repository.ts)

**Functions / Methods:**
- `SprintRepository.findAll()`
- `SprintRepository.findById(id)`
- `SprintRepository.create(sprint)`
- `SprintRepository.update(id, changes)`
- `SprintRepository.delete(id)`
- `SprintRepository.findItems(sprintId)`
- `BacklogRepository.updateEstimate(id, story_points, estimate_days)`
- `BacklogRepository.aggregateEstimate(rootId)`

**Routes:**
- `GET /api/sprints`
- `POST /api/sprints`
- `GET /api/sprints/:id`
- `PUT /api/sprints/:id`
- `DELETE /api/sprints/:id`
- `PUT /api/backlog/:id/estimate`
- `GET /api/backlog/:id/aggregate-estimate`

**New File Paths:**
- `src/data/sprint-repository.ts`
- `src/components/SprintForm.tsx`
- `src/components/SprintList.tsx`
