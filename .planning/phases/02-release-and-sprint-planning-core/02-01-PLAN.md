---
phase: 02-release-and-sprint-planning-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - scripts/db-sync.ts
  - src/app/api/releases/route.ts
  - src/app/releases/page.tsx
  - src/features/releases/release-form.tsx
  - src/features/releases/release-list.tsx
  - src/lib/releases.ts
  - src/lib/sprint-generation.ts
  - tests/releases.test.ts
  - tests/e2e/releases.spec.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
user_setup: []
must_haves:
  truths:
    - "The Releases screen lets the user create a release with name, objective, optional description, start date, end date, default sprint length in business days, meeting percentage, support percentage, and status."
    - "Saving a valid release writes a Release row and generated Sprint rows to SQLite."
    - "The system prevents more than one release with status IN_PROGRESS."
    - "Generated sprints are sequential, non-overlapping, use Monday-Friday business days, and exclude registered holidays."
    - "When the release period does not divide evenly, the final generated sprint absorbs the remaining business days instead of creating a very small final sprint."
    - "Schema changes are applied to the local SQLite database before final verification."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Release and Sprint models plus status enums"
      contains: "model Release"
    - path: "src/lib/sprint-generation.ts"
      provides: "Business-day sprint generation"
      exports:
        - generateSprintsForRelease
    - path: "src/lib/releases.ts"
      provides: "Release create/list helpers"
      exports:
        - createRelease
        - listReleases
    - path: "src/app/api/releases/route.ts"
      provides: "Release list/create API"
      exports:
        - GET
        - POST
    - path: "src/features/releases/release-form.tsx"
      provides: "Interactive release creation form"
      min_lines: 80
  key_links:
    - from: "src/features/releases/release-form.tsx"
      to: "/api/releases"
      via: "client fetch for release creation"
      pattern: "fetch\\('/api/releases'"
    - from: "src/app/api/releases/route.ts"
      to: "createRelease"
      via: "POST delegates validation and persistence"
      pattern: "createRelease"
    - from: "src/lib/releases.ts"
      to: "generateSprintsForRelease"
      via: "release save creates sprints"
      pattern: "generateSprintsForRelease"
---

<objective>
As a squad planner, I want to create a release and immediately get a generated sprint sequence, so that release planning has real persisted periods to build on.

Purpose: Deliver the thinnest end-to-end Phase 2 path: Releases UI form -> API -> SQLite Release/Sprint rows -> visible generated sprint list.
Output: Release/Sprint Prisma schema, local SQLite sync, release creation API, sprint generation helper, release form/list UI, and tests for release creation, active-release uniqueness, and sprint generation rules.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-foundation-and-squad-setup/01-SKELETON.md
@.planning/phases/01-local-foundation-and-squad-setup/01-01-SUMMARY.md
@.planning/phases/01-local-foundation-and-squad-setup/01-02-SUMMARY.md
@.planning/phases/01-local-foundation-and-squad-setup/01-03-SUMMARY.md
@spec.md
@telas.md
</context>

<artifacts_this_phase_produces>
- Prisma enums: `ReleaseStatus`, `SprintStatus`.
- Prisma models: `Release`, `Sprint`.
- Functions: `generateSprintsForRelease`, `validateReleaseInput`, `createRelease`, `listReleases`, `toReleaseView`.
- API route exports: `GET` and `POST` in `src/app/api/releases/route.ts`.
- React components: `ReleaseForm`, `ReleaseList`.
- Test files: `tests/releases.test.ts`, `tests/e2e/releases.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add a release creation slice with generated persisted sprints</name>
  <files>prisma/schema.prisma, scripts/db-sync.ts, src/lib/sprint-generation.ts, src/lib/releases.ts, src/app/api/releases/route.ts, src/app/releases/page.tsx, src/features/releases/release-form.tsx, src/features/releases/release-list.tsx, tests/releases.test.ts</files>
  <read_first>prisma/schema.prisma, scripts/db-sync.ts, src/lib/squad.ts, src/lib/validation.ts, src/app/api/settings/route.ts, src/app/releases/page.tsx, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, spec.md, telas.md</read_first>
  <action>Add `ReleaseStatus` enum values `PLANNED`, `IN_PROGRESS`, `CLOSED`, and `CANCELLED`, and `SprintStatus` enum values `PLANNED`, `IN_PROGRESS`, and `CLOSED` to `prisma/schema.prisma`. Add `Release` fields `id`, `name`, `objective`, optional `description`, `startDate`, `endDate`, `defaultSprintLengthBusinessDays`, `meetingPercentage`, `supportPercentage`, `status`, `sprints`, `createdAt`, and `updatedAt`. Add `Sprint` fields `id`, `releaseId`, `release`, `name`, optional `goal`, `startDate`, `endDate`, `status`, `createdAt`, and `updatedAt`. Update `scripts/db-sync.ts` to create matching SQLite tables and indexes/foreign keys for Release and Sprint. Create `src/lib/sprint-generation.ts` with `generateSprintsForRelease({ startDate, endDate, defaultSprintLengthBusinessDays, holidays })` that counts Monday-Friday business days excluding registered holidays, generates sequential non-overlapping sprint date ranges, names them `Sprint 1`, `Sprint 2`, and so on, and makes the last sprint absorb all remaining days when remaining business days are less than the default length. Create `src/lib/releases.ts` with validation for required name/objective/start/end/default length/percentages/status, `meetingPercentage` and `supportPercentage` from 0 to 100, combined percentages less than or equal to 100, end date after start date, default sprint length greater than 0, and a database check that rejects creating an `IN_PROGRESS` release when another `IN_PROGRESS` release exists. Implement `POST /api/releases` to create the release and generated sprints in one transaction and `GET /api/releases` to list releases with sprint counts. Replace the Phase 2 placeholder in `src/app/releases/page.tsx` with a compact operational Releases screen containing `ReleaseForm` and `ReleaseList`; after create, refresh the visible list and show the generated sprint count.</action>
  <verify>npm run test -- releases; npm run build</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model Release`, `model Sprint`, `enum ReleaseStatus`, and `enum SprintStatus`.
    - `scripts/db-sync.ts` contains `CREATE TABLE IF NOT EXISTS "Release"` and `CREATE TABLE IF NOT EXISTS "Sprint"`.
    - `src/lib/sprint-generation.ts` exports `generateSprintsForRelease`.
    - `src/lib/releases.ts` rejects a second `IN_PROGRESS` release with an error message containing `Only one release can be in progress`.
    - `src/app/api/releases/route.ts` exports `GET` and `POST`.
    - `src/features/releases/release-form.tsx` calls `fetch('/api/releases'`.
    - Unit tests cover uneven business-day sprint generation, holiday exclusion, invalid percentage totals, and duplicate `IN_PROGRESS` rejection.
  </acceptance_criteria>
  <done>A user can create a release from `/releases` and see persisted generated sprints in the list.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Sync the Prisma schema to the local SQLite database</name>
  <files>prisma/schema.prisma, scripts/db-sync.ts, package-lock.json</files>
  <read_first>prisma/schema.prisma, scripts/db-sync.ts, package.json, README.md</read_first>
  <action>Run the project database generation and local SQLite sync after the Release and Sprint schema changes are complete. Use the existing project commands `npm run db:generate` and `npm run db:migrate`. If Prisma CLI prompts in a non-TTY context, use the local sync script path already defined by `npm run db:migrate`; do not switch the app to a remote database or add external migration services. Confirm that `./data/squad-planner.db` contains the new `Release` and `Sprint` tables through the sync command output or an equivalent SQLite inspection.</action>
  <verify>npm run db:generate; npm run db:migrate</verify>
  <acceptance_criteria>
    - `npm run db:generate` exits 0.
    - `npm run db:migrate` exits 0.
    - The local SQLite database has tables named `Release` and `Sprint`.
    - No remote database configuration is added.
  </acceptance_criteria>
  <done>The live local database is ready for Release and Sprint reads/writes before final plan verification.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the release creation path in the browser</name>
  <files>tests/e2e/releases.spec.ts, src/app/releases/page.tsx, src/features/releases/release-form.tsx, src/features/releases/release-list.tsx</files>
  <read_first>tests/e2e/local-smoke.spec.ts, tests/e2e/squad.spec.ts, playwright.config.ts, src/app/releases/page.tsx, src/features/releases/release-form.tsx, src/features/releases/release-list.tsx</read_first>
  <action>Add Playwright coverage for `/releases` that creates a release named `Release Q3 2026` with objective `Plan Q3 scope`, start date `2026-07-01`, end date `2026-07-31`, default sprint length `10`, meeting percentage `10`, support percentage `20`, and status `IN_PROGRESS`. Assert that the page shows `Release Q3 2026`, `IN_PROGRESS`, and a generated sprint count. Reload `/releases` and assert the release remains visible. Add one assertion that attempting to create another `IN_PROGRESS` release surfaces the duplicate-active-release validation error.</action>
  <verify>npm run test:e2e -- releases; npm run build</verify>
  <acceptance_criteria>
    - `tests/e2e/releases.spec.ts` creates `Release Q3 2026`.
    - The e2e test reloads `/releases` and still sees `Release Q3 2026`.
    - The e2e test expects text containing `Only one release can be in progress`.
    - `npm run build` exits 0 after release creation UI changes.
  </acceptance_criteria>
  <done>The release creation workflow is proven through the same browser path a local user uses.</done>
</task>

</tasks>

<verification>
- [ ] `npm run db:generate` succeeds.
- [ ] `npm run db:migrate` succeeds and creates local Release/Sprint tables.
- [ ] `npm run test -- releases` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- releases` creates a release, verifies generated sprints, and proves persistence after reload.
</verification>

<success_criteria>
- REL-01 is partially complete for release create/list fields in the local UI and API.
- REL-02 is complete for preventing multiple in-progress releases on create.
- REL-03 is complete for automatic sprint generation when saving a release.
- REL-04 is complete for sequential, non-overlapping sprint generation with final sprint absorption.
- The app remains local-first with SQLite at `./data/squad-planner.db` and localhost-only browser access.
</success_criteria>

<output>
After completion, create `.planning/phases/02-release-and-sprint-planning-core/02-01-SUMMARY.md`.
</output>
