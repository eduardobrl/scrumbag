---
phase: 02-release-and-sprint-planning-core
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/api/sprints/[id]/route.ts
  - src/app/sprints/[id]/page.tsx
  - src/app/sprints/[id]/edit/page.tsx
  - src/app/sprints/page.tsx
  - src/features/sprints/sprint-detail.tsx
  - src/features/sprints/sprint-edit-form.tsx
  - src/features/sprints/sprint-list.tsx
  - src/lib/sprint-planning-summary.ts
  - src/lib/sprints.ts
  - tests/sprints.test.ts
  - tests/e2e/sprints.spec.ts
autonomous: true
requirements:
  - SPR-01
  - SPR-02
  - SPR-03
user_setup: []
must_haves:
  truths:
    - "The Sprints screen lists sprints for the selected or active release with period, status, capacity placeholder, planned effort placeholder, remaining capacity placeholder, occupancy placeholder, and risk placeholder."
    - "The user can open a sprint detail page and edit sprint goal and dates."
    - "Sprint date edits prevent overlaps with other sprints in the same release."
    - "Sprint date edits surface release gap warnings when edited dates leave uncovered business-day periods inside the release."
    - "Sprint edits trigger a recalculation hook for affected sprint planning summaries even though real story/capacity calculations are deferred."
  artifacts:
    - path: "src/lib/sprints.ts"
      provides: "Sprint list/detail/update helpers"
      exports:
        - listSprintsForRelease
        - getSprintDetails
        - updateSprint
        - detectSprintScheduleWarnings
    - path: "src/lib/sprint-planning-summary.ts"
      provides: "Phase 2 planned/capacity/risk placeholders and recalculation hook"
      exports:
        - getSprintPlanningSummary
        - recalculateSprintPlanningSummary
    - path: "src/app/api/sprints/[id]/route.ts"
      provides: "Sprint detail/update API"
      exports:
        - GET
        - PATCH
    - path: "src/app/sprints/page.tsx"
      provides: "Sprint list route"
      min_lines: 30
    - path: "src/app/sprints/[id]/page.tsx"
      provides: "Sprint detail route"
      min_lines: 30
  key_links:
    - from: "src/features/sprints/sprint-edit-form.tsx"
      to: "/api/sprints/"
      via: "client PATCH for sprint updates"
      pattern: "PATCH"
    - from: "src/app/api/sprints/[id]/route.ts"
      to: "updateSprint"
      via: "API delegates schedule validation"
      pattern: "updateSprint"
    - from: "src/lib/sprints.ts"
      to: "recalculateSprintPlanningSummary"
      via: "date/goal edits trigger placeholder recalculation hook"
      pattern: "recalculateSprintPlanningSummary"
---

<objective>
As a squad planner, I want to inspect generated sprints and safely adjust sprint dates or goals, so that the release plan can be refined before stories and capacity are added.

Purpose: Complete the sprint planning core by replacing the Sprints placeholder with persisted sprint list/detail/edit flows, overlap prevention, release-gap warnings, and summary placeholders ready for later capacity wiring.
Output: Sprint list/detail/update helpers and APIs, Sprints routes, sprint edit UI, planning-summary placeholder hooks, and tests for sprint schedule validation.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-foundation-and-squad-setup/01-SKELETON.md
@.planning/phases/01-local-foundation-and-squad-setup/01-03-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-01-SUMMARY.md
@spec.md
@telas.md
</context>

<artifacts_this_phase_produces>
- Functions: `listSprintsForRelease`, `getSprintDetails`, `validateSprintInput`, `updateSprint`, `detectSprintScheduleWarnings`, `getSprintPlanningSummary`, `recalculateSprintPlanningSummary`.
- API route exports: `GET` and `PATCH` in `src/app/api/sprints/[id]/route.ts`.
- App routes: `/sprints`, `/sprints/[id]`, `/sprints/[id]/edit`.
- React components: `SprintList`, `SprintDetail`, `SprintEditForm`.
- Test files: `tests/sprints.test.ts`, `tests/e2e/sprints.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add sprint planning helpers, placeholders, and update API</name>
  <files>src/lib/sprints.ts, src/lib/sprint-planning-summary.ts, src/app/api/sprints/[id]/route.ts, tests/sprints.test.ts</files>
  <read_first>src/lib/releases.ts, src/lib/sprint-generation.ts, prisma/schema.prisma, src/app/api/releases/[id]/route.ts, tests/releases.test.ts, spec.md, telas.md</read_first>
  <action>Create `src/lib/sprint-planning-summary.ts` with `getSprintPlanningSummary(sprintId)` returning Phase 2 placeholders: `plannedEffortDays: 0`, `capacityDays: null`, `remainingCapacityDays: null`, `occupancyPercentage: null`, and `riskLabel: "Pending capacity"`. Add `recalculateSprintPlanningSummary(sprintId)` that currently returns the same placeholder summary and is explicitly documented as the Phase 4 integration hook. Create `src/lib/sprints.ts` with `listSprintsForRelease(releaseId)`, `getSprintDetails(id)`, `validateSprintInput(input)`, `updateSprint(id, input)`, and `detectSprintScheduleWarnings(releaseId, proposedSprint)`. Validate optional `goal`, required start/end dates, end date on or after start date, status in `PLANNED | IN_PROGRESS | CLOSED`, no overlap with other sprints from the same release, and release gap warnings based on Monday-Friday business days inside the release period. Implement `GET` and `PATCH` in `/api/sprints/[id]`; `PATCH` updates goal/dates/status, calls `recalculateSprintPlanningSummary`, returns warnings for gaps, and returns HTTP 400 for overlaps.</action>
  <verify>npm run test -- sprints; npm run build</verify>
  <acceptance_criteria>
    - `src/lib/sprint-planning-summary.ts` exports `getSprintPlanningSummary` and `recalculateSprintPlanningSummary`.
    - `getSprintPlanningSummary` returns `plannedEffortDays: 0` and `riskLabel: "Pending capacity"`.
    - `src/lib/sprints.ts` exports `listSprintsForRelease`, `getSprintDetails`, `updateSprint`, and `detectSprintScheduleWarnings`.
    - Tests reject overlapping sprint date ranges with an error containing `Sprint dates cannot overlap`.
    - Tests produce a gap warning containing `Gap in release schedule` when a business-day hole exists between sprints.
    - `src/app/api/sprints/[id]/route.ts` exports `GET` and `PATCH`.
  </acceptance_criteria>
  <done>Sprint data can be queried and updated with schedule safety before UI work depends on it.</done>
</task>

<task type="auto">
  <name>Task 2: Build sprint list and detail screens with planning placeholders</name>
  <files>src/app/sprints/page.tsx, src/app/sprints/[id]/page.tsx, src/features/sprints/sprint-list.tsx, src/features/sprints/sprint-detail.tsx, tests/e2e/sprints.spec.ts</files>
  <read_first>src/app/sprints/page.tsx, src/app/release-placeholder.tsx, src/lib/releases.ts, src/lib/sprints.ts, src/lib/sprint-planning-summary.ts, src/components/ui/card.tsx, src/components/ui/badge.tsx, telas.md</read_first>
  <action>Replace the Sprints placeholder with `/sprints` showing sprints for the active `IN_PROGRESS` release when one exists, otherwise the most recently created release. Include a release selector if multiple releases exist. Render a dense table with sprint name, period, status, goal, capacity `--`, planned effort `0d`, remaining `--`, occupancy `--`, and risk `Pending capacity`. Add empty state text for no release and no sprints. Add `/sprints/[id]` detail showing release name, sprint period, goal, status, the same placeholder metrics, and links/buttons to edit the sprint and return to release detail. Do not add story board columns, drag-and-drop, closure, leakage, or real capacity calculations in this phase.</action>
  <verify>npm run test:e2e -- sprints; npm run build</verify>
  <acceptance_criteria>
    - `/sprints` displays text `Pending capacity` for generated sprints.
    - `/sprints` displays planned effort as `0d`.
    - `/sprints/[id]` displays sprint goal, status, period, `Capacity`, `Planned effort`, `Remaining`, `Occupancy`, and `Risk`.
    - The e2e test creates a release, opens `/sprints`, and opens a sprint detail page.
    - No `@dnd-kit` imports are added in this plan.
  </acceptance_criteria>
  <done>The user can browse generated sprints and see Phase 2 sprint planning context without Phase 4 functionality leaking in.</done>
</task>

<task type="auto">
  <name>Task 3: Add sprint goal/date editing with overlap blocking and gap warnings</name>
  <files>src/app/sprints/[id]/edit/page.tsx, src/features/sprints/sprint-edit-form.tsx, src/features/sprints/sprint-detail.tsx, src/features/sprints/sprint-list.tsx, tests/e2e/sprints.spec.ts</files>
  <read_first>src/app/sprints/[id]/page.tsx, src/app/api/sprints/[id]/route.ts, src/lib/sprints.ts, src/features/settings/settings-form.tsx, src/features/releases/release-edit-form.tsx, tests/e2e/sprints.spec.ts, telas.md</read_first>
  <action>Add `/sprints/[id]/edit` with `SprintEditForm` that pre-fills goal, start date, end date, and status. On save, send `PATCH /api/sprints/{id}` and show inline validation errors for overlapping dates. When the API returns schedule warnings, render them as non-blocking warning badges or callouts on the edit/detail/list screens; the warning copy must include `Gap in release schedule`. After successful save, return to sprint detail and show updated dates/goal plus any warning. Ensure editing sprint dates triggers `recalculateSprintPlanningSummary` through the API path. Keep closed-sprint restrictions, board status movement, and leakage out of scope for Phase 2.</action>
  <verify>npm run test -- sprints; npm run test:e2e -- sprints; npm run build</verify>
  <acceptance_criteria>
    - `/sprints/[id]/edit` renders inputs for goal, start date, end date, and status.
    - Editing a sprint goal to `Stabilize release planning` shows that goal on `/sprints/[id]` after reload.
    - Attempting to overlap two sprints shows an error containing `Sprint dates cannot overlap`.
    - Creating a gap between sprints shows warning text containing `Gap in release schedule` without blocking save.
    - `PATCH /api/sprints/{id}` calls `recalculateSprintPlanningSummary`.
  </acceptance_criteria>
  <done>The user can safely refine generated sprints and see schedule risks before story/capacity phases arrive.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- sprints` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- sprints` succeeds.
- [ ] Manual browser check can open `/sprints`, inspect a generated sprint, edit its goal, block an overlap, and show a release gap warning.
</verification>

<success_criteria>
- SPR-01 is complete with sprint list period/status/capacity/planned/remaining/occupancy/risk context.
- SPR-02 is complete with sprint goal/date editing.
- SPR-03 is complete with overlap prevention, gap warnings, and recalculation hooks.
- Phase 4-only board, story, real capacity, closure, reopening, and leakage behavior remain deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/02-release-and-sprint-planning-core/02-03-SUMMARY.md`.
</output>
