---
phase: 02-release-and-sprint-planning-core
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/api/releases/[id]/route.ts
  - src/app/releases/[id]/page.tsx
  - src/app/releases/[id]/edit/page.tsx
  - src/app/releases/page.tsx
  - src/components/app-shell.tsx
  - src/features/releases/release-detail.tsx
  - src/features/releases/release-edit-form.tsx
  - src/features/releases/release-list.tsx
  - src/lib/releases.ts
  - tests/releases.test.ts
  - tests/e2e/release-management.spec.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
user_setup: []
must_haves:
  truths:
    - "The user can list releases, open a release detail page, and edit release fields."
    - "Release edit preserves the single in-progress release invariant."
    - "When release dates or default sprint length change in Phase 2, generated sprints are reconciled safely because no story planning exists yet."
    - "Release detail shows objective, period, status, meeting percentage, support percentage, sprint count, and generated sprint rows."
    - "The global header reads the active in-progress release when one exists and keeps the existing no-release fallback when none exists."
  artifacts:
    - path: "src/app/api/releases/[id]/route.ts"
      provides: "Release detail/update API"
      exports:
        - GET
        - PATCH
    - path: "src/app/releases/[id]/page.tsx"
      provides: "Release detail route"
      min_lines: 30
    - path: "src/app/releases/[id]/edit/page.tsx"
      provides: "Release edit route"
      min_lines: 20
    - path: "src/lib/releases.ts"
      provides: "Release detail/update/active helpers"
      exports:
        - getReleaseDetails
        - updateRelease
        - getActiveReleaseSummary
    - path: "src/components/app-shell.tsx"
      provides: "Stored active release context in global header"
      contains: "Active release"
  key_links:
    - from: "src/app/releases/[id]/edit/page.tsx"
      to: "getReleaseDetails"
      via: "server-side initial form data"
      pattern: "getReleaseDetails"
    - from: "src/features/releases/release-edit-form.tsx"
      to: "/api/releases/"
      via: "client PATCH for release updates"
      pattern: "PATCH"
    - from: "src/components/app-shell.tsx"
      to: "getActiveReleaseSummary"
      via: "global header release context"
      pattern: "getActiveReleaseSummary"
---

<objective>
As a squad planner, I want to inspect and edit releases after creation, so that the release plan can be corrected while keeping generated sprints and the active-release rule safe.

Purpose: Turn the initial create flow into full Phase 2 release management: list, detail, edit, regenerate/reconcile sprints, and active release context in the app shell.
Output: Release detail/update APIs, release list/detail/edit routes, active release header context, and tests for release editing and sprint reconciliation.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-foundation-and-squad-setup/01-SKELETON.md
@.planning/phases/01-local-foundation-and-squad-setup/01-03-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-01-SUMMARY.md
@spec.md
@telas.md
</context>

<artifacts_this_phase_produces>
- Functions: `getReleaseDetails`, `updateRelease`, `getActiveReleaseSummary`, `reconcileGeneratedSprints`.
- API route exports: `GET` and `PATCH` in `src/app/api/releases/[id]/route.ts`.
- App routes: `/releases/[id]`, `/releases/[id]/edit`.
- React components: `ReleaseDetail`, `ReleaseEditForm`.
- Test file: `tests/e2e/release-management.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add release detail and edit APIs with sprint reconciliation</name>
  <files>src/lib/releases.ts, src/app/api/releases/[id]/route.ts, tests/releases.test.ts</files>
  <read_first>src/lib/releases.ts, src/lib/sprint-generation.ts, src/app/api/releases/route.ts, prisma/schema.prisma, tests/releases.test.ts, spec.md, telas.md</read_first>
  <action>Extend `src/lib/releases.ts` with `getReleaseDetails(id)`, `updateRelease(id, input)`, `getActiveReleaseSummary()`, and `reconcileGeneratedSprints(releaseId, generatedRanges)`. `getReleaseDetails` must return the release with ordered sprints. `updateRelease` must validate the same fields as creation, preserve REL-02 by rejecting updates that would make two releases `IN_PROGRESS`, and update release fields. When `startDate`, `endDate`, or `defaultSprintLengthBusinessDays` changes, regenerate the Phase 2 sprint sequence from business days and holidays, then reconcile existing generated sprints by order: update existing sprint names/dates/status where possible, create missing sprints, and remove extra generated sprints only while no stories exist in v1. Implement `GET` and `PATCH` in `src/app/api/releases/[id]/route.ts` with 404 for missing release and 400 for validation errors.</action>
  <verify>npm run test -- releases; npm run build</verify>
  <acceptance_criteria>
    - `src/lib/releases.ts` exports `getReleaseDetails`, `updateRelease`, `getActiveReleaseSummary`, and `reconcileGeneratedSprints`.
    - `src/app/api/releases/[id]/route.ts` exports `GET` and `PATCH`.
    - Updating a second release to `IN_PROGRESS` returns a validation error containing `Only one release can be in progress`.
    - Changing a release end date updates the ordered sprint date ranges in tests.
    - `getReleaseDetails` orders sprints by `startDate` ascending.
  </acceptance_criteria>
  <done>Release data can be fetched and updated safely without violating active-release or sprint-sequence rules.</done>
</task>

<task type="auto">
  <name>Task 2: Build release list, detail, and edit screens</name>
  <files>src/app/releases/page.tsx, src/app/releases/[id]/page.tsx, src/app/releases/[id]/edit/page.tsx, src/features/releases/release-list.tsx, src/features/releases/release-detail.tsx, src/features/releases/release-edit-form.tsx, tests/e2e/release-management.spec.ts</files>
  <read_first>src/app/releases/page.tsx, src/features/releases/release-form.tsx, src/features/releases/release-list.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, telas.md, .planning/REQUIREMENTS.md</read_first>
  <action>Upgrade `/releases` from create-only to an operational release list with status and text filtering, rows for name, period, status, sprint count, meeting percentage, support percentage, and actions to open detail/edit. Add `/releases/[id]` with objective, description, period, status, meeting/support percentages, default sprint length, sprint count, and a generated sprints table showing sprint name, period, status, goal placeholder, planned effort `0d`, capacity `--`, remaining `--`, and risk `Pending capacity`. Add `/releases/[id]/edit` with `ReleaseEditForm` that pre-fills all release fields, sends `PATCH /api/releases/{id}`, shows inline validation errors, and returns to detail after save. Keep the UI desktop-first and information-dense; use existing UI primitives and avoid introducing a marketing hero layout.</action>
  <verify>npm run test:e2e -- release-management; npm run build</verify>
  <acceptance_criteria>
    - `/releases` shows columns or row labels for `Period`, `Status`, `Sprints`, `Meetings`, and `Support`.
    - `/releases/[id]` shows `Planned effort`, `Capacity`, `Remaining`, and `Pending capacity` for generated sprints.
    - `/releases/[id]/edit` renders inputs for name, objective, description, start date, end date, default sprint length, meeting percentage, support percentage, and status.
    - The e2e test edits `Release Q3 2026` objective text and sees the changed objective on the detail page after reload.
  </acceptance_criteria>
  <done>The user can manage a release after creation and inspect the generated sprint context from the release detail page.</done>
</task>

<task type="auto">
  <name>Task 3: Show the persisted active release in the global header</name>
  <files>src/components/app-shell.tsx, src/lib/releases.ts, tests/e2e/release-management.spec.ts</files>
  <read_first>src/components/app-shell.tsx, src/app/layout.tsx, src/lib/releases.ts, src/lib/navigation.ts, tests/e2e/local-smoke.spec.ts</read_first>
  <action>Update the app shell so the global header uses `getActiveReleaseSummary()` when an `IN_PROGRESS` release exists. Because `AppShell` is currently a client component, split only what is necessary to keep pathname-based navigation working while allowing server-side active release data to reach the header. The header must display the active release name, status label `In progress`, and a capacity placeholder `Capacity: --` until Phase 4 capacity is implemented. Preserve the existing fallback text `No active release`, `Not configured`, and `Capacity: --` when there is no active release. Do not add authentication, remote data fetching, or external integration.</action>
  <verify>npm run test:e2e -- release-management; npm run build</verify>
  <acceptance_criteria>
    - After creating an `IN_PROGRESS` release named `Release Q3 2026`, the global header shows `Release Q3 2026`.
    - When no `IN_PROGRESS` release exists, the global header still shows `No active release`.
    - `src/components/app-shell.tsx` or a new child component preserves `usePathname` only in client-side navigation code.
    - `npm run build` exits 0 with the app shell changes.
  </acceptance_criteria>
  <done>The application-level release context reflects real local release data.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- releases` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- release-management` succeeds.
- [ ] Manual browser check can create, open, edit, and reload a release while seeing generated sprints.
</verification>

<success_criteria>
- REL-01 is complete for create, edit, list, and detail release flows.
- REL-02 remains enforced on create and edit.
- REL-03 and REL-04 remain true after release date/default-length edits through sprint reconciliation.
- Release detail provides sprint-level planning context needed by Phase 3 and Phase 4 without inventing capacity values.
</success_criteria>

<output>
After completion, create `.planning/phases/02-release-and-sprint-planning-core/02-02-SUMMARY.md`.
</output>
