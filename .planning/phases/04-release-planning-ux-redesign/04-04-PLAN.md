---
phase: 04-release-planning-ux-redesign
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
  - 03-04
files_modified:
  - src/App.tsx
  - src/components/ReleaseDetailScreen.tsx
  - src/components/SprintDetailScreen.tsx
  - src/components/SprintList.tsx
  - src/components/SprintPlanningWorkspace.tsx
  - src/components/SprintBoard.tsx
  - src/components/SprintCapacitySummary.tsx
  - src/components/SprintClosePanel.tsx
autonomous: true
requirements:
  - UX-01
  - REL-01
user_setup: []

must_haves:
  truths:
    - "Phase 4 context: sprint list remains a place to browse available sprints"
    - "Phase 4 context: clicking a sprint opens a dedicated sprint screen"
    - "Phase 4 context: sprint screen uses tabs Board, Planning, Capacity, and Closure"
    - "Research: selected sprint rendering in App.tsx is the current ambiguity to remove"
    - "UI-SPEC: sprint drill-down opens a dedicated sprint screen, not inline content below the list"
  artifacts:
    - path: "src/components/SprintDetailScreen.tsx"
      provides: "Dedicated sprint screen with Board, Planning, Capacity, Closure tabs"
      exports: ["SprintDetailScreen"]
    - path: "src/App.tsx"
      provides: "Top-level screen state for release detail and sprint detail"
      exports: []
    - path: "src/components/ReleaseDetailScreen.tsx"
      provides: "Sprint open/drill-down entry from release context"
      exports: ["ReleaseDetailScreen"]
  key_links:
    - from: "src/components/ReleaseDetailScreen.tsx"
      to: "src/components/SprintDetailScreen.tsx"
      via: "open sprint callback in App"
      pattern: "onOpenSprint"
    - from: "src/components/SprintDetailScreen.tsx"
      to: "src/components/SprintPlanningWorkspace.tsx"
      via: "Planning tab"
      pattern: "SprintPlanningWorkspace"
    - from: "src/components/SprintDetailScreen.tsx"
      to: "src/components/SprintBoard.tsx"
      via: "Board tab"
      pattern: "SprintBoard"
---

<objective>
Separate sprint browsing from sprint execution. Clicking a sprint opens a dedicated sprint detail screen with tabs for Board, Planning, Capacity, and Closure instead of rendering planning workspace and board inline under the sprint list.

Purpose: Remove the ambiguous Phase 3 one-screen sprint UX while preserving the working sprint planning and board features.
Output: SprintDetailScreen, App screen navigation state, release-to-sprint drill-down, and cleaned-up sprint list behavior.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-release-planning-ux-redesign/04-CONTEXT.md
@.planning/phases/04-release-planning-ux-redesign/04-RESEARCH.md
@.planning/phases/04-release-planning-ux-redesign/04-UI-SPEC.md
@.planning/phases/04-release-planning-ux-redesign/04-03-PLAN.md
@.planning/phases/03-sprint-planning-board-estimation/03-04-PLAN.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce app-level screen navigation for sprint detail</name>
  <files>src/App.tsx, src/components/ReleaseDetailScreen.tsx, src/components/SprintList.tsx</files>
  <read_first>
    src/App.tsx
    src/components/ReleaseDetailScreen.tsx
    src/components/SprintList.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Replace the old selectedSprint inline rendering with explicit screen state. A simple local state machine is enough:
    - "home" or active top-level tab.
    - selected release detail.
    - selected sprint detail with optional parent release context.
    In ReleaseDetailScreen, pass an onOpenSprint callback to SprintList. In any remaining legacy Sprints tab, clicking a sprint should also open SprintDetailScreen instead of selecting it inline.
    Update SprintList copy/action from "Selecionar" to "Abrir sprint" or equivalent action-specific copy.
  </action>
  <acceptance_criteria>
    - Clicking a sprint no longer renders planning workspace and board below the list.
    - App can navigate back from sprint detail to the previous release detail or sprint list.
    - SprintList remains a browse surface.
    - No URL router package is added.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Sprint drill-down state replaces inline selected sprint rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Build SprintDetailScreen tabs</name>
  <files>src/components/SprintDetailScreen.tsx</files>
  <read_first>
    src/components/SprintPlanningWorkspace.tsx
    src/components/SprintBoard.tsx
    src/components/SprintCapacitySummary.tsx
    src/components/SprintClosePanel.tsx
    src/domain/types.ts
    .planning/phases/04-release-planning-ux-redesign/04-UI-SPEC.md
  </read_first>
  <action>
    Create SprintDetailScreen with:
    - Header showing sprint goal, release name when available, date range, status chip, and back action.
    - Text tabs: Board, Planning, Capacity, Closure. Default to Board.
    - Board tab renders SprintBoard.
    - Planning tab renders SprintPlanningWorkspace for non-closed sprints and read-only message for closed sprints.
    - Capacity tab renders capacity summary and sprint totals. Reuse SprintCapacitySummary or extract a small wrapper if current component expects planning props.
    - Closure tab renders SprintClosePanel with the current sprint items/totals.
    If SprintBoard currently owns SprintClosePanel, move close rendering out so Closure is not duplicated.
  </action>
  <acceptance_criteria>
    - Sprint detail screen has Board, Planning, Capacity, and Closure tabs.
    - Board tab shows the Kanban board.
    - Planning tab shows add/remove sprint scope.
    - Capacity tab shows sprint capacity and estimate totals.
    - Closure tab shows close sprint action and warnings.
    - Closed sprint remains viewable and read-only where mutations would occur.
    - SprintClosePanel is not duplicated on multiple tabs.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Dedicated sprint screen exposes daily workflow through clear tabs.</done>
</task>

<task type="auto">
  <name>Task 3: Decouple sprint board and close panel responsibilities</name>
  <files>src/components/SprintBoard.tsx, src/components/SprintClosePanel.tsx, src/components/SprintPlanningWorkspace.tsx</files>
  <read_first>
    src/components/SprintBoard.tsx
    src/components/SprintClosePanel.tsx
    src/components/SprintPlanningWorkspace.tsx
  </read_first>
  <action>
    Refactor SprintBoard so it focuses on board display and board persistence. It should expose board item state changes through callbacks or allow SprintDetailScreen to fetch board items/totals for Closure.
    Ensure SprintClosePanel can be rendered independently in the Closure tab with provided items/totals and can notify SprintDetailScreen when the sprint closes.
    Ensure SprintPlanningWorkspace remains usable inside the Planning tab and triggers board refresh when scope changes.
  </action>
  <acceptance_criteria>
    - SprintBoard does not render SprintClosePanel directly.
    - Closing a sprint from Closure tab updates SprintDetailScreen state and makes Planning/Board read-only.
    - Adding/removing sprint scope refreshes Board tab data when the user switches back.
    - Existing board drag/drop behavior still works.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Sprint detail composes board, planning, capacity, and closure without duplicate UI.</done>
</task>

<task type="auto">
  <name>Task 4: Clean up legacy Sprints tab behavior</name>
  <files>src/App.tsx, src/components/SprintList.tsx</files>
  <read_first>
    src/App.tsx
    src/components/SprintList.tsx
    .planning/phases/04-release-planning-ux-redesign/04-CONTEXT.md
  </read_first>
  <action>
    If a legacy top-level Sprints tab remains, make it a browse/search surface only:
    - No inline SprintForm for global sprint creation unless it is clearly legacy/read-only or hidden behind release context.
    - No selected sprint banner.
    - No SprintPlanningWorkspace.
    - No SprintBoard.
    Add copy directing users to create sprints inside a release if they try to create globally.
  </action>
  <acceptance_criteria>
    - Sprint list and sprint board are separated.
    - Sprint creation is release-first.
    - The current ambiguous one-screen sprint tab is gone.
    - Existing sprint records remain discoverable.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
    <manual>Open Sprints tab and verify it is browse/drill-down only.</manual>
  </verify>
  <done>Legacy sprint tab no longer mixes creation, planning, and board in one screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screen navigation state | User switches between release planning and sprint execution without a router |
| Sprint detail mutations | Planning, board, and closure tabs mutate the same sprint state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | Closed sprint mutated from a hidden tab | mitigate | Preserve API closed-sprint guards and disable UI mutations after close |
| T-04-15 | Information Integrity | User edits wrong sprint due to stale selected state | mitigate | Dedicated detail header and explicit back navigation |
| T-04-16 | Denial of Service | Refactor duplicates board fetches and creates stale UI | mitigate | Central refresh callbacks in SprintDetailScreen |
</threat_model>

<verification>
1. Run `bun run build`.
2. Run `bun run dev`.
3. Open a release, open a sprint from that release, and verify SprintDetailScreen appears.
4. Switch between Board, Planning, Capacity, and Closure tabs.
5. Add/remove an item in Planning and verify Board updates.
6. Move an item on Board and verify it persists.
7. Close the sprint from Closure and verify Planning/Board become read-only.
8. Open the legacy Sprints tab and verify it does not render workspace or board inline.
</verification>

<success_criteria>
- User opens a sprint into a dedicated sprint screen.
- Sprint screen has Board, Planning, Capacity, and Closure tabs.
- Sprint list is only for browsing and drill-down.
- Release planning, sprint planning, and sprint board are no longer all rendered on one page.
- Existing Phase 3 sprint functionality still works inside the dedicated screen.
</success_criteria>

<output>
Create `.planning/phases/04-release-planning-ux-redesign/04-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Components:**
- `SprintDetailScreen`

**Modified Components:**
- `App`
- `ReleaseDetailScreen`
- `SprintList`
- `SprintBoard`
- `SprintClosePanel`
- `SprintPlanningWorkspace`

**New File Paths:**
- `src/components/SprintDetailScreen.tsx`
