---
phase: 04-release-planning-ux-redesign
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
  - 04-01
files_modified:
  - src/App.tsx
  - src/components/BacklogForm.tsx
  - src/components/BacklogList.tsx
  - src/components/FeatureFirstBacklog.tsx
  - src/components/FeatureCard.tsx
  - src/data/backlog-repository.ts
  - server.ts
autonomous: true
requirements:
  - UX-02
  - REL-03
user_setup: []

must_haves:
  truths:
    - "Phase 4 context: backlog management should become feature-first"
    - "Phase 4 context: creating stories and bugs should happen inside a feature context"
    - "Phase 4 context: root-level work should primarily be features, with optional epics above them"
    - "Phase 4 context: the form should reduce ambiguity around type, parent, and status"
    - "Research: root-level story/bug creation is incompatible with REL-03 and UX-02"
    - "UI-SPEC: Feature creation CTA is Criar feature and story/bug creation CTA is Adicionar historia ou bug"
  artifacts:
    - path: "src/components/FeatureFirstBacklog.tsx"
      provides: "Feature-first backlog management surface"
      exports: ["FeatureFirstBacklog"]
    - path: "src/components/FeatureCard.tsx"
      provides: "Feature card with child story/bug creation actions"
      exports: ["FeatureCard"]
    - path: "src/components/BacklogForm.tsx"
      provides: "Context-aware form that can create features or children inside a feature"
      exports: ["BacklogForm"]
  key_links:
    - from: "src/components/FeatureFirstBacklog.tsx"
      to: "/api/backlog"
      via: "feature-first CRUD and child creation"
      pattern: "fetch.*/api/backlog"
    - from: "src/components/BacklogForm.tsx"
      to: "src/data/backlog-repository.ts"
      via: "UI parent context plus repository validation"
      pattern: "parent_id"
---

<objective>
Redesign backlog management around features. Users create features first, then add stories or bugs inside a feature context; root-level orphan stories and bugs disappear from the normal UI and remain blocked at the API/repository/database layers.

Purpose: This resolves the ambiguous backlog creation flow and completes the release-first hierarchy contract.
Output: FeatureFirstBacklog, context-aware BacklogForm, feature child actions, and final API/repository hardening for orphan prevention.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-release-planning-ux-redesign/04-CONTEXT.md
@.planning/phases/04-release-planning-ux-redesign/04-RESEARCH.md
@.planning/phases/04-release-planning-ux-redesign/04-UI-SPEC.md
@.planning/phases/04-release-planning-ux-redesign/04-01-PLAN.md
@.planning/phases/04-release-planning-ux-redesign/04-04-PLAN.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make BacklogForm context-aware</name>
  <files>src/components/BacklogForm.tsx</files>
  <read_first>
    src/components/BacklogForm.tsx
    src/domain/types.ts
    .planning/phases/04-release-planning-ux-redesign/04-UI-SPEC.md
  </read_first>
  <action>
    Refactor BacklogForm so it supports two explicit modes:
    - mode="feature": creates or edits a feature, with optional epic parent if epics are present.
    - mode="child": creates or edits a story or bug under a required feature parent.
    In child mode:
    - Hide epic/feature type choices and allow only story/bug.
    - parent_id is fixed to the feature context and not user-selected.
    - Show story_points and estimate_days fields.
    In feature mode:
    - Default type is feature.
    - Hide story/bug estimate fields.
    - Use CTA copy "Criar feature".
    Preserve edit behavior for existing items, but never allow a story/bug edit to remove its feature parent.
  </action>
  <acceptance_criteria>
    - User cannot create a root-level story or bug through the form.
    - Story/bug creation requires a feature context.
    - Feature creation is a first-class form mode.
    - Existing estimate controls still work for stories and bugs.
    - User-facing button labels are action-specific, not generic.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>BacklogForm supports feature-first creation and child creation inside a feature.</done>
</task>

<task type="auto">
  <name>Task 2: Build FeatureFirstBacklog surface</name>
  <files>src/components/FeatureFirstBacklog.tsx, src/components/FeatureCard.tsx, src/components/BacklogList.tsx, src/App.tsx</files>
  <read_first>
    src/components/BacklogList.tsx
    src/App.tsx
    src/components/BacklogForm.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Create FeatureFirstBacklog as the main Backlog tab content.
    It should:
    - Show a feature creation form or compact "Criar feature" action.
    - List features as cards with title, status, aggregate points/days, story/bug counts, and optional epic label.
    - Allow expanding a feature to view child stories and bugs.
    - Provide "Adicionar historia ou bug" inside each feature card, opening BacklogForm in child mode.
    - Keep edit/delete actions for features and children using existing APIs.
    - Optionally show epics as a lightweight grouping/filter if present, but do not make epics mandatory.
    BacklogList may remain as an internal tree component or be replaced on the Backlog tab by FeatureFirstBacklog.
  </action>
  <acceptance_criteria>
    - Backlog tab starts from features, not from a generic item form.
    - User creates a feature before creating story/bug children.
    - User can add stories and bugs inside a feature.
    - Feature cards show aggregate estimate information.
    - Root-level stories and bugs are not presented as the normal creation path.
    - Existing drag/drop priority for backlog items remains available for features and children where practical.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Backlog tab becomes feature-first and less ambiguous.</done>
</task>

<task type="auto">
  <name>Task 3: Harden backlog API errors for orphan prevention</name>
  <files>server.ts, src/data/backlog-repository.ts</files>
  <read_first>
    server.ts
    src/data/backlog-repository.ts
    src/domain/types.ts
  </read_first>
  <action>
    Confirm server.ts returns clear 400 errors from BacklogRepository when story/bug parent validation fails. Avoid generic 500 for expected validation failures.
    Add helper functions in BacklogRepository if needed:
    - isFeature(id)
    - validateExecutableParent(type, parentId)
    Ensure update(id, changes) validates the effective item type and effective parent_id, not only the changed fields.
    Ensure delete(id) for a feature with story/bug children returns a clear error instead of a silent SQLite failure, if the schema trigger from 04-01 is in place.
  </action>
  <acceptance_criteria>
    - POST /api/backlog with story/bug and no parent returns 400.
    - POST /api/backlog with story/bug and non-feature parent returns 400.
    - PUT /api/backlog/:id cannot remove a story/bug parent.
    - DELETE /api/backlog/:id for a feature with story/bug children returns a clear 400 or 409.
    - Unexpected errors still return 500 where appropriate.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
    <manual>Use curl to verify orphan story/bug create and parent-removal update both fail with 400.</manual>
  </verify>
  <done>Backlog API error handling matches the feature-first invariant.</done>
</task>

<task type="auto">
  <name>Task 4: Connect release feature workflows to feature-first backlog</name>
  <files>src/components/ReleaseFeatureBacklog.tsx, src/components/FeatureFirstBacklog.tsx, src/App.tsx</files>
  <read_first>
    src/components/ReleaseFeatureBacklog.tsx
    src/components/FeatureFirstBacklog.tsx
    src/App.tsx
  </read_first>
  <action>
    Ensure the release feature backlog panel and the Backlog tab use the same feature-first assumptions:
    - Newly created features can immediately appear as available release features after refresh.
    - Stories/bugs added inside a feature affect aggregate estimates visible on release board after refresh.
    - Copy consistently refers to features as the release planning unit and stories/bugs as execution items.
    Add a lightweight "Criar feature" entry from ReleaseFeatureBacklog if no available feature exists, opening or navigating to the feature creation flow without duplicating full backlog CRUD inside the release board.
  </action>
  <acceptance_criteria>
    - Creating a feature in backlog makes it available to release planning.
    - Adding child stories/bugs updates release estimate aggregates after refresh.
    - Release board does not expose generic root-level story/bug creation.
    - User can recover from empty feature backlog by creating a feature.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
    <manual>Create a feature, add a story under it, add feature to release, and verify estimate appears on release timeline.</manual>
  </verify>
  <done>Feature-first backlog and release planning reinforce the same hierarchy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser backlog form to API | User input defines backlog hierarchy and executable work |
| Backlog API to SQLite | Repository and triggers enforce parent-child invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | User creates root-level story/bug through stale UI | mitigate | Remove generic creation path and keep API validation |
| T-04-18 | Tampering | User edits story/bug to remove parent | mitigate | Validate effective type/parent on update |
| T-04-19 | Denial of Service | Deleting feature with children breaks feature cards | mitigate | Block delete with clear message until children are moved/deleted |
| T-04-20 | Information Integrity | Release estimates stale after child changes | mitigate | Refresh release board after feature/backlog changes |
</threat_model>

<verification>
1. Run `bun run build`.
2. Run `bun run dev`.
3. Open Backlog tab and create a feature.
4. Add a story and a bug inside that feature.
5. Verify neither story nor bug can be created without a feature via the UI.
6. Use the API to attempt orphan story creation; verify 400.
7. Add the feature to a release and verify aggregate estimates appear on the release board.
8. Try deleting a feature with children and verify a clear blocking message.
</verification>

<success_criteria>
- User manages backlog through a clearer feature-first flow.
- Stories and bugs are created inside feature context.
- Root-level story/bug creation is blocked in UI and API.
- Feature cards expose child work and aggregate estimates.
- Release planning consumes feature estimates from child stories/bugs.
</success_criteria>

<output>
Create `.planning/phases/04-release-planning-ux-redesign/04-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Components:**
- `FeatureFirstBacklog`
- `FeatureCard`

**Modified Components:**
- `BacklogForm`
- `BacklogList`
- `ReleaseFeatureBacklog`
- `App`

**New File Paths:**
- `src/components/FeatureFirstBacklog.tsx`
- `src/components/FeatureCard.tsx`
