---
phase: 02-squad-capacity-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/data/schema.ts
  - src/domain/types.ts
  - src/data/squad-repository.ts
  - src/data/absence-repository.ts
  - server.ts
  - src/App.tsx
  - src/components/SquadMemberForm.tsx
  - src/components/SquadMemberList.tsx
  - src/components/AbsenceForm.tsx
  - src/components/AbsenceList.tsx
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
user_setup: []

must_haves:
  truths:
    - "User can register squad members with name, role, and daily capacity via the web UI"
    - "User can register absences (vacation, sick leave, unpaid leave, holidays) for squad members"
    - "Holidays can be registered as squad-wide absences that apply to all members"
    - "Data persists to SQLite and survives server restarts"
  artifacts:
    - path: "src/data/schema.ts"
      provides: "SQLite schema with squad_members, absences tables"
      contains: "squad_members, absences"
    - path: "src/domain/types.ts"
      provides: "Domain types for SquadMember, NewSquadMember, Absence, NewAbsence"
      exports: []
    - path: "src/data/squad-repository.ts"
      provides: "Repository for squad member CRUD"
      exports: ["SquadRepository"]
    - path: "src/data/absence-repository.ts"
      provides: "Repository for absence CRUD"
      exports: ["AbsenceRepository"]
    - path: "server.ts"
      provides: "REST API endpoints for /api/squad and /api/absences"
      exports: []
    - path: "src/components/SquadMemberForm.tsx"
      provides: "React form for creating and editing squad members"
      exports: ["SquadMemberForm"]
    - path: "src/components/SquadMemberList.tsx"
      provides: "React list showing squad members"
      exports: ["SquadMemberList"]
    - path: "src/components/AbsenceForm.tsx"
      provides: "React form for creating and editing absences"
      exports: ["AbsenceForm"]
    - path: "src/components/AbsenceList.tsx"
      provides: "React list showing absences"
      exports: ["AbsenceList"]
  key_links:
    - from: "server.ts"
      to: "src/data/squad-repository.ts"
      via: "import and instantiation"
      pattern: "new SquadRepository"
    - from: "server.ts"
      to: "src/data/absence-repository.ts"
      via: "import and instantiation"
      pattern: "new AbsenceRepository"
    - from: "src/App.tsx"
      to: "/api/squad"
      via: "fetch GET/POST/PUT/DELETE"
      pattern: "fetch.*/api/squad"
    - from: "src/App.tsx"
      to: "/api/absences"
      via: "fetch GET/POST/PUT/DELETE"
      pattern: "fetch.*/api/absences"
---

<objective>
Build squad management and absence tracking. Users can register squad members (name, role, daily capacity) and record absences (vacation, sick leave, unpaid leave, holidays). Holidays can be squad-wide. All data persists to SQLite.

Purpose: This is the data foundation for capacity calculation in later plans. Without members and absences, there is no input for capacity.
Output: Working CRUD UI for squad members and absences, REST API, and SQLite schema.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-squad-capacity-engine/02-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install date-fns and extend schema</name>
  <files>package.json, src/data/schema.ts, src/domain/types.ts</files>
  <read_first>
    src/data/schema.ts
    src/domain/types.ts
    package.json
  </read_first>
  <action>
    Install date-fns@4.4.0 by adding it to package.json dependencies: "date-fns": "4.4.0". Run bun install.
    In src/data/schema.ts, add two new CREATE TABLE IF NOT EXISTS statements inside initSchema:
    1) squad_members: id TEXT PRIMARY KEY, name TEXT NOT NULL, role TEXT NOT NULL, daily_capacity_hours REAL NOT NULL DEFAULT 6.0, created_at TEXT DEFAULT CURRENT_TIMESTAMP, updated_at TEXT DEFAULT CURRENT_TIMESTAMP.
    2) absences: id TEXT PRIMARY KEY, member_id TEXT REFERENCES squad_members(id) ON DELETE CASCADE, type TEXT NOT NULL CHECK(type IN ('vacation','sick_leave','unpaid_leave','holiday','other')), start_date TEXT NOT NULL, end_date TEXT NOT NULL, description TEXT, created_at TEXT DEFAULT CURRENT_TIMESTAMP. Note: member_id is nullable; when null, the absence is treated as a squad-wide holiday.
    In src/domain/types.ts, add interfaces: SquadMember { id: string; name: string; role: string; daily_capacity_hours: number; created_at: string; updated_at: string; }, NewSquadMember { name: string; role: string; daily_capacity_hours?: number; }, UpdateSquadMember = Partial<Omit<SquadMember, "id" | "created_at" | "updated_at">>. Add Absence { id: string; member_id: string | null; type: string; start_date: string; end_date: string; description: string | null; created_at: string; }, NewAbsence { member_id?: string | null; type: string; start_date: string; end_date: string; description?: string; }, UpdateAbsence = Partial<Omit<Absence, "id" | "created_at">>.
  </action>
  <acceptance_criteria>
    - package.json contains "date-fns": "4.4.0" in dependencies.
    - schema.ts creates squad_members and absences tables with exact columns specified.
    - absences table has member_id as nullable with foreign key ON DELETE CASCADE.
    - absences.type has CHECK constraint for ('vacation','sick_leave','unpaid_leave','holiday','other').
    - domain/types.ts exports SquadMember, NewSquadMember, UpdateSquadMember, Absence, NewAbsence, UpdateAbsence interfaces.
  </acceptance_criteria>
  <verify>
    <automated>bun install && test -f node_modules/date-fns/package.json</automated>
  </verify>
  <done>date-fns installed, schema extended, types defined.</done>
</task>

<task type="auto">
  <name>Task 2: Create SquadRepository and AbsenceRepository</name>
  <files>src/data/squad-repository.ts, src/data/absence-repository.ts</files>
  <read_first>
    src/data/schema.ts
    src/domain/types.ts
    src/data/backlog-repository.ts
  </read_first>
  <action>
    Create src/data/squad-repository.ts with class SquadRepository, constructor(db: Database). Implement: findAll() returning SquadMember[] ordered by name; findById(id) returning SquadMember | null; create(member: NewSquadMember) generating id with crypto.randomUUID(), inserting, and returning created item; update(id, changes) building dynamic UPDATE like backlog-repository; delete(id) returning boolean; findByName(name) for exact match (case-insensitive via COLLATE NOCASE or lower()).
    Create src/data/absence-repository.ts with class AbsenceRepository, constructor(db: Database). Implement: findAll() returning Absence[] ordered by start_date DESC; findById(id); create(absence: NewAbsence) with id from crypto.randomUUID(); update(id, changes); delete(id); findByMember(memberId) returning absences for a specific member ordered by start_date DESC; findByDateRange(start, end) returning absences overlapping the range (SQL: start_date <= ? AND end_date >= ?); findHolidays() returning absences where member_id IS NULL.
  </action>
  <acceptance_criteria>
    - SquadRepository exports class with findAll, findById, create, update, delete methods.
    - AbsenceRepository exports class with findAll, findById, create, update, delete, findByMember, findByDateRange, findHolidays methods.
    - Both repositories use parameterized queries (?, ?) — no string interpolation in SQL.
    - create methods use crypto.randomUUID() for id generation.
    - findByDateRange uses correct overlap condition: start_date <= end_param AND end_date >= start_param.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; PID=$! &amp;&amp; sleep 2 &amp;&amp; curl -s http://localhost:3000/api/squad | grep -c "\[\]" &amp;&amp; curl -s http://localhost:3000/api/absences | grep -c "\[\]" &amp;&amp; kill $PID</automated>
  </verify>
  <done>Repositories created with full CRUD and query methods.</done>
</task>

<task type="auto">
  <name>Task 3: Add REST API endpoints for squad and absences</name>
  <files>server.ts</files>
  <read_first>
    server.ts
    src/data/squad-repository.ts
    src/data/absence-repository.ts
    src/domain/types.ts
  </read_first>
  <action>
    In server.ts, import SquadRepository and AbsenceRepository. Instantiate both after backlogRepo. Add Zod schemas: squadMemberSchema with name (string, min 1), role (string, min 1), daily_capacity_hours (number, positive, default 6). absenceSchema with member_id (string, nullable, optional), type (enum vacation/sick_leave/unpaid_leave/holiday/other), start_date (string, regex YYYY-MM-DD), end_date (string, regex YYYY-MM-DD), description (string, optional). Add API routes: GET/POST /api/squad, GET/PUT/DELETE /api/squad/:id, GET/POST /api/absences, GET/PUT/DELETE /api/absences/:id, GET /api/absences/holidays (returns squad-wide holidays). For POST /api/absences, if type is "holiday" and no member_id is provided, store member_id as NULL. Validation: end_date must be >= start_date.
  </action>
  <acceptance_criteria>
    - server.ts imports and instantiates SquadRepository and AbsenceRepository.
    - GET /api/squad returns array of squad members.
    - POST /api/squad creates a member and returns it with 201.
    - PUT /api/squad/:id updates a member and returns it.
    - DELETE /api/squad/:id returns 204 on success, 404 if not found.
    - GET /api/absences returns array of absences.
    - POST /api/absences creates an absence with validation of date format and end_date >= start_date.
    - GET /api/absences/holidays returns absences where member_id is null.
    - All endpoints use Zod for input validation and return 400 with error messages on invalid input.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; PID=$! &amp;&amp; sleep 2 &amp;&amp; curl -s -X POST -H "Content-Type: application/json" -d '{"name":"Alice","role":"Dev","daily_capacity_hours":6}' http://localhost:3000/api/squad | grep -c "Alice" &amp;&amp; curl -s http://localhost:3000/api/squad | grep -c "Dev" &amp;&amp; curl -s -X POST -H "Content-Type: application/json" -d '{"type":"vacation","start_date":"2026-06-01","end_date":"2026-06-05"}' http://localhost:3000/api/absences | grep -c "vacation" &amp;&amp; kill $PID</automated>
  </verify>
  <done>API endpoints for squad and absences are live and tested via curl.</done>
</task>

<task type="auto">
  <name>Task 4: Create React UI for squad members</name>
  <files>src/App.tsx, src/components/SquadMemberForm.tsx, src/components/SquadMemberList.tsx</files>
  <read_first>
    src/App.tsx
    src/components/BacklogForm.tsx
    src/components/BacklogList.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Create src/components/SquadMemberForm.tsx: functional component with props onSubmit, initialMember?, onCancel?. State for name (string), role (string), daily_capacity_hours (number, default 6). Form validation: name and role are required. On submit, call onSubmit with {name, role, daily_capacity_hours}. Use Tailwind classes consistent with BacklogForm (rounded-lg, border, bg-white, p-4, input styling).
    Create src/components/SquadMemberList.tsx: functional component with props members: SquadMember[], onEdit, onDelete. Display as table with columns: Nome, Função, Capacidade Diária (h), Ações. Include edit and delete buttons. Delete confirms with window.confirm.
    In src/App.tsx: add "Squad" tab. State for squadMembers (SquadMember[]), editingMember (SquadMember | null), squadRefreshKey. On mount and when tab is active, fetch /api/squad and set items. Handle create/update/delete with fetch calls. Render SquadMemberForm and SquadMemberList inside the Squad tab.
  </action>
  <acceptance_criteria>
    - SquadMemberForm has inputs for name (text), role (text), daily_capacity_hours (number, min 0.5, step 0.5, default 6).
    - SquadMemberForm validates that name and role are non-empty before submit.
    - SquadMemberList renders a table with all members and edit/delete buttons.
    - App.tsx has a third tab "Squad" alongside "Backlog" and "Sincronização".
    - Clicking "Squad" tab shows the form and list.
    - Creating a member via form immediately appears in the list.
    - Editing a member updates it in the list.
    - Deleting a member removes it after confirmation.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Squad UI is functional and builds without errors.</done>
</task>

<task type="auto">
  <name>Task 5: Create React UI for absences</name>
  <files>src/App.tsx, src/components/AbsenceForm.tsx, src/components/AbsenceList.tsx</files>
  <read_first>
    src/App.tsx
    src/components/SquadMemberForm.tsx
    src/components/SquadMemberList.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Create src/components/AbsenceForm.tsx: functional component with props onSubmit, members: SquadMember[], initialAbsence?, onCancel?. State for member_id (string | null), type (string), start_date (string), end_date (string), description (string). Member dropdown: option with empty value for "Squad-wide (holiday)" plus one option per member. Type dropdown: vacation, sick_leave, unpaid_leave, holiday, other. Date inputs use type="date". Validate: start_date <= end_date. On submit, call onSubmit.
    Create src/components/AbsenceList.tsx: functional component with props absences: Absence[], members: SquadMember[], onEdit, onDelete. Display as table with columns: Membro, Tipo, Início, Fim, Descrição, Ações. For member_id null, display "Toda a Squad". Type labels map to Portuguese (Férias, Licença Médica, Licença Não Paga, Feriado, Outro).
    In src/App.tsx: add "Ausências" tab. State for absences, editingAbsence, absenceRefreshKey. Fetch /api/absences and squad members. Render AbsenceForm and AbsenceList. Handle create/update/delete.
  </action>
  <acceptance_criteria>
    - AbsenceForm has a member dropdown with "Toda a Squad (feriado)" option and all squad members.
    - AbsenceForm has type dropdown with 5 options.
    - AbsenceForm uses HTML5 date inputs for start_date and end_date.
    - AbsenceForm validates end_date >= start_date.
    - AbsenceList shows member name or "Toda a Squad" for null member_id.
    - AbsenceList shows translated type labels in Portuguese.
    - App.tsx has an "Ausências" tab that displays the form and list.
    - Creating an absence appears immediately in the list.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Absence UI is functional and builds without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Bun server | User input crosses to local server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SQL injection in absence date range | mitigate | Repository uses parameterized queries; no string interpolation |
| T-02-02 | Information Disclosure | Member absence data is sensitive | accept | Local-only app; no external network exposure; corporate network is trust boundary |
| T-02-03 | Denial of Service | Invalid date ranges | mitigate | Zod validation ensures start_date <= end_date |
</threat_model>

<verification>
1. Run `bun install` — completes with zero errors.
2. Run `bun run dev` — server starts.
3. Open browser to `http://localhost:3000`, click "Squad" tab — form and list visible.
4. Create a squad member "João Silva", role "Dev", capacity 6 — appears in list.
5. Create another member "Maria Souza", role "QA", capacity 5 — appears in list.
6. Click "Ausências" tab — create absence for João, type "Férias", dates 2026-06-01 to 2026-06-05 — appears in list.
7. Create a squad-wide holiday "Feriado", dates 2026-06-15 to 2026-06-15, no member selected — appears as "Toda a Squad".
8. Stop server, restart — all members and absences persist.
</verification>

<success_criteria>
- Squad members can be created, edited, deleted via web UI.
- Absences can be created, edited, deleted via web UI.
- Holidays can be created as squad-wide absences (no member selected).
- All data persists to SQLite.
- UI is in Portuguese where appropriate (labels, type translations).
- date-fns is installed for later plans.
</success_criteria>

<output>
Create `.planning/phases/02-squad-capacity-engine/02-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Decorators / Classes:**
- `SquadRepository` (src/data/squad-repository.ts) — repository class for squad member CRUD.
- `AbsenceRepository` (src/data/absence-repository.ts) — repository class for absence CRUD.

**Functions / Methods:**
- `SquadRepository.findAll()` — returns all members ordered by name.
- `SquadRepository.create(member)` — inserts a new member.
- `SquadRepository.findByName(name)` — case-insensitive exact match.
- `AbsenceRepository.findAll()` — returns all absences ordered by start_date DESC.
- `AbsenceRepository.findByDateRange(start, end)` — returns absences overlapping the range.
- `AbsenceRepository.findHolidays()` — returns squad-wide absences (member_id IS NULL).
- `AbsenceRepository.findByMember(memberId)` — returns absences for a specific member.

**CLI Flags / Scripts:**
- `bun run dev` — starts server with new API endpoints.

**Struct / Dataclass Fields:**
- `squad_members` table: id, name, role, daily_capacity_hours, created_at, updated_at.
- `absences` table: id, member_id, type, start_date, end_date, description, created_at.
- Absence types: vacation, sick_leave, unpaid_leave, holiday, other.

**New File Paths:**
- `src/data/squad-repository.ts`
- `src/data/absence-repository.ts`
- `src/components/SquadMemberForm.tsx`
- `src/components/SquadMemberList.tsx`
- `src/components/AbsenceForm.tsx`
- `src/components/AbsenceList.tsx`
