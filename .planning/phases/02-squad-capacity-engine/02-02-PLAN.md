---
phase: 02-squad-capacity-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/services/capacity-service.ts
  - src/domain/types.ts
  - server.ts
  - src/App.tsx
  - src/components/CapacityView.tsx
autonomous: true
requirements:
  - CAP-01
user_setup: []

must_haves:
  truths:
    - "System calculates realistic sprint capacity adjusting for absences and holidays"
    - "Capacity view shows a date-range selector and per-member breakdown"
    - "Capacity calculation counts working days excluding weekends and holidays"
    - "Absence hours are deducted from raw capacity based on overlap with the selected range"
  artifacts:
    - path: "src/services/capacity-service.ts"
      provides: "Pure business logic for capacity calculation"
      exports: ["CapacityService"]
    - path: "src/domain/types.ts"
      provides: "CapacityBreakdown and CapacityResult types"
      exports: ["CapacityBreakdown", "CapacityResult"]
    - path: "server.ts"
      provides: "REST API endpoint for capacity queries"
      exports: []
    - path: "src/components/CapacityView.tsx"
      provides: "React component for capacity visualization"
      exports: ["CapacityView"]
  key_links:
    - from: "src/services/capacity-service.ts"
      to: "src/data/squad-repository.ts"
      via: "import and read members"
      pattern: "squadRepo"
    - from: "src/services/capacity-service.ts"
      to: "src/data/absence-repository.ts"
      via: "import and read absences"
      pattern: "absenceRepo"
    - from: "server.ts"
      to: "src/services/capacity-service.ts"
      via: "import and call calculate"
      pattern: "CapacityService"
    - from: "src/App.tsx"
      to: "/api/capacity"
      via: "fetch GET with date range params"
      pattern: "fetch.*/api/capacity"
---

<objective>
Build the capacity calculation engine. Given a date range, the system computes realistic available capacity per squad member and total, deducting absences and holidays. The user selects a date range in the UI and sees a transparent per-member breakdown.

Purpose: This is the core value of Scrumbag — realistic capacity planning. It transforms raw squad data into actionable planning information.
Output: Capacity service class, API endpoint, and React view showing breakdown.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-squad-capacity-engine/02-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capacity domain types</name>
  <files>src/domain/types.ts</files>
  <read_first>
    src/domain/types.ts
  </read_first>
  <action>
    In src/domain/types.ts, add interfaces:
    CapacityBreakdown { member_id: string; member_name: string; role: string; daily_capacity_hours: number; raw_capacity_hours: number; absence_hours: number; holiday_hours: number; real_capacity_hours: number; }.
    CapacityResult { start_date: string; end_date: string; total_members: number; total_raw_hours: number; total_absence_hours: number; total_holiday_hours: number; total_real_hours: number; members: CapacityBreakdown[]; }.
    Ensure no name collisions with existing types.
  </action>
  <acceptance_criteria>
    - src/domain/types.ts exports CapacityBreakdown and CapacityResult interfaces.
    - CapacityBreakdown includes all fields listed above with correct types.
    - CapacityResult includes all fields listed above with correct types.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "CapacityBreakdown" src/domain/types.ts &amp;&amp; grep -c "CapacityResult" src/domain/types.ts</automated>
  </verify>
  <done>Capacity types added to domain/types.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CapacityService with working-day logic</name>
  <files>src/services/capacity-service.ts</files>
  <read_first>
    src/data/squad-repository.ts
    src/data/absence-repository.ts
    src/domain/types.ts
    node_modules/date-fns/package.json
  </read_first>
  <action>
    Create src/services/capacity-service.ts. Export class CapacityService with constructor(db: Database). Private methods: isWorkingDay(date: Date): boolean — returns true if not Saturday (6) or Sunday (0). getWorkingDaysInRange(start: string, end: string): number — iterate from start to end inclusive using date-fns addDays or native Date, count days where isWorkingDay is true. getAbsenceHoursInRange(absence: Absence, start: string, end: string): number — compute overlap days between absence range and query range, multiply by the member's daily_capacity_hours. For squad-wide holidays, apply to all members. Public method: calculate(start_date: string, end_date: string): CapacityResult — query all members via SquadRepository, query absences overlapping range via AbsenceRepository, for each member compute raw_capacity = working_days × daily_capacity_hours, absence_hours = sum of overlapping absence days × daily_capacity_hours, holiday_hours = sum of overlapping holiday days × daily_capacity_hours, real_capacity = raw_capacity − absence_hours − holiday_hours. Return CapacityResult with totals.
  </action>
  <acceptance_criteria>
    - CapacityService is exported from src/services/capacity-service.ts.
    - getWorkingDaysInRange correctly excludes Saturdays and Sundays.
    - getWorkingDaysInRange includes both start and end dates if they are working days.
    - getAbsenceHoursInRange computes only the overlapping portion of an absence with the query range.
    - calculate returns a CapacityResult with accurate totals for raw, absence, holiday, and real hours.
    - Squad-wide holidays (member_id IS NULL) are applied to every member.
    - If a member has no absences in the range, their absence_hours and holiday_hours are 0.
    - The service uses date-fns functions where appropriate (e.g., parseISO, addDays, eachDayOfInterval if available, otherwise native Date iteration).
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; PID=$! &amp;&amp; sleep 2 &amp;&amp; curl -s "http://localhost:3000/api/capacity?start_date=2026-06-01\&end_date=2026-06-05" | grep -c "total_real_hours" &amp;&amp; kill $PID</automated>
  </verify>
  <done>CapacityService correctly calculates working days and deducts absences/holidays.</done>
</task>

<task type="auto">
  <name>Task 3: Add capacity API endpoint</name>
  <files>server.ts</files>
  <read_first>
    server.ts
    src/services/capacity-service.ts
    src/domain/types.ts
  </read_first>
  <action>
    In server.ts, import CapacityService. Instantiate it after repositories. Add route GET /api/capacity with query params start_date and end_date (both required, format YYYY-MM-DD). Validate with Zod: start_date and end_date are strings matching YYYY-MM-DD regex. Ensure end_date >= start_date. Call capacityService.calculate(start_date, end_date). Return JSON with the CapacityResult. If no squad members exist, return result with zeros and empty members array.
  </action>
  <acceptance_criteria>
    - GET /api/capacity?start_date=2026-06-01&end_date=2026-06-05 returns a JSON object with start_date, end_date, total_members, total_raw_hours, total_absence_hours, total_holiday_hours, total_real_hours, and members array.
    - Missing or invalid query params return 400 with error message.
    - end_date < start_date returns 400 with error message.
    - If no members exist, total_members is 0 and members is empty array.
    - Response Content-Type is application/json.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; PID=$! &amp;&amp; sleep 2 &amp;&amp; curl -s "http://localhost:3000/api/capacity?start_date=2026-06-01\&end_date=2026-06-05" | grep -c "members" &amp;&amp; curl -s "http://localhost:3000/api/capacity" | grep -c "error" &amp;&amp; kill $PID</automated>
  </verify>
  <done>Capacity API endpoint accepts date range and returns breakdown.</done>
</task>

<task type="auto">
  <name>Task 4: Create CapacityView React component</name>
  <files>src/App.tsx, src/components/CapacityView.tsx</files>
  <read_first>
    src/App.tsx
    src/components/BacklogList.tsx
    src/domain/types.ts
  </read_first>
  <action>
    Create src/components/CapacityView.tsx: functional component. State: startDate (string), endDate (string), result (CapacityResult | null), loading (boolean), error (string). On mount or when dates change, fetch `/api/capacity?start_date=${startDate}&end_date=${endDate}`. Default to current week (Monday to Friday of current week) using native Date or date-fns. Render: two date inputs for start and end, a "Calcular" button, and a results table. Table columns: Membro, Capacidade Bruta (h), Ausências (h), Feriados (h), Capacidade Real (h). After the per-member rows, show a totals row (bold) with sums. If no members, show "Cadastre membros na aba Squad para calcular capacity."
    In src/App.tsx: add "Capacity" tab. Render CapacityView inside it. Ensure tab label is in Portuguese: "Capacity" is acceptable as a Scrum term, or use "Capacidade".
  </action>
  <acceptance_criteria>
    - CapacityView has two HTML5 date inputs and a calculate button.
    - Default date range is the current Monday-to-Friday week.
    - Results table shows per-member breakdown with all capacity columns.
    - Totals row shows aggregated values.
    - App.tsx has a "Capacidade" tab that renders CapacityView.
    - When dates are changed and calculate is clicked, a new request is made.
    - If the API returns an error, it is displayed to the user.
    - Loading state shows a visual indicator.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>CapacityView renders correctly and builds without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Bun server | Date range params are user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Invalid date range causing infinite loop | mitigate | Zod validation ensures start_date <= end_date; service limits range to reasonable max (e.g., 1 year) |
| T-02-05 | Denial of Service | Very large date range | mitigate | Service clamps or rejects ranges > 365 days |
</threat_model>

<verification>
1. Ensure at least one squad member exists (e.g., from 02-01).
2. Create an absence for that member: vacation 2026-06-01 to 2026-06-05.
3. Open "Capacidade" tab — default week should show values.
4. Select date range 2026-06-01 to 2026-06-05 (Mon-Fri, 5 working days).
5. For a member with daily_capacity=6, raw should be 30h. Since the member is on vacation all 5 days, absence should be 30h, real should be 0h.
6. Create a squad-wide holiday 2026-06-15. Select 2026-06-15 to 2026-06-15 — all members should show 1 holiday day deducted.
7. Select a range with no absences — absence and holiday hours should be 0.
</verification>

<success_criteria>
- Capacity calculation correctly counts working days (Mon-Fri) in any date range.
- Absences overlapping the range are deducted proportionally.
- Squad-wide holidays are deducted for all members.
- UI shows a transparent per-member breakdown.
- Total row aggregates all members.
- API validates inputs and returns clear errors for invalid ranges.
</success_criteria>

<output>
Create `.planning/phases/02-squad-capacity-engine/02-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Decorators / Classes:**
- `CapacityService` (src/services/capacity-service.ts) — pure business logic for capacity calculation.

**Functions / Methods:**
- `CapacityService.calculate(start_date, end_date)` — returns CapacityResult with full breakdown.
- `CapacityService.getWorkingDaysInRange(start, end)` — counts Mon-Fri days in range.
- `CapacityService.getAbsenceHoursInRange(absence, start, end)` — computes overlap hours.

**CLI Flags / Scripts:**
- `bun run dev` — serves capacity API.

**Struct / Dataclass Fields:**
- `CapacityBreakdown`: member_id, member_name, role, daily_capacity_hours, raw_capacity_hours, absence_hours, holiday_hours, real_capacity_hours.
- `CapacityResult`: start_date, end_date, total_members, total_raw_hours, total_absence_hours, total_holiday_hours, total_real_hours, members[].

**New File Paths:**
- `src/services/capacity-service.ts`
- `src/components/CapacityView.tsx`
