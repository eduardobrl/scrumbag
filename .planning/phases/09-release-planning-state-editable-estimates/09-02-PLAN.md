---
phase: 09-release-planning-state-editable-estimates
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/features/releases/release-form.tsx
  - src/features/releases/release-edit-form.tsx
  - src/features/releases/release-list.tsx
  - src/features/releases/release-detail.tsx
  - src/components/release-switcher.tsx
  - src/features/stories/story-form.tsx
  - src/app/features/[id]/stories/new/page.tsx
  - src/app/stories/[id]/edit/page.tsx
  - src/messages/en.json
  - src/messages/pt-BR.json
  - tests/e2e/releases.spec.ts
  - tests/e2e/stories.spec.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
  - EST-01
  - EST-02
must_haves:
  truths:
    - "User can see PLANNING as a first-class release state on release screens"
    - "User can open a story form while the parent release is PLANNING and still edit story points and estimated days"
    - "Story estimate edits in PLANNING persist after save and reload without any audit blocking"
  artifacts:
    - path: "src/features/releases/release-form.tsx"
      provides: "Release creation form with the updated lifecycle options"
    - path: "src/features/releases/release-edit-form.tsx"
      provides: "Release edit form with ordered lifecycle options"
    - path: "src/features/releases/release-list.tsx"
      provides: "Release list badges and status display"
    - path: "src/features/releases/release-detail.tsx"
      provides: "Release detail badges and lifecycle presentation"
    - path: "src/components/release-switcher.tsx"
      provides: "Global release header status badge"
    - path: "src/features/stories/story-form.tsx"
      provides: "Story estimate editor with PLANNING context"
    - path: "src/app/features/[id]/stories/new/page.tsx"
      provides: "New story page that passes the parent release status"
    - path: "src/app/stories/[id]/edit/page.tsx"
      provides: "Story edit page that passes the parent release status"
    - path: "src/messages/en.json"
      provides: "English labels for the new lifecycle state"
    - path: "src/messages/pt-BR.json"
      provides: "Portuguese labels for the new lifecycle state"
    - path: "tests/e2e/releases.spec.ts"
      provides: "Browser coverage for lifecycle UI changes"
    - path: "tests/e2e/stories.spec.ts"
      provides: "Browser coverage for planning-time story estimate edits"
  key_links:
    - from: "src/features/releases/release-form.tsx"
      to: "src/messages/*.json"
      via: "status option labels"
      pattern: "PLANNING"
    - from: "src/app/stories/[id]/edit/page.tsx"
      to: "src/features/stories/story-form.tsx"
      via: "release status prop"
      pattern: "releaseStatus"
    - from: "src/features/stories/story-form.tsx"
      to: "storyPoints / estimatedDays inputs"
      via: "planning helper note instead of disabling controls"
      pattern: "estimatedDays|storyPoints"
---

<objective>
Make the release UI speak the new planning language and keep estimate editing explicitly available while a release is in PLANNING.
Purpose: give users a clear planning-state workflow before the audit phase arrives.
Output: updated release screens, planning-aware story forms, localized labels, and Playwright coverage.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/release-status.ts
@src/features/releases/release-form.tsx
@src/features/stories/story-form.tsx
@tests/e2e/releases.spec.ts
@tests/e2e/stories.spec.ts
</execution_context>

<context>
<interfaces>
From `src/lib/release-status.ts`:
- `RELEASE_STATUS_OPTIONS` should drive the lifecycle dropdowns and the planned/planning/in-progress/closed badge logic.
- `isAllowedReleaseTransition(existing, next)` should be the single source of truth for release lifecycle order.

From `src/features/stories/story-form.tsx`:
- The form already edits `storyPoints` and `estimatedDays`; this plan adds release-status context and copy, not extra blocking.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface PLANNING across every release-facing screen</name>
  <files>
    src/features/releases/release-form.tsx
    src/features/releases/release-edit-form.tsx
    src/features/releases/release-list.tsx
    src/features/releases/release-detail.tsx
    src/components/release-switcher.tsx
    src/messages/en.json
    src/messages/pt-BR.json
  </files>
  <action>
    Add PLANNING as a first-class lifecycle state everywhere the user sees or chooses release status. Reuse the shared release-status helper for options and tone mapping so the new state is not treated as a generic neutral label. Keep the current planned/in-progress/closed/cancelled labels intact, but make the new planning step obvious in both locales.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/releases.spec.ts</automated>
  </verify>
  <done>
    The release header, list, detail page, and edit/create forms all show PLANNING consistently in both languages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make story estimate editing planning-aware</name>
  <files>
    src/features/stories/story-form.tsx
    src/app/features/[id]/stories/new/page.tsx
    src/app/stories/[id]/edit/page.tsx
    tests/e2e/stories.spec.ts
  </files>
  <action>
    Pass the parent release status into the story form, show a small helper note when the release is PLANNING, and keep the story point and estimated-day fields editable. Do not add audit UI or blocking rules here; the point of this phase is to preserve free estimate editing while the release is in planning and leave history tracking for phase 10.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/stories.spec.ts</automated>
  </verify>
  <done>
    A PLANNING release can still save story estimate edits and the saved values survive reload.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rework the browser flows for the planning step</name>
  <files>
    tests/e2e/releases.spec.ts
    tests/e2e/stories.spec.ts
  </files>
  <action>
    Update the release and story Playwright flows so they explicitly move a release through PLANNING before editing story estimates. Keep the existing happy-path checks, but make the test assertions prove the new status appears in the UI and the estimate edits remain available after reload.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/releases.spec.ts tests/e2e/stories.spec.ts</automated>
  </verify>
  <done>
    Browser coverage demonstrates the planning-state UI and estimate editing flow together.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> local UI state | Users can select status options and edit estimates, so the UI must not imply unsupported transitions. |
| browser -> local API | Story and release forms send user input to local API routes that must still validate transitions server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | release/status dropdowns | mitigate | Drive options from the shared lifecycle helper and keep server-side validation authoritative. |
| T-09-02-02 | Tampering | story estimate form | mitigate | Leave the estimate fields editable in PLANNING, but keep the submission path identical so validation remains centralized. |
| T-09-02-03 | Information Disclosure | localized copy | accept | The new labels expose no sensitive data; they only reflect the release lifecycle state. |
</threat_model>

<verification>
Prove the UI with browser tests: the release screens should surface PLANNING, and the story editor should still accept and persist estimate changes while the parent release is in PLANNING.
</verification>

<success_criteria>
- Release forms, lists, detail views, and the global header all render PLANNING clearly.
- Story edit and create flows still allow story points and estimated-day edits when the release is in PLANNING.
- Playwright coverage demonstrates the new lifecycle step and the planning-time estimate edit flow together.
</success_criteria>

<output>
Create `.planning/phases/09-release-planning-state-editable-estimates/09-02-PLAN.md` when done
</output>
