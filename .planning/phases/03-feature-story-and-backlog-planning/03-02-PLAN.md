---
phase: 03-feature-story-and-backlog-planning
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/api/stories/route.ts
  - src/app/api/stories/[id]/route.ts
  - src/app/features/[id]/page.tsx
  - src/app/features/[id]/stories/new/page.tsx
  - src/app/stories/[id]/edit/page.tsx
  - src/features/features/feature-detail.tsx
  - src/features/stories/story-form.tsx
  - src/features/stories/story-list.tsx
  - src/lib/features.ts
  - src/lib/stories.ts
  - tests/stories.test.ts
  - tests/e2e/stories.spec.ts
autonomous: true
requirements:
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-05
  - FEAT-06
user_setup: []
must_haves:
  truths:
    - "Every story belongs to a feature."
    - "Story create/edit captures title, description, acceptance criteria, story points, estimated business days, status, and current sprint visibility."
    - "New stories default to general backlog when no sprint is assigned."
    - "Canceled stories are kept historically with status CANCELLED and are excluded from feature totals, progress, and planned effort."
    - "Story status values map to the product workflow: BACKLOG, SPRINT_BACKLOG, IN_PROGRESS, DONE, and CANCELLED."
    - "Direct story editing must not silently bypass backlog planning impact preview when assigning an unplanned story to a sprint."
  artifacts:
    - path: "src/lib/stories.ts"
      provides: "Story CRUD helpers and validation"
      exports:
        - createStory
        - updateStory
        - cancelStory
        - getStoryDetails
        - listStoriesForFeature
    - path: "src/app/api/stories/route.ts"
      provides: "Story create API"
      exports:
        - POST
    - path: "src/app/api/stories/[id]/route.ts"
      provides: "Story detail/update/cancel API"
      exports:
        - GET
        - PATCH
    - path: "src/features/stories/story-form.tsx"
      provides: "Story create/edit form"
      min_lines: 80
    - path: "src/features/stories/story-list.tsx"
      provides: "Feature detail story table"
      min_lines: 50
  key_links:
    - from: "src/features/stories/story-form.tsx"
      to: "/api/stories"
      via: "client fetch for story creation"
      pattern: "fetch('/api/stories'"
    - from: "src/app/api/stories/[id]/route.ts"
      to: "updateStory"
      via: "PATCH delegates story validation and persistence"
      pattern: "updateStory"
    - from: "src/lib/stories.ts"
      to: "calculateFeatureSummary"
      via: "story mutations rely on feature aggregate recalculation through shared queries"
      pattern: "calculateFeatureSummary"
---

<objective>
As a squad planner, I want to break features into estimated stories and see feature metrics update automatically, so that release scope has actionable units of work before sprint assignment.

Purpose: Fill the story-management half of Phase 3: story create/edit/list/detail/cancel behavior, story forms and tables inside feature detail, and tests that prove feature aggregates update from non-canceled stories.
Output: Story helper library, story APIs, story create/edit routes, feature detail story table, and unit/e2e tests for story validation, cancellation, aggregate recalculation, and current sprint display.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-release-and-sprint-planning-core/02-01-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-02-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-03-SUMMARY.md
@.planning/phases/03-feature-story-and-backlog-planning/03-01-SUMMARY.md
@spec.md
@telas.md
</context>

<artifacts_this_phase_produces>
- Functions: `validateStoryInput`, `createStory`, `updateStory`, `cancelStory`, `getStoryDetails`, `listStoriesForFeature`, `toStoryView`.
- API route exports: `POST` in `src/app/api/stories/route.ts`; `GET` and `PATCH` in `src/app/api/stories/[id]/route.ts`.
- App routes: `/features/[id]/stories/new`, `/stories/[id]/edit`.
- React components: `StoryForm`, `StoryList`.
- Test files: `tests/stories.test.ts`, `tests/e2e/stories.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add story validation and CRUD APIs</name>
  <files>src/lib/stories.ts, src/app/api/stories/route.ts, src/app/api/stories/[id]/route.ts, tests/stories.test.ts</files>
  <read_first>src/lib/features.ts, src/lib/releases.ts, src/lib/sprints.ts, src/lib/validation.ts, prisma/schema.prisma, tests/features.test.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create `src/lib/stories.ts` with `validateStoryInput`, `createStory`, `updateStory`, `cancelStory`, `getStoryDetails`, `listStoriesForFeature`, and `toStoryView`. Validate required `featureId` and `title`; optional `description` and `acceptanceCriteria`; numeric `storyPoints` and `estimatedDays` greater than or equal to zero when provided; valid `StoryStatus`; and valid existing feature. `createStory` must default `status` to `BACKLOG` and `currentSprintId` to null unless the caller provides an already-consistent sprint assignment from the backlog planning API introduced in Plan 03-03. `updateStory` must allow editing text, estimates, and status, but must reject assigning a previously unplanned story to a sprint directly with an error that points to backlog planning. `cancelStory` must set status `CANCELLED` and leave the row in SQLite. Implement `POST /api/stories` and `GET`/`PATCH /api/stories/[id]` with 404 for missing stories and 400 for validation errors.</action>
  <verify>npm run test -- stories; npm run test -- features; npm run build</verify>
  <acceptance_criteria>
    - `src/lib/stories.ts` exports `createStory`, `updateStory`, `cancelStory`, `getStoryDetails`, and `listStoriesForFeature`.
    - Creating a story without `title` returns a validation error containing `Required`.
    - Story points and estimated days accept `0` and reject negative values.
    - Canceling a story sets status `CANCELLED` without deleting it.
    - Updating `currentSprintId` from null through `updateStory` returns an error containing `Use backlog planning`.
    - `src/app/api/stories/route.ts` exports `POST`.
    - `src/app/api/stories/[id]/route.ts` exports `GET` and `PATCH`.
  </acceptance_criteria>
  <done>Story persistence and validation are available without bypassing the backlog planning workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Build story create/edit forms and feature story table</name>
  <files>src/app/features/[id]/page.tsx, src/app/features/[id]/stories/new/page.tsx, src/app/stories/[id]/edit/page.tsx, src/features/features/feature-detail.tsx, src/features/stories/story-form.tsx, src/features/stories/story-list.tsx, tests/e2e/stories.spec.ts</files>
  <read_first>src/app/features/[id]/page.tsx, src/features/features/feature-detail.tsx, src/features/features/feature-form.tsx, src/features/releases/release-edit-form.tsx, src/features/sprints/sprint-list.tsx, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, telas.md</read_first>
  <action>Add `StoryForm` for create/edit with feature selector or fixed feature context, title, description, acceptance criteria, story points, estimated business days, and status. Show current sprint as read-only text when the story has a sprint, and as `Backlog` when `currentSprintId` is null; actual sprint assignment is handled by Plan 03-03. Add `/features/[id]/stories/new` to create a story under a feature and return to the feature detail page. Add `/stories/[id]/edit` for editing story fields. Replace the Plan 03-01 empty story placeholder on feature detail with `StoryList`, showing title, status, story points, estimated days, current sprint name or `Backlog`, and edit/cancel actions. Keep the table compact and horizontally resilient for desktop-first operation.</action>
  <verify>npm run test:e2e -- stories; npm run build</verify>
  <acceptance_criteria>
    - `/features/[id]` shows a `New Story` control.
    - Creating a story titled `Create onboarding screen` with `5` story points and `2` estimated days shows it in the feature story table after reload.
    - The story table shows columns or row labels for `Status`, `SP`, `Days`, and `Sprint`.
    - `/stories/[id]/edit` renders inputs for title, description, acceptance criteria, story points, estimated days, and status.
    - A story with no sprint shows `Backlog` as its current sprint context.
  </acceptance_criteria>
  <done>The user can create and maintain stories from a feature detail workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Prove story changes recalculate feature totals and progress</name>
  <files>src/lib/features.ts, src/lib/stories.ts, src/features/features/feature-detail.tsx, tests/features.test.ts, tests/stories.test.ts, tests/e2e/stories.spec.ts</files>
  <read_first>src/lib/features.ts, src/lib/stories.ts, src/features/features/feature-detail.tsx, tests/features.test.ts, tests/stories.test.ts, spec.md, telas.md</read_first>
  <action>Ensure every feature list/detail query reflects current story aggregates after story create, edit, done-status changes, and cancellation. Add unit coverage where a feature starts with zero totals, gains multiple stories, changes one story to `DONE`, cancels one story, and recalculates story point total, estimated day total, calculated status, and progress. Add e2e assertions that after creating two stories for a feature, the feature detail metrics show the summed story points and days; after editing one story to `DONE`, progress changes from `0%` to a non-zero value; after canceling a story, totals exclude it. Preserve cancellation instead of destructive delete.</action>
  <verify>npm run test -- features; npm run test -- stories; npm run test:e2e -- stories; npm run build</verify>
  <acceptance_criteria>
    - Unit tests prove story create/edit/cancel changes feature story point totals.
    - Unit tests prove story create/edit/cancel changes feature estimated day totals.
    - Unit tests prove feature status becomes `In progress` when at least one non-canceled story is `DONE` and at least one remains unfinished.
    - E2E coverage sees feature totals update after story creation.
    - E2E coverage sees canceled stories excluded from feature totals.
  </acceptance_criteria>
  <done>Feature aggregates are visibly driven by story data instead of static placeholders.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- stories` succeeds.
- [ ] `npm run test -- features` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- stories` succeeds.
- [ ] Manual browser check can create, edit, cancel, and reload stories from a feature detail page while feature metrics update.
</verification>

<success_criteria>
- FEAT-02 is complete for create, edit, list, inspect, and cancel story flows.
- FEAT-03 is complete for story title, description, acceptance criteria, story points, estimated business days, status, and current sprint visibility.
- FEAT-04 remains complete with story-driven aggregate totals.
- FEAT-05 remains complete with story-driven calculated feature status.
- FEAT-06 remains complete with story-driven feature progress and fallback behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-story-and-backlog-planning/03-02-SUMMARY.md`.
</output>
