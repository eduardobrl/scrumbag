---
phase: 03-feature-story-and-backlog-planning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - scripts/db-sync.ts
  - src/app/api/features/route.ts
  - src/app/api/features/[id]/route.ts
  - src/app/features/page.tsx
  - src/app/features/[id]/page.tsx
  - src/app/features/[id]/edit/page.tsx
  - src/features/features/feature-detail.tsx
  - src/features/features/feature-form.tsx
  - src/features/features/feature-list.tsx
  - src/lib/features.ts
  - tests/features.test.ts
  - tests/e2e/features.spec.ts
autonomous: true
requirements:
  - FEAT-01
  - FEAT-04
  - FEAT-05
  - FEAT-06
user_setup: []
must_haves:
  truths:
    - "Feature rows belong to a Release and are canceled through status instead of destructive deletion."
    - "Story data structures are introduced in this plan so feature aggregates can be calculated from persisted stories in later Phase 3 plans."
    - "Canceled stories are excluded from feature story point totals, estimated day totals, calculated status, and progress."
    - "Feature status is calculated as NOT_STARTED, IN_PROGRESS, or FINISHED from non-canceled stories."
    - "Feature progress uses finished story points divided by total story points, falling back to finished story count divided by total story count when story points are absent or zero."
    - "Feature list and detail screens remain desktop-first operational UI using tables, badges, compact cards, and existing app shell patterns."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Feature and Story models plus status enums"
      contains: "model Feature"
    - path: "scripts/db-sync.ts"
      provides: "Local SQLite tables for Feature and Story"
      contains: "CREATE TABLE IF NOT EXISTS \"Feature\""
    - path: "src/lib/features.ts"
      provides: "Feature CRUD and aggregate helpers"
      exports:
        - createFeature
        - updateFeature
        - cancelFeature
        - listFeatures
        - getFeatureDetails
        - calculateFeatureSummary
    - path: "src/app/api/features/route.ts"
      provides: "Feature list/create API"
      exports:
        - GET
        - POST
    - path: "src/app/api/features/[id]/route.ts"
      provides: "Feature detail/update/cancel API"
      exports:
        - GET
        - PATCH
  key_links:
    - from: "src/features/features/feature-form.tsx"
      to: "/api/features"
      via: "client fetch for feature create/update"
      pattern: "fetch('/api/features"
    - from: "src/app/api/features/[id]/route.ts"
      to: "updateFeature"
      via: "PATCH delegates feature validation and persistence"
      pattern: "updateFeature"
    - from: "src/lib/features.ts"
      to: "calculateFeatureSummary"
      via: "list/detail views use shared aggregate logic"
      pattern: "calculateFeatureSummary"
---

<objective>
As a squad planner, I want to create and inspect release features with calculated scope metrics, so that the release has a real feature structure before stories and backlog planning are layered in.

Purpose: Establish the Phase 3 data foundation and first feature management slice: Feature and Story schema, local SQLite sync, feature CRUD APIs, feature list/detail/edit UI, and aggregate helpers that future story and backlog plans reuse.
Output: Feature/Story Prisma schema, local SQLite sync, feature helper library, feature APIs, feature list/detail/edit routes, and tests for feature validation, cancellation, and aggregate calculations.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-foundation-and-squad-setup/01-SKELETON.md
@.planning/phases/01-local-foundation-and-squad-setup/01-03-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-01-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-02-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-03-SUMMARY.md
@spec.md
@telas.md
</context>

<artifacts_this_phase_produces>
- Prisma enums: `FeatureLifecycleStatus`, `StoryStatus`.
- Prisma models: `Feature`, `Story`.
- Functions: `validateFeatureInput`, `createFeature`, `updateFeature`, `cancelFeature`, `listFeatures`, `getFeatureDetails`, `calculateFeatureSummary`, `toFeatureView`.
- API route exports: `GET` and `POST` in `src/app/api/features/route.ts`; `GET` and `PATCH` in `src/app/api/features/[id]/route.ts`.
- App routes: `/features`, `/features/[id]`, `/features/[id]/edit`.
- React components: `FeatureList`, `FeatureDetail`, `FeatureForm`.
- Test files: `tests/features.test.ts`, `tests/e2e/features.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add Feature and Story schema with local SQLite sync</name>
  <files>prisma/schema.prisma, scripts/db-sync.ts, tests/features.test.ts</files>
  <read_first>prisma/schema.prisma, scripts/db-sync.ts, src/lib/releases.ts, src/lib/sprints.ts, spec.md, telas.md</read_first>
  <action>Add `FeatureLifecycleStatus` enum values `ACTIVE` and `CANCELLED` and `StoryStatus` enum values `BACKLOG`, `SPRINT_BACKLOG`, `IN_PROGRESS`, `DONE`, and `CANCELLED` to `prisma/schema.prisma`. Add `Feature` with `id`, `releaseId`, `release`, `name`, optional `description`, `lifecycleStatus`, `stories`, `createdAt`, and `updatedAt`. Add `Story` with `id`, `featureId`, `feature`, optional `currentSprintId`, optional `currentSprint`, `title`, optional `description`, optional `acceptanceCriteria`, nullable `storyPoints`, nullable `estimatedDays`, `status`, `createdAt`, and `updatedAt`. Add inverse relations on `Release` and `Sprint`. Update `scripts/db-sync.ts` to create matching `Feature` and `Story` tables, indexes on `Feature.releaseId`, `Story.featureId`, and `Story.currentSprintId`, and foreign keys with cascade from Release to Feature and Feature to Story. Keep `currentSprintId` nullable so unplanned stories live in the general backlog.</action>
  <verify>npm run db:generate; npm run db:migrate; npm run test -- features</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model Feature`, `model Story`, `enum StoryStatus`, and `enum FeatureLifecycleStatus`.
    - `Story.currentSprintId` is nullable and relates to `Sprint`.
    - `scripts/db-sync.ts` contains `CREATE TABLE IF NOT EXISTS "Feature"` and `CREATE TABLE IF NOT EXISTS "Story"`.
    - The local SQLite database has tables named `Feature` and `Story` after `npm run db:migrate`.
    - No remote database configuration or multi-user auth is added.
  </acceptance_criteria>
  <done>The local database can persist features and stories with the release/sprint relationships Phase 3 needs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement feature CRUD and aggregate helpers</name>
  <files>src/lib/features.ts, src/app/api/features/route.ts, src/app/api/features/[id]/route.ts, tests/features.test.ts</files>
  <read_first>src/lib/releases.ts, src/lib/validation.ts, src/app/api/releases/route.ts, src/app/api/releases/[id]/route.ts, tests/releases.test.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create `src/lib/features.ts` with validation for required `releaseId` and `name`, optional `description`, and release existence. Implement `createFeature`, `updateFeature`, `cancelFeature`, `listFeatures`, `getFeatureDetails`, `calculateFeatureSummary`, and `toFeatureView`. `cancelFeature` must set lifecycle status to `CANCELLED` instead of deleting the row. `calculateFeatureSummary` must ignore canceled stories, sum non-canceled story points and estimated days, calculate status as `NOT_STARTED` when no non-canceled stories are in progress/done, `IN_PROGRESS` when at least one story is in progress/done but not all are done, and `FINISHED` when all non-canceled stories are done. If there are no non-canceled stories, status is `NOT_STARTED` and progress is `0`. Progress uses finished story points over total story points when total story points is greater than zero, otherwise finished story count over total story count. Implement `GET`/`POST` in `/api/features` and `GET`/`PATCH` in `/api/features/[id]`, with `PATCH` accepting update fields and a cancel action.</action>
  <verify>npm run test -- features; npm run build</verify>
  <acceptance_criteria>
    - `src/lib/features.ts` exports `createFeature`, `updateFeature`, `cancelFeature`, `listFeatures`, `getFeatureDetails`, and `calculateFeatureSummary`.
    - Creating a feature without `name` returns a validation error containing `Required`.
    - Canceling a feature changes lifecycle status to `CANCELLED` without deleting it.
    - Unit tests prove canceled stories are excluded from feature totals and progress.
    - Unit tests prove progress falls back to story count when story point totals are zero or absent.
    - `src/app/api/features/route.ts` exports `GET` and `POST`.
    - `src/app/api/features/[id]/route.ts` exports `GET` and `PATCH`.
  </acceptance_criteria>
  <done>Feature behavior is centralized in a reusable library and exposed through local API routes.</done>
</task>

<task type="auto">
  <name>Task 3: Build feature list, detail, edit, and cancellation UI</name>
  <files>src/app/features/page.tsx, src/app/features/[id]/page.tsx, src/app/features/[id]/edit/page.tsx, src/features/features/feature-list.tsx, src/features/features/feature-detail.tsx, src/features/features/feature-form.tsx, tests/e2e/features.spec.ts</files>
  <read_first>src/app/features/page.tsx, src/features/releases/release-list.tsx, src/features/releases/release-detail.tsx, src/features/releases/release-edit-form.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, telas.md</read_first>
  <action>Replace the Features placeholder with a dense `/features` screen that selects the active `IN_PROGRESS` release when available, otherwise the most recently created release, and includes a release selector when multiple releases exist. Show feature rows for name, calculated status, lifecycle status, story points total, estimated days total, progress, timeline placeholder, and actions to open/edit/cancel. Add feature creation from `/features` using `FeatureForm`. Add `/features/[id]` detail with description, calculated status, story point total, estimated days total, progress, lifecycle status, and an empty stories table placeholder that Plan 03-02 will fill. Add `/features/[id]/edit` for editing name/description/release. Provide a cancel action that uses the API, requires explicit user click, and updates the visible lifecycle badge to `Cancelled` without deleting the feature.</action>
  <verify>npm run test:e2e -- features; npm run build</verify>
  <acceptance_criteria>
    - `/features` no longer renders the Phase 3 placeholder component.
    - `/features` shows a `New Feature` control and a table with `Feature`, `Status`, `SP`, `Days`, `Progress`, and `Period` columns or row labels.
    - Creating `Onboarding Digital` from `/features` persists it and shows it after reload.
    - `/features/[id]` shows calculated metrics and an empty-state prompt for stories.
    - Editing the feature description persists after reload.
    - Canceling a feature shows a `Cancelled` badge and does not remove the row from the database.
  </acceptance_criteria>
  <done>The user can create, edit, inspect, and cancel features while seeing calculated feature-level metrics.</done>
</task>

</tasks>

<verification>
- [ ] `npm run db:generate` succeeds.
- [ ] `npm run db:migrate` succeeds and creates local Feature/Story tables.
- [ ] `npm run test -- features` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- features` succeeds.
- [ ] Manual browser check can create, open, edit, reload, and cancel a feature without destructive deletion.
</verification>

<success_criteria>
- FEAT-01 is complete for create, edit, list, inspect, and cancel feature flows.
- FEAT-04 is complete through reusable aggregate helpers that sum non-canceled stories.
- FEAT-05 is complete through calculated feature status rules.
- FEAT-06 is complete through feature progress calculation with story-count fallback.
- Feature and Story persistence remains local-first in SQLite at `./data/squad-planner.db`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-story-and-backlog-planning/03-01-SUMMARY.md`.
</output>
