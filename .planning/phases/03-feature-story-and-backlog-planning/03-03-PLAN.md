---
phase: 03-feature-story-and-backlog-planning
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/api/backlog/route.ts
  - src/app/api/stories/[id]/plan/route.ts
  - src/app/api/stories/[id]/backlog/route.ts
  - src/app/backlog/page.tsx
  - src/features/backlog/backlog-filters.tsx
  - src/features/backlog/backlog-list.tsx
  - src/features/backlog/plan-story-dialog.tsx
  - src/features/releases/release-detail.tsx
  - src/features/sprints/sprint-detail.tsx
  - src/features/sprints/sprint-list.tsx
  - src/lib/backlog.ts
  - src/lib/sprint-planning-summary.ts
  - src/lib/stories.ts
  - tests/backlog.test.ts
  - tests/e2e/backlog.spec.ts
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - BACK-03
  - BACK-04
  - FEAT-03
user_setup: []
must_haves:
  truths:
    - "General backlog means stories with `currentSprintId` null and status BACKLOG, excluding canceled stories unless canceled filtering is explicitly enabled."
    - "Backlog filters include release, feature, status, free text, unplanned stories, and canceled stories."
    - "Planning a backlog story into a sprint shows a confirmation preview before mutation."
    - "The preview shows current planned effort, selected story estimated days, and after-add planned effort; real capacity, remaining, and occupancy stay `Pending capacity` until Phase 4."
    - "Planning a story into a sprint sets `currentSprintId` and status `SPRINT_BACKLOG`."
    - "Removing a story from a sprint clears `currentSprintId` and sets status `BACKLOG`."
    - "Sprint and release planning summaries start using non-canceled story estimated days for planned effort while capacity remains Phase 4 scope."
  artifacts:
    - path: "src/lib/backlog.ts"
      provides: "Backlog query and story planning helpers"
      exports:
        - listBacklogStories
        - previewStorySprintPlan
        - planStoryIntoSprint
        - moveStoryToBacklog
    - path: "src/app/api/backlog/route.ts"
      provides: "Backlog list API"
      exports:
        - GET
    - path: "src/app/api/stories/[id]/plan/route.ts"
      provides: "Story-to-sprint planning API"
      exports:
        - POST
    - path: "src/app/api/stories/[id]/backlog/route.ts"
      provides: "Move story back to general backlog API"
      exports:
        - POST
    - path: "src/features/backlog/plan-story-dialog.tsx"
      provides: "Planning confirmation with effort impact preview"
      min_lines: 80
  key_links:
    - from: "src/features/backlog/plan-story-dialog.tsx"
      to: "/api/stories/"
      via: "client POST for sprint assignment after preview"
      pattern: "/plan"
    - from: "src/app/api/stories/[id]/plan/route.ts"
      to: "planStoryIntoSprint"
      via: "API delegates assignment validation and mutation"
      pattern: "planStoryIntoSprint"
    - from: "src/lib/sprint-planning-summary.ts"
      to: "Story.estimatedDays"
      via: "planned effort sums non-canceled stories assigned to sprint"
      pattern: "estimatedDays"
---

<objective>
As a squad planner, I want to filter backlog stories and move them into or out of sprints after seeing planned-effort impact, so that release scope can be organized into sprint backlog without losing control of capacity risk.

Purpose: Complete Phase 3 with the backlog planning workflow: backlog filters, planning preview, story-to-sprint assignment, return-to-backlog behavior, and planned effort integration across backlog, sprint, and release screens.
Output: Backlog helper library and APIs, backlog screen, plan-story dialog, unplanning action, planned effort summary updates, and tests for filters, assignment, unassignment, and persistence.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-release-and-sprint-planning-core/02-01-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-02-SUMMARY.md
@.planning/phases/02-release-and-sprint-planning-core/02-03-SUMMARY.md
@.planning/phases/03-feature-story-and-backlog-planning/03-01-SUMMARY.md
@.planning/phases/03-feature-story-and-backlog-planning/03-02-SUMMARY.md
@spec.md
@telas.md
</context>

<artifacts_this_phase_produces>
- Functions: `listBacklogStories`, `previewStorySprintPlan`, `planStoryIntoSprint`, `moveStoryToBacklog`, updated `getSprintPlanningSummary`.
- API route exports: `GET` in `src/app/api/backlog/route.ts`; `POST` in `src/app/api/stories/[id]/plan/route.ts`; `POST` in `src/app/api/stories/[id]/backlog/route.ts`.
- App route: `/backlog`.
- React components: `BacklogFilters`, `BacklogList`, `PlanStoryDialog`.
- Test files: `tests/backlog.test.ts`, `tests/e2e/backlog.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add backlog queries, filters, and planned-effort summary integration</name>
  <files>src/lib/backlog.ts, src/lib/sprint-planning-summary.ts, src/lib/stories.ts, src/app/api/backlog/route.ts, tests/backlog.test.ts</files>
  <read_first>src/lib/stories.ts, src/lib/features.ts, src/lib/sprints.ts, src/lib/sprint-planning-summary.ts, src/lib/releases.ts, prisma/schema.prisma, tests/sprints.test.ts, tests/stories.test.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create `src/lib/backlog.ts` with `listBacklogStories(filters)`, `previewStorySprintPlan(storyId, sprintId)`, `planStoryIntoSprint(storyId, sprintId)`, and `moveStoryToBacklog(storyId)`. Implement filters for release, feature, status, free text across title/description/acceptance criteria, unplanned stories, and canceled stories. Default backlog listing must show unplanned non-canceled stories for the selected release. Update `getSprintPlanningSummary(sprintId)` in `src/lib/sprint-planning-summary.ts` so `plannedEffortDays` sums non-canceled stories assigned to the sprint by `estimatedDays`; keep `capacityDays`, `remainingCapacityDays`, and `occupancyPercentage` null and `riskLabel` as `Pending capacity` until Phase 4. Add `GET /api/backlog` for filtered backlog results.</action>
  <verify>npm run test -- backlog; npm run test -- sprints; npm run build</verify>
  <acceptance_criteria>
    - `src/lib/backlog.ts` exports `listBacklogStories`, `previewStorySprintPlan`, `planStoryIntoSprint`, and `moveStoryToBacklog`.
    - `listBacklogStories` defaults to stories where `currentSprintId` is null and status is not `CANCELLED`.
    - Unit tests cover release, feature, status, text, unplanned, and canceled filters.
    - `getSprintPlanningSummary` returns assigned non-canceled story estimated days as `plannedEffortDays`.
    - Capacity fields remain null and risk remains `Pending capacity`.
    - `src/app/api/backlog/route.ts` exports `GET`.
  </acceptance_criteria>
  <done>Backlog data and sprint planned effort are queryable before the UI planning workflow uses them.</done>
</task>

<task type="auto">
  <name>Task 2: Build backlog screen with filters and planning preview</name>
  <files>src/app/backlog/page.tsx, src/features/backlog/backlog-filters.tsx, src/features/backlog/backlog-list.tsx, src/features/backlog/plan-story-dialog.tsx, tests/e2e/backlog.spec.ts</files>
  <read_first>src/app/backlog/page.tsx, src/features/sprints/release-selector.tsx, src/features/sprints/sprint-list.tsx, src/features/stories/story-list.tsx, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, telas.md</read_first>
  <action>Replace the Backlog placeholder with `/backlog`, selecting the active `IN_PROGRESS` release when available and supporting release selection. Add filter controls for release, feature, status, free text, unplanned only, and include canceled. Render a compact backlog table with story title, feature, status, story points, estimated days, current sprint context, and actions. Add `PlanStoryDialog` opened from each eligible backlog row. The dialog must let the user choose a sprint from the selected release and show story title, feature, story points, estimated days, current planned effort, after-add planned effort, and `Capacity: Pending capacity` before the user confirms. If a story lacks estimated days, show a visible warning that planned effort impact may be incomplete while still allowing planning. Do not add drag-and-drop board behavior in this phase.</action>
  <verify>npm run test:e2e -- backlog; npm run build</verify>
  <acceptance_criteria>
    - `/backlog` no longer renders the Phase 3 placeholder component.
    - `/backlog` shows filters for release, feature, status, search text, unplanned stories, and canceled stories.
    - An unplanned story created in Plan 03-02 appears in `/backlog`.
    - Opening planning for a story shows current planned effort and after-add planned effort before confirmation.
    - The planning dialog shows `Capacity: Pending capacity`.
    - Stories with status `CANCELLED` are hidden by default and visible when include canceled is enabled.
  </acceptance_criteria>
  <done>The user can find backlog stories and preview sprint planning impact from the Backlog screen.</done>
</task>

<task type="auto">
  <name>Task 3: Implement sprint assignment, return-to-backlog, and visible summary updates</name>
  <files>src/app/api/stories/[id]/plan/route.ts, src/app/api/stories/[id]/backlog/route.ts, src/features/backlog/backlog-list.tsx, src/features/stories/story-list.tsx, src/features/sprints/sprint-list.tsx, src/features/sprints/sprint-detail.tsx, src/features/releases/release-detail.tsx, src/lib/backlog.ts, src/lib/sprint-planning-summary.ts, tests/backlog.test.ts, tests/e2e/backlog.spec.ts</files>
  <read_first>src/lib/backlog.ts, src/lib/stories.ts, src/lib/sprint-planning-summary.ts, src/features/backlog/plan-story-dialog.tsx, src/features/stories/story-list.tsx, src/features/sprints/sprint-list.tsx, src/features/sprints/sprint-detail.tsx, src/features/releases/release-detail.tsx, tests/e2e/sprints.spec.ts, tests/e2e/stories.spec.ts</read_first>
  <action>Implement `POST /api/stories/[id]/plan` to validate the story exists, is not canceled, is currently unplanned or assigned to a different open sprint, and the sprint belongs to the story feature's release. On confirmation, set `currentSprintId` to the selected sprint and status to `SPRINT_BACKLOG`. Implement `POST /api/stories/[id]/backlog` to clear `currentSprintId` and set status `BACKLOG`. Add a return-to-backlog action wherever assigned stories are listed in feature detail and sprint detail contexts. Update sprint list/detail and release detail planned-effort cells to use `getSprintPlanningSummary` so planned effort changes from `0d` to the sum of assigned story estimated days; keep capacity/remaining/occupancy placeholders until Phase 4. Ensure planned stories disappear from the default general backlog and reappear after moving back to backlog.</action>
  <verify>npm run test -- backlog; npm run test -- sprints; npm run test:e2e -- backlog; npm run build</verify>
  <acceptance_criteria>
    - `src/app/api/stories/[id]/plan/route.ts` exports `POST`.
    - `src/app/api/stories/[id]/backlog/route.ts` exports `POST`.
    - Planning a story into a sprint sets status `SPRINT_BACKLOG` and assigns `currentSprintId`.
    - Planned stories disappear from the default `/backlog` list after confirmation.
    - Removing a story from a sprint clears `currentSprintId`, sets status `BACKLOG`, and makes it reappear in `/backlog`.
    - Sprint list/detail planned effort reflects assigned story estimated days.
    - Release detail generated sprint planned effort reflects assigned story estimated days.
  </acceptance_criteria>
  <done>The user can move stories between general backlog and sprint backlog while planned effort updates visibly.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- backlog` succeeds.
- [ ] `npm run test -- sprints` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- backlog` succeeds.
- [ ] Manual browser check can filter backlog, preview planning impact, assign a story to a sprint, remove it back to backlog, and see planned effort update.
</verification>

<success_criteria>
- BACK-01 is complete with a general backlog of unplanned stories.
- BACK-02 is complete with release, feature, status, text, unplanned, and canceled filters.
- BACK-03 is complete with a before-confirmation planning preview and sprint assignment workflow.
- BACK-04 is complete with move-to-backlog behavior that clears sprint assignment and restores `BACKLOG` status.
- FEAT-03 remains complete with current sprint visible and updated through the backlog planning workflow.
- Phase 4 capacity math, board drag-and-drop, sprint closure, reopening, and leakage history remain deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-story-and-backlog-planning/03-03-SUMMARY.md`.
</output>
