---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/data/backlog-repository.ts
  - server.ts
  - src/components/BacklogList.tsx
  - src/components/BacklogForm.tsx
  - src/App.tsx
autonomous: true
requirements:
  - BACK-02

must_haves:
  truths:
    - "User can create an epic, then create a feature under that epic, then a story under that feature"
    - "The backlog table shows items indented or grouped by their parent-child relationships"
    - "User can assign or change a parent when creating or editing an item"
    - "Deleting a parent sets children's parent_id to NULL via the SQLite ON DELETE SET NULL constraint defined in Plan 01-01, so children become root items"
  artifacts:
    - path: "src/data/backlog-repository.ts"
      provides: "Hierarchy query methods"
      exports: ["BacklogRepository"]
    - path: "src/components/BacklogList.tsx"
      provides: "Hierarchical backlog display"
      exports: ["BacklogList"]
    - path: "src/components/BacklogForm.tsx"
      provides: "Parent selection dropdown"
      exports: ["BacklogForm"]
  key_links:
    - from: "src/components/BacklogForm.tsx"
      to: "/api/backlog"
      via: "fetch GET for parent candidates"
      pattern: "fetch.*api/backlog.*parent"
    - from: "src/data/backlog-repository.ts"
      to: "bun:sqlite"
      via: "recursive CTE or multiple queries for hierarchy"
      pattern: "WITH RECURSIVE|SELECT.*parent_id"
---

<objective>
Enable backlog item hierarchy (epics → features → stories) as a vertical slice: extend the repository to support parent-child relationships, update the API to expose hierarchy-aware queries, and modify the UI to display nested items and allow parent assignment during creation/editing.

Purpose: BACK-02 requires users to organize work items in hierarchy. This plan adds parent-child semantics to the existing backlog without changing the core schema (parent_id was created in Plan 01-01).
Output: Users can see backlog items nested by parent and assign parents when creating or editing items.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend repository and API for hierarchy queries</name>
  <files>src/data/backlog-repository.ts, server.ts</files>
  <read_first>
    src/data/schema.ts
    src/data/backlog-repository.ts
    server.ts
    src/domain/types.ts
  </read_first>
  <action>
    In src/data/backlog-repository.ts, add to BacklogRepository:
    - findRootItems(): BacklogItem[] — query SELECT * FROM backlog_items WHERE parent_id IS NULL ORDER BY priority DESC, created_at DESC.
    - findChildren(parentId: string): BacklogItem[] — query SELECT * FROM backlog_items WHERE parent_id = ? ORDER BY priority DESC, created_at DESC.
    - findDescendants(rootId: string): BacklogItem[] — use a SQLite recursive CTE: WITH RECURSIVE descendants AS (SELECT * FROM backlog_items WHERE id = ? UNION ALL SELECT b.* FROM backlog_items b JOIN descendants d ON b.parent_id = d.id) SELECT * FROM descendants WHERE id != ?.
    - findById already exists from Plan 01-02; ensure it returns parent_id.
    In server.ts, add query parameter support to GET /api/backlog: if req.url has query param "parent_id", return repo.findChildren(parentId); if "root=true", return repo.findRootItems(); otherwise return repo.findAll() as before. No new route needed.
  </action>
  <acceptance_criteria>
    - findRootItems returns only items where parent_id IS NULL.
    - findChildren returns items whose parent_id matches the argument.
    - findDescendants returns the item and all its transitive children.
    - GET /api/backlog?root=true returns root items.
    - GET /api/backlog?parent_id=XXX returns children of that parent.
    - All existing endpoints (GET all, POST, PUT, DELETE) continue to work unchanged.
    - Parameterized queries are used for all new database access.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; sleep 2 &amp;&amp; EPIC=$(curl -s -X POST -H "Content-Type: application/json" -d '{"type":"epic","title":"Epic 1"}' http://localhost:3000/api/backlog | grep -o '"id":"[^"]*"' | cut -d'"' -f4) &amp;&amp; curl -s -X POST -H "Content-Type: application/json" -d "{\"type\":\"feature\",\"title\":\"Feature 1\",\"parent_id\":\"$EPIC\"}" http://localhost:3000/api/backlog &amp;&amp; curl -s "http://localhost:3000/api/backlog?parent_id=$EPIC" | grep -c "Feature 1" &amp;&amp; curl -s "http://localhost:3000/api/backlog?root=true" | grep -c "Epic 1"</automated>
  </verify>
  <done>Repository and API support querying items by parent, root, and full descendant tree.</done>
</task>

<task type="auto">
  <name>Task 2: Update UI to display hierarchy and assign parents</name>
  <files>src/components/BacklogList.tsx, src/components/BacklogForm.tsx, src/App.tsx</files>
  <read_first>
    src/components/BacklogList.tsx
    src/components/BacklogForm.tsx
    src/App.tsx
    src/domain/types.ts
    server.ts
  </read_first>
  <action>
    In src/components/BacklogList.tsx: Refactor to display items hierarchically. Fetch root items first, then for each root item that has children (fetch GET /api/backlog?parent_id=rootId), render children indented below the parent. Use a recursive or nested component approach (e.g., BacklogTreeItem component that renders itself and its children). Show depth indicator (e.g., margin-left or chevron icons). Keep Edit and Delete buttons on every row. Sorting: root items by priority DESC, children likewise.
    In src/components/BacklogForm.tsx: Add a "Parent" dropdown (&lt;select&gt;) that fetches GET /api/backlog?root=true and lists all root items (epics and features) as options, plus an empty option for "No parent". When editing an item, exclude the current item and its descendants from the parent options to prevent circular references. Pass selected parent_id in the submit payload.
    In src/App.tsx: Ensure that after any mutation (create, update, delete), the hierarchy view refreshes correctly. If an item's parent is changed, it should move in the tree view immediately.
  </action>
  <acceptance_criteria>
    - BacklogList shows root items first; children are indented below their parent.
    - Epic "Epic 1" is visible at top level; "Feature 1" (child of Epic 1) is indented below it.
    - BacklogForm has a Parent dropdown populated with root items.
    - Creating a feature with "Epic 1" as parent causes it to appear indented under Epic 1 in the list.
    - Editing a story to change its parent moves it to the new parent's section in the list.
    - The dropdown excludes the current item and its descendants when editing to prevent cycles.
    - Delete button on a parent does not hide children from the UI; children become root items because the database schema defines ON DELETE SET NULL on parent_id.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; sleep 2 &amp;&amp; curl -s http://localhost:3000 | grep -c "Parent"</automated>
  </verify>
  <done>Browser UI supports creating nested backlog items and viewing them in a hierarchical tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Bun server | Parent_id is user-supplied; must not allow arbitrary IDs or cycles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Circular parent references | mitigate | UI excludes current item and descendants from parent dropdown; API can validate parent_id is not a descendant of the item being updated (optional hardening) |
| T-01-08 | Information Disclosure | Descendant CTE could be expensive | accept | Local-only app; max items in hundreds; CTE performance is acceptable |
| T-01-09 | Denial of Service | Deep recursion in tree rendering | accept | Max depth enforced by business rules (epic→feature→story); UI renders limited depth |
</threat_model>

<verification>
1. `bun run dev` starts successfully.
2. Open browser to `http://localhost:3000`.
3. Create an epic titled "Platform Upgrade".
4. Create a feature titled "Auth Module" with parent "Platform Upgrade" — it appears indented under the epic.
5. Create a story titled "Login Form" with parent "Auth Module" — it appears indented under the feature.
6. Edit "Auth Module" and change its parent to "None" — it moves to root level.
7. Delete "Platform Upgrade" — its children remain visible (now as root items or with null parent).
</verification>

<success_criteria>
- User can create an epic, feature, or story and assign it to a parent item.
- The backlog view shows a clear hierarchy with indentation or grouping.
- Editing an item's parent immediately repositions it in the tree.
- The system prevents (or makes impossible) setting an item as its own ancestor.
- All hierarchy data persists across server restarts.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Decorators / Classes:**
- `BacklogRepository` (src/data/backlog-repository.ts) — extended with hierarchy methods.

**Functions / Methods:**
- `BacklogRepository.findRootItems()` — returns top-level items.
- `BacklogRepository.findChildren(parentId)` — returns direct children.
- `BacklogRepository.findDescendants(rootId)` — returns item and all nested children via recursive CTE.
- `BacklogTreeItem` component (src/components/BacklogList.tsx) — recursive render of item + children.

**New File Paths:**
- No new files; modifies existing components and repository.

**Modified Exports:**
- `BacklogList` now renders hierarchically.
- `BacklogForm` now includes parent selection.
