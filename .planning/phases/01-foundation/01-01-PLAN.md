---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - tailwind.config.ts
  - index.html
  - server.ts
  - src/data/schema.ts
  - src/data/backlog-repository.ts
  - src/domain/types.ts
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements:
  - BACK-01
user_setup: []

must_haves:
  truths:
    - "User can launch the app by running `bun run dev` and open http://localhost:3000"
    - "User can create a backlog item via a form in the browser"
    - "Created item appears in the list immediately and persists after server restart"
    - "Database tables backlog_items and file_hashes exist in scrumbag.db"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and dev/build scripts"
      contains: "scripts.dev, scripts.build, dependencies.react"
    - path: "server.ts"
      provides: "Bun.serve HTTP server with static SPA serving and API routes"
      exports: []
    - path: "src/data/schema.ts"
      provides: "SQLite schema initialization"
      exports: ["initSchema"]
    - path: "src/data/backlog-repository.ts"
      provides: "Repository pattern for backlog CRUD"
      exports: ["BacklogRepository"]
    - path: "src/App.tsx"
      provides: "React root component with form and list"
      exports: ["App"]
  key_links:
    - from: "src/App.tsx"
      to: "/api/backlog"
      via: "fetch POST and GET"
      pattern: "fetch.*api/backlog"
    - from: "server.ts"
      to: "src/data/backlog-repository.ts"
      via: "import and instantiation"
      pattern: "new BacklogRepository"
    - from: "src/data/backlog-repository.ts"
      to: "bun:sqlite"
      via: "Database import and queries"
      pattern: "bun:sqlite"
---

<objective>
Establish the Walking Skeleton: a single-executable Bun + React 19 SPA that serves a web UI, reads and writes to `bun:sqlite`, and proves the full stack end-to-end with a minimal backlog item form and list.

Purpose: Without this skeleton, no later slice can add features. This is the architectural backbone that validates Bun compile, SQLite persistence, static file serving, and React UI interactivity in one thin vertical slice.
Output: A runnable project where `bun run dev` starts a server on port 3000, the browser shows a working UI, and creating an item persists it to `scrumbag.db`.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project scaffold and build configuration</name>
  <files>package.json, tsconfig.json, vite.config.ts, tailwind.config.ts, index.html</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create package.json with: name "scrumbag", type "module", devDependencies (typescript, vite, @vitejs/plugin-react, @types/react, @types/react-dom, bun-types), dependencies (react, react-dom, tailwindcss, @tailwindcss/vite, zod). Use the exact SheetJS CDN tarball URL from RESEARCH.md for xlsx: "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz". Add scripts: "dev" = "bun server.ts", "build" = "vite build", "compile" = "vite build && bun build --compile --outfile scrumbag server.ts".
    Create tsconfig.json with: target "ESNext", module "ESNext", jsx "react-jsx", strict true, types include ["bun-types"], include ["src/**/*", "server.ts"].
    Create vite.config.ts: import { defineConfig } from "vite"; import react from "@vitejs/plugin-react"; import tailwindcss from "@tailwindcss/vite"; export default defineConfig({ plugins: [react(), tailwindcss()], build: { outDir: "dist" } }).
    Create tailwind.config.ts: import type { Config } from "tailwindcss"; export default { content: ["./index.html", "./src/**/*.{ts,tsx}"] } as Config.
    Create index.html with a div id="root" and script src="/src/main.tsx" type="module".
  </action>
  <acceptance_criteria>
    - package.json contains "type": "module" and scripts for dev, build, and compile.
    - package.json dependencies include react@19.2.6, react-dom@19.2.6, tailwindcss@4.3.0, @tailwindcss/vite, zod.
    - package.json devDependencies include vite@8.0.14, @vitejs/plugin-react, typescript.
    - tsconfig.json has "jsx": "react-jsx" and includes "bun-types" in types array.
    - vite.config.ts exports a config with react and tailwindcss plugins and outDir "dist".
    - index.html has a div with id="root".
    - The compile script includes the vite build step and outputs a binary named "scrumbag".
  </acceptance_criteria>
  <verify>
    <automated>bun install</automated>
  </verify>
  <done>bun install completes without errors; all config files exist with correct content.</done>
</task>

<task type="auto">
  <name>Task 2: Create SQLite schema, repository, and Bun server</name>
  <files>server.ts, src/data/schema.ts, src/data/backlog-repository.ts, src/domain/types.ts</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    In src/data/schema.ts, export function initSchema(db: Database) that creates two tables:
    1) backlog_items: id TEXT PRIMARY KEY, type TEXT NOT NULL CHECK(type IN ('epic','feature','story','bug')), title TEXT NOT NULL, description TEXT, parent_id TEXT REFERENCES backlog_items(id) ON DELETE SET NULL, status TEXT NOT NULL DEFAULT 'backlog', priority INTEGER DEFAULT 0, created_at TEXT DEFAULT CURRENT_TIMESTAMP, updated_at TEXT DEFAULT CURRENT_TIMESTAMP.
    2) file_hashes: path TEXT PRIMARY KEY, hash TEXT NOT NULL, synced_at TEXT DEFAULT CURRENT_TIMESTAMP.
    Use db.run for CREATE TABLE IF NOT EXISTS statements.
    In src/domain/types.ts, define minimal interfaces: BacklogItem { id: string; type: string; title: string; description: string | null; parent_id: string | null; status: string; priority: number; created_at: string; updated_at: string; } and NewBacklogItem { type: string; title: string; description?: string; parent_id?: string | null; status?: string; priority?: number; }.
    In src/data/backlog-repository.ts, export class BacklogRepository with constructor(db: Database). Implement findAll(): BacklogItem[] — query SELECT * FROM backlog_items ORDER BY created_at DESC. Implement create(item: NewBacklogItem): BacklogItem — generate id with crypto.randomUUID(), run INSERT, return the created item.
    In server.ts, import { Database } from "bun:sqlite", initSchema from "./src/data/schema.ts", BacklogRepository from "./src/data/backlog-repository.ts". Create new Database("scrumbag.db"). Call initSchema(db). Instantiate repository. Start Bun.serve on port 3000 with: static routes for "/" serving dist/index.html and "/assets/*" serving dist/assets files; fetch handler for /api/backlog (GET calls repo.findAll and returns JSON; POST parses body, calls repo.create, returns 201 with JSON). For non-API routes, serve dist/index.html as SPA fallback. Log "Scrumbag running at http://localhost:3000" on startup.
  </action>
  <acceptance_criteria>
    - schema.ts exports initSchema(db) that creates backlog_items and file_hashes tables with exact columns specified, including ON DELETE SET NULL on parent_id.
    - src/domain/types.ts exports BacklogItem and NewBacklogItem interfaces.
    - backlog-repository.ts exports BacklogRepository with findAll() and create() methods.
    - server.ts imports bun:sqlite, starts Bun.serve on port 3000, and handles GET/POST /api/backlog.
    - server.ts serves dist/index.html for SPA fallback on unknown routes.
    - scrumbag.db is created in the project root after first run.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; sleep 2 &amp;&amp; curl -s http://localhost:3000/api/backlog | grep -c "\[\]" &amp;&amp; curl -s -X POST -H "Content-Type: application/json" -d '{"type":"story","title":"Skeleton Test"}' http://localhost:3000/api/backlog | grep -c "Skeleton Test"</automated>
  </verify>
  <done>Server starts, GET /api/backlog returns empty array, POST /api/backlog creates and returns an item.</done>
</task>

<task type="auto">
  <name>Task 3: Create minimal React SPA proving full stack end-to-end</name>
  <files>src/main.tsx, src/App.tsx</files>
  <read_first>
    src/data/schema.ts
    src/data/backlog-repository.ts
    server.ts
  </read_first>
  <action>
    In src/main.tsx: import { StrictMode } from "react"; import { createRoot } from "react-dom/client"; import App from "./App"; createRoot(document.getElementById("root")!).render(&lt;StrictMode&gt;&lt;App /&gt;&lt;/StrictMode&gt;).
    In src/App.tsx: create a functional component App. Use useState for items (BacklogItem[]) and form fields (type: string, title: string). On mount, fetch("/api/backlog").then(r =&gt; r.json()).then(setItems). Provide a form with: a &lt;select&gt; for type (options: epic, feature, story, bug), an &lt;input&gt; for title, and a submit button. On submit, POST JSON to /api/backlog with {type, title}, then refresh the list. Display the items in a simple &lt;ul&gt; or table showing title and type. Use Tailwind utility classes for minimal layout (container, padding, border).
  </action>
  <acceptance_criteria>
    - main.tsx renders App into the #root element using React 18+ createRoot API.
    - App.tsx fetches /api/backlog on mount and displays the returned items.
    - App.tsx has a form with a type selector (epic/feature/story/bug) and a title input.
    - Submitting the form POSTs to /api/backlog and the new item appears in the list without page reload.
    - Tailwind CSS classes are present on at least one element.
    - After stopping and restarting the server, previously created items are still visible.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; DEV_PID=$! &amp;&amp; sleep 2 &amp;&amp; curl -s http://localhost:3000 | grep -c "root" &amp;&amp; kill $DEV_PID &amp;&amp; bun run build &amp;&amp; bun run compile &amp;&amp; ./scrumbag &amp; BIN_PID=$! &amp;&amp; sleep 2 &amp;&amp; curl -s http://localhost:3000 | grep -c "root" &amp;&amp; kill $BIN_PID</automated>
  </verify>
  <done>Browser at http://localhost:3000 shows a form and list; creating an item persists across restarts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Bun server | All user input crosses from untrusted browser to local server |
| File system ↔ Bun process | chokidar/fs access is within same process boundary; no external trust boundary yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm package install | mitigate | All packages are well-established (RESEARCH.md audit); bun install verifies lockfile if present |
| T-01-02 | Tampering | SQLite queries | mitigate | Repository uses parameterized queries via bun:sqlite query API; no string interpolation |
| T-01-03 | Denial of Service | Large POST body | accept | Local-only app; 3 internal users; no public network exposure |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | All packages marked Approved in RESEARCH.md Package Legitimacy Audit; no [ASSUMED] or [SUS] packages in dependency list |
</threat_model>

<verification>
1. Run `bun install` — completes with zero errors.
2. Run `bun run dev` — server starts and logs "Scrumbag running at http://localhost:3000".
3. Open browser to `http://localhost:3000` — page loads without 404s for CSS/JS assets.
4. Submit form with type "story" and title "Hello Skeleton" — item appears in list.
5. Stop server (Ctrl+C), run `bun run dev` again — item "Hello Skeleton" still appears.
6. Verify `scrumbag.db` exists in project root.
</verification>

<success_criteria>
- Project scaffold is complete with all config files.
- `bun run dev` starts a working full-stack app on port 3000.
- Browser UI allows creating a backlog item and viewing it in a list.
- Data persists to `scrumbag.db` and survives server restarts.
- No external services or credentials are required.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Decorators / Classes:**
- `BacklogRepository` (src/data/backlog-repository.ts) — repository class for backlog CRUD.

**Functions / Methods:**
- `initSchema(db)` (src/data/schema.ts) — initializes SQLite schema.
- `BacklogRepository.findAll()` — returns all backlog items ordered by created_at DESC.
- `BacklogRepository.create(item)` — inserts a new item and returns it.
- `Bun.serve` fetch handler (server.ts) — routes API and static requests.

**CLI Flags / Scripts:**
- `bun run dev` — starts the Bun server in development mode.
- `bun run build` — builds the React SPA to `dist/` via Vite.
- `bun run compile` — compiles server.ts into a single executable.

**Struct / Dataclass Fields:**
- `backlog_items` table: id, type, title, description, parent_id, status, priority, created_at, updated_at.
- `file_hashes` table: path, hash, synced_at.

**New File Paths:**
- `package.json`
- `tsconfig.json`
- `vite.config.ts`
- `tailwind.config.ts`
- `index.html`
- `server.ts`
- `src/data/schema.ts`
- `src/data/backlog-repository.ts`
- `src/main.tsx`
- `src/App.tsx`
- `scrumbag.db` (generated at runtime)
