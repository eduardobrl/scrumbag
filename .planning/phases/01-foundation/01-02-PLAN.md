---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/domain/types.ts
  - src/data/backlog-repository.ts
  - server.ts
  - src/components/BacklogList.tsx
  - src/components/BacklogForm.tsx
  - src/App.tsx
autonomous: true
requirements:
  - BACK-01

must_haves:
  truths:
    - "User can create backlog items of types epic, feature, story, or bug"
    - "User can view all backlog items in a table with type, title, status, and actions"
    - "User can edit an existing backlog item's title, description, type, and status"
    - "User can delete a backlog item and it disappears from the list"
  artifacts:
    - path: "src/domain/types.ts"
      provides: "Domain type definitions for backlog items"
      exports: ["BacklogItem", "NewBacklogItem", "BacklogItemType", "BacklogItemStatus"]
    - path: "src/data/backlog-repository.ts"
      provides: "Full CRUD repository"
      exports: ["BacklogRepository"]
    - path: "src/components/BacklogList.tsx"
      provides: "Table of backlog items with edit and delete actions"
      exports: ["BacklogList"]
    - path: "src/components/BacklogForm.tsx"
      provides: "Form for creating and editing backlog items"
      exports: ["BacklogForm"]
  key_links:
    - from: "src/components/BacklogList.tsx"
      to: "/api/backlog"
      via: "fetch DELETE and PUT"
      pattern: "fetch.*api/backlog"
    - from: "server.ts"
      to: "src/domain/types.ts"
      via: "type imports for request/response validation"
      pattern: "import.*domain/types"
---

<objective>
Deliver full backlog CRUD as a vertical slice: domain types, repository methods for read/update/delete, REST API endpoints, and React components for listing, creating, and editing backlog items.

Purpose: BACK-01 requires users to create and manage backlog items. The walking skeleton proved the stack; this plan makes the backlog feature fully usable.
Output: A UI where users can create, view, edit, and delete stories, features, bugs, and epics with validation and persistence.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define domain types and extend repository with full CRUD</name>
  <files>src/domain/types.ts, src/data/backlog-repository.ts</files>
  <read_first>
    src/data/schema.ts
    src/data/backlog-repository.ts
  </read_first>
  <action>
    In src/domain/types.ts, update the existing minimal types from Plan 01-01:
    - Add type BacklogItemType = "epic" | "feature" | "story" | "bug";
    - Add type BacklogItemStatus = "backlog" | "in_progress" | "done";
    - Refine interface BacklogItem to use BacklogItemType and BacklogItemStatus instead of string.
    - Refine interface NewBacklogItem to use BacklogItemType and BacklogItemStatus instead of string.
    - Add type UpdateBacklogItem = Partial&lt;Omit&lt;BacklogItem, "id" | "created_at" | "updated_at"&gt;&gt;;
    In src/data/backlog-repository.ts, add to BacklogRepository:
    - findById(id: string): BacklogItem | null — query with parameterized id, return item or null.
    - update(id: string, changes: UpdateBacklogItem): BacklogItem — build dynamic UPDATE query using only provided fields, set updated_at = CURRENT_TIMESTAMP, return updated item via findById.
    - delete(id: string): boolean — run DELETE, return true if changes > 0.
    Keep findAll and create from Plan 01-01. Ensure all methods use parameterized queries (e.g., db.query("SELECT * FROM backlog_items WHERE id = ?").get(id)).
  </action>
  <acceptance_criteria>
    - src/domain/types.ts exports BacklogItem, NewBacklogItem, BacklogItemType, BacklogItemStatus, and UpdateBacklogItem.
    - BacklogItemType is a union of exactly "epic", "feature", "story", "bug".
    - BacklogItemStatus is a union of exactly "backlog", "in_progress", "done".
    - BacklogRepository has findAll, create, findById, update, and delete methods.
    - findById returns null for non-existent ids.
    - update modifies only the fields provided in changes and updates updated_at.
    - delete returns true if a row was deleted, false otherwise.
    - All database queries use parameterized placeholders (?).
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; sleep 2 &amp;&amp; curl -s -X POST -H "Content-Type: application/json" -d '{"type":"story","title":"Repo Test"}' http://localhost:3000/api/backlog &gt; /tmp/create.json &amp;&amp; ID=$(cat /tmp/create.json | grep -o '"id":"[^"]*"' | cut -d'"' -f4) &amp;&amp; curl -s http://localhost:3000/api/backlog/$ID | grep -c "Repo Test" &amp;&amp; curl -s -X PUT -H "Content-Type: application/json" -d '{"title":"Updated"}' http://localhost:3000/api/backlog/$ID | grep -c "Updated" &amp;&amp; curl -s -X DELETE http://localhost:3000/api/backlog/$ID -w "%{http_code}" | grep -c "204"</automated>
  </verify>
  <done>Repository supports full CRUD and all API endpoints respond correctly via curl.</done>
</task>

<task type="auto">
  <name>Task 2: Implement full backlog REST API with validation</name>
  <files>server.ts</files>
  <read_first>
    server.ts
    src/domain/types.ts
    src/data/backlog-repository.ts
  </read_first>
  <action>
    Extend the Bun.serve fetch handler in server.ts to add:
    - GET /api/backlog/:id — call repo.findById(id), return 200 with JSON if found, 404 if null.
    - PUT /api/backlog/:id — parse body as JSON, call repo.update(id, body), return 200 with updated item or 404 if not found.
    - DELETE /api/backlog/:id — call repo.delete(id), return 204 if true, 404 if false.
    - Enhance POST /api/backlog — use Zod schema validation. Define z.object({ type: z.enum(["epic","feature","story","bug"]), title: z.string().min(1), description: z.string().optional(), parent_id: z.string().nullable().optional(), status: z.enum(["backlog","in_progress","done"]).optional(), priority: z.number().optional() }). Parse body with schema.safeParse(). If invalid, return 400 with JSON { error: string }.
    Add helper function parseJson(req: Request) that safely parses JSON and returns 400 on SyntaxError. Ensure all API routes set Content-Type: application/json on responses.
  </action>
  <acceptance_criteria>
    - GET /api/backlog/:id returns 200 and the item JSON for existing ids.
    - GET /api/backlog/:id returns 404 for non-existing ids.
    - PUT /api/backlog/:id returns 200 and updated item for existing ids.
    - PUT /api/backlog/:id returns 404 for non-existing ids.
    - DELETE /api/backlog/:id returns 204 for existing ids.
    - DELETE /api/backlog/:id returns 404 for non-existing ids.
    - POST /api/backlog returns 400 if type is missing or invalid, or if title is missing or empty.
    - All API responses include Content-Type: application/json (except 204 which has no body).
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; sleep 2 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/backlog/nonexistent | grep -c "404" &amp;&amp; curl -s -o /dev/null -w "%{http_code}" -X POST -H "Content-Type: application/json" -d '{"type":"invalid","title":"x"}' http://localhost:3000/api/backlog | grep -c "400" &amp;&amp; curl -s -o /dev/null -w "%{http_code}" -X POST -H "Content-Type: application/json" -d '{"title":"missing type"}' http://localhost:3000/api/backlog | grep -c "400"</automated>
  </verify>
  <done>All CRUD endpoints exist with proper HTTP status codes and input validation.</done>
</task>

<task type="auto">
  <name>Task 3: Build backlog list and form UI components</name>
  <files>src/components/BacklogList.tsx, src/components/BacklogForm.tsx, src/App.tsx</files>
  <read_first>
    src/App.tsx
    src/domain/types.ts
    src/data/backlog-repository.ts
    server.ts
  </read_first>
  <action>
    In src/components/BacklogList.tsx: Create BacklogList component accepting props { items: BacklogItem[], onEdit: (item: BacklogItem) => void, onDelete: (id: string) => void }. Render items in a table (&lt;table&gt;) with columns: Type, Title, Status, Actions. In Actions, show "Edit" and "Delete" buttons. Delete should call window.confirm before invoking onDelete. Use Tailwind for borders, padding, and hover states.
    In src/components/BacklogForm.tsx: Create BacklogForm component accepting props { onSubmit: (item: NewBacklogItem) => void, initialItem?: BacklogItem | null }. Render fields: type (&lt;select&gt; with epic/feature/story/bug), title (&lt;input&gt;), description (&lt;textarea&gt;), status (&lt;select&gt; with backlog/in_progress/done). If initialItem is provided, populate fields and call onSubmit with UpdateBacklogItem shape on save. Include a "Cancel" button that clears the form.
    In src/App.tsx: Replace the minimal skeleton UI. Maintain state for items (BacklogItem[]), editingItem (BacklogItem | null), and form visibility. Fetch /api/backlog on mount. Render BacklogForm at the top. Render BacklogList below. Implement handlers: handleCreate submits POST /api/backlog and refreshes list; handleUpdate submits PUT /api/backlog/:id and refreshes; handleDelete submits DELETE and refreshes. Use useEffect for initial load.
  </action>
  <acceptance_criteria>
    - BacklogList renders a &lt;table&gt; with columns for Type, Title, Status, and Actions.
    - Each row has an Edit button that populates the form with that item's data.
    - Each row has a Delete button that triggers a browser confirm dialog; on confirm, the item is removed.
    - BacklogForm has select inputs for type and status, and text inputs for title and description.
    - Submitting the form with empty title shows a visual error or prevents submission (client-side check is sufficient for now).
    - After creating an item, it appears at the top of the list within 1 second without manual refresh.
    - After editing an item, the list reflects the change.
    - After deleting an item, it disappears from the list.
    - App.tsx integrates both components and manages fetching state.
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; sleep 2 &amp;&amp; curl -s http://localhost:3000 | grep -c "Backlog"</automated>
  </verify>
  <done>Browser UI supports creating, viewing, editing, and deleting backlog items of all four types with validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Bun server | User input crosses from browser to API; validation is critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02b | Tampering | SQLite injection via API | mitigate | All repository queries use parameterized placeholders (?); no dynamic SQL string building |
| T-01-04 | Information Disclosure | Error messages leak stack traces | mitigate | API returns generic { error: string } on failure; no raw exception details in response body |
| T-01-05 | Denial of Service | Malformed JSON body | mitigate | parseJson helper catches SyntaxError and returns 400 before reaching repository |
| T-01-06 | Elevation of Privilege | No auth on CRUD endpoints | accept | Local-only single-user app; corporate network is trust boundary per PROJECT.md |
</threat_model>

<verification>
1. `bun run dev` starts successfully.
2. Open browser to `http://localhost:3000`.
3. Create items of each type (epic, feature, story, bug) — all appear in the table.
4. Edit an item — title and status update in the table.
5. Delete an item — it disappears after confirmation.
6. Try creating with empty title — form prevents submission or API returns 400.
7. Restart server — all items persist in the table.
</verification>

<success_criteria>
- User can create backlog items of types epic, feature, story, and bug.
- User can view all items in a styled table.
- User can edit any field of an existing item.
- User can delete an item with confirmation.
- Input validation prevents empty titles or invalid types.
- All changes persist to SQLite and survive restarts.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Decorators / Classes:**
- `BacklogRepository` (src/data/backlog-repository.ts) — extended with findById, update, delete.

**Functions / Methods:**
- `BacklogRepository.findById(id)` (src/data/backlog-repository.ts) — returns single item or null.
- `BacklogRepository.update(id, changes)` (src/data/backlog-repository.ts) — partial update with dynamic SET clause.
- `BacklogRepository.delete(id)` (src/data/backlog-repository.ts) — returns boolean success.
- `parseJson(req)` (server.ts) — safely parses JSON body, returns 400 on SyntaxError.
- `BacklogList` component (src/components/BacklogList.tsx) — table with edit/delete actions.
- `BacklogForm` component (src/components/BacklogForm.tsx) — form for create/edit with validation.

**Type Definitions:**
- `BacklogItem` (src/domain/types.ts)
- `NewBacklogItem` (src/domain/types.ts)
- `UpdateBacklogItem` (src/domain/types.ts)
- `BacklogItemType` union type (src/domain/types.ts)
- `BacklogItemStatus` union type (src/domain/types.ts)

**New File Paths:**
- `src/domain/types.ts`
- `src/components/BacklogList.tsx`
- `src/components/BacklogForm.tsx`
