---
phase: 01-local-foundation-and-squad-setup
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/api/settings/route.ts
  - src/app/settings/page.tsx
  - src/features/settings/settings-form.tsx
  - src/lib/settings.ts
  - tests/settings.test.ts
  - tests/e2e/settings.spec.ts
autonomous: true
requirements:
  - APP-04
  - APP-05
  - APP-06
user_setup: []
must_haves:
  truths:
    - "The Settings screen lets the user view and save full-time hours, intern hours, standard day hours, MCP host, MCP port, and MCP enabled state."
    - "Settings persist in SQLite and remain visible after reload."
    - "Capacity defaults use full-time 8h, intern 6h, standard day 8h, MCP host localhost, and MCP port 3333 until the user changes them."
    - "The global header can read stored settings without breaking when no release exists."
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "Settings route connected to stored AppSettings"
      min_lines: 20
    - path: "src/features/settings/settings-form.tsx"
      provides: "Interactive settings edit form"
      min_lines: 70
    - path: "src/app/api/settings/route.ts"
      provides: "Settings read/write API"
      exports:
        - GET
        - PUT
    - path: "src/lib/settings.ts"
      provides: "Default and persisted settings helpers"
      contains: "getOrCreateSettings"
  key_links:
    - from: "src/features/settings/settings-form.tsx"
      to: "/api/settings"
      via: "client fetch for save"
      pattern: "fetch\\('/api/settings'"
    - from: "src/app/api/settings/route.ts"
      to: "prisma.appSettings"
      via: "Prisma upsert or update"
      pattern: "prisma\\.appSettings\\.(upsert|update|findFirst)"
---

<objective>
Implement persisted app settings for capacity defaults and local MCP configuration.

Purpose: Give future capacity and MCP phases a durable local configuration surface while completing APP-06.
Output: Settings route, settings API, reusable settings helper, validation tests, and browser persistence coverage.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-foundation-and-squad-setup/01-SKELETON.md
@.planning/phases/01-local-foundation-and-squad-setup/01-01-SUMMARY.md
@spec.md
@telas.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable settings data access and API validation</name>
  <files>src/lib/settings.ts, src/app/api/settings/route.ts, tests/settings.test.ts</files>
  <read_first>src/lib/settings.ts, prisma/schema.prisma, spec.md, telas.md</read_first>
  <action>Extend `src/lib/settings.ts` with `getOrCreateSettings()` and `updateSettings(input)`. Store exactly one AppSettings row; use Prisma `findFirst` plus `create` or `upsert`. Validate numeric fields: `workingHoursFullTime > 0`, `workingHoursIntern > 0`, `standardDayHours > 0`, `mcpPort` integer from 1 to 65535, and `mcpHost` non-empty. Implement `/api/settings` with `GET` returning the stored/default settings and `PUT` saving validated updates. Return HTTP 400 with field messages for invalid input.</action>
  <verify>npm run test -- settings</verify>
  <acceptance_criteria>
    - `src/lib/settings.ts` exports `getOrCreateSettings` and `updateSettings`.
    - `/api/settings` exports `GET` and `PUT`.
    - Invalid `mcpPort` outside 1-65535 returns HTTP 400.
    - Tests cover default creation and at least one invalid numeric field.
  </acceptance_criteria>
  <done>Settings can be read, initialized, updated, and rejected when invalid.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Settings screen and persistence flow</name>
  <files>src/app/settings/page.tsx, src/features/settings/settings-form.tsx, tests/e2e/settings.spec.ts</files>
  <read_first>telas.md, src/components/app-shell.tsx, src/components/ui/button.tsx, src/components/ui/input.tsx, src/app/api/settings/route.ts</read_first>
  <action>Create a desktop-first Settings page matching `telas.md` section 11. Include numeric inputs for `workingHoursFullTime`, `workingHoursIntern`, `standardDayHours`, `mcpPort`; text input for `mcpHost`; checkbox/toggle for `mcpEnabled`; read-only database path text `./data/squad-planner.db`; save button. On save, call `PUT /api/settings`, show an inline success state, and keep validation errors next to fields. Add Playwright coverage that changes intern hours to `5`, saves, reloads `/settings`, and still sees `5`.</action>
  <verify>npm run build; npm run test:e2e -- settings</verify>
  <acceptance_criteria>
    - `src/app/settings/page.tsx` renders the current values from `getOrCreateSettings()`.
    - `src/features/settings/settings-form.tsx` contains labels for Full-time hours, Intern hours, Standard day hours, MCP host, and MCP port.
    - The database path `./data/squad-planner.db` is visible on the Settings screen.
    - The e2e test proves a saved setting survives reload.
  </acceptance_criteria>
  <done>The user can configure Phase 1 capacity/MCP defaults locally and see them persist.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- settings` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- settings` succeeds.
- [ ] Manual browser check at `http://localhost:3000/settings` shows all APP-06 fields and the local database path.
</verification>

<success_criteria>
- APP-06 is complete with persisted settings for full-time hours, intern hours, standard day hours, MCP host, and MCP port.
- APP-05 remains true for settings data across app restarts/reloads.
- Settings do not enable remote exposure; default MCP host remains `localhost`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-foundation-and-squad-setup/01-02-SUMMARY.md`
</output>
