---
phase: 01-local-foundation-and-squad-setup
plan: "01"
subsystem: foundation
tags: [nextjs, react, prisma, sqlite, tailwind, vitest, playwright]
requires: []
provides:
  - Local Next.js App Router scaffold with desktop app shell
  - Prisma SQLite schema for Phase 1 local persistence
  - SQLite-backed squad member create/list loop
  - Unit, build, lint, and browser smoke verification
affects: [phase-01, phase-02, phase-03, phase-04, phase-05]
tech-stack:
  added: [next, react, prisma, sqlite, tailwind, vitest, playwright, lucide-react]
  patterns:
    - App Router server pages with client form islands
    - Prisma data helpers under src/lib
    - Local SQLite database at ./data/squad-planner.db
key-files:
  created:
    - package.json
    - prisma/schema.prisma
    - prisma.config.ts
    - scripts/db-sync.ts
    - src/components/app-shell.tsx
    - src/app/page.tsx
    - src/app/api/squad-members/route.ts
    - src/lib/squad.ts
    - tests/e2e/local-smoke.spec.ts
  modified:
    - .gitignore
    - README.md
key-decisions:
  - "Pinned the local runtime to localhost with Next dev host configuration."
  - "Used Prisma 7 with better-sqlite3 adapter because Node 24 is active on this machine."
  - "Added a local SQLite sync script after Prisma schema-engine apply failed while schema validation and SQL generation succeeded."
patterns-established:
  - "Operational shell: fixed side navigation plus global release/capacity header."
  - "Persistence helpers: route handlers call typed src/lib helpers rather than embedding Prisma logic in UI."
  - "Browser proof: Playwright tests reset local data, create through UI, reload, and assert persistence."
requirements-completed: [APP-01, APP-02, APP-03, APP-04, APP-05, SQUAD-01, SQUAD-02]
duration: 1h 12m
completed: 2026-06-02
---

# Phase 1 Plan 01: Walking Skeleton Summary

**Local Next.js and Prisma SQLite app shell with persisted squad member creation from the dashboard**

## Performance

- **Duration:** 1h 12m
- **Started:** 2026-06-02T20:23:00-03:00
- **Completed:** 2026-06-02T21:35:18-03:00
- **Tasks:** 3 completed
- **Files modified:** 45

## Accomplishments

- Scaffolded a runnable Next.js App Router project with TypeScript, Tailwind, Prisma, Vitest, and Playwright.
- Added the full v1 navigation shell and global header with active release, status, capacity, and Assistant AI entry points.
- Implemented the first real SQLite-backed loop: create a squad member, list it, reload the browser, and see it persist.
- Documented local setup and kept runtime data under `./data/squad-planner.db`.

## Task Commits

1. **Tasks 1-3: Walking skeleton, app shell, and persisted member loop** - `11e10c7` (feat)

**Plan metadata:** pending in this summary commit.

## Files Created/Modified

- `package.json` - Local scripts for dev, build, lint, test, e2e, Prisma generation, migration, and seed.
- `prisma/schema.prisma` - Phase 1 models for squad members, absences, holidays, and app settings.
- `prisma.config.ts` - Prisma 7 datasource configuration for the local SQLite database.
- `scripts/db-sync.ts` - Local SQLite schema preparation script.
- `src/components/app-shell.tsx` - Desktop-first side navigation and global header.
- `src/app/page.tsx` - Operational dashboard with summary cards and member creation.
- `src/app/api/squad-members/route.ts` - GET/POST API for persisted squad members.
- `src/lib/squad.ts` - Validation plus Prisma create/list helpers.
- `tests/squad-member.test.ts` - Unit coverage for member validation.
- `tests/e2e/local-smoke.spec.ts` - Browser persistence smoke test.

## Decisions Made

- Used Prisma 7 because the installed Node version is 24 and Prisma 7 declares support for Node 24.
- Kept the SQLite database local at `./data/squad-planner.db` and generated by a local setup command.
- Scoped lint to app, test, Prisma, and config files so hidden GSD tool bundles are not linted as application source.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Prisma schema engine apply failed in this Windows/Node 24 environment**
- **Found during:** Task 1 verification
- **Issue:** `prisma db push` and `prisma migrate dev` both returned a terse schema-engine error, while `prisma validate` and `prisma migrate diff --script` succeeded.
- **Fix:** Upgraded Prisma/client to 7.8.0, added the required better-sqlite3 adapter, moved datasource config to `prisma.config.ts`, and added `scripts/db-sync.ts` to prepare the local SQLite tables reliably.
- **Files modified:** `package.json`, `package-lock.json`, `prisma.config.ts`, `prisma/schema.prisma`, `src/lib/db.ts`, `scripts/db-sync.ts`, `README.md`
- **Verification:** `npm run db:generate`, `npm run test`, `npm run build`, and `npm run test:e2e -- local-smoke` pass.
- **Committed in:** `11e10c7`

**2. [Rule 3 - Blocking] ESLint scanned hidden GSD runtime folders**
- **Found during:** Task 2 verification
- **Issue:** `eslint .` walked `.claude/` and `.opencode/`, producing unrelated CommonJS lint errors from local tooling.
- **Fix:** Scoped the lint script to application, tests, Prisma, and config files.
- **Files modified:** `package.json`, `eslint.config.mjs`
- **Verification:** `npm run lint` passes.
- **Committed in:** `11e10c7`

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes preserve the local-first Phase 1 scope and make the planned verification reliable on this host.

## Issues Encountered

- `npm.ps1` is blocked by PowerShell execution policy. Commands were run through `npm.cmd`, and package scripts remain standard `npm run ...` commands for normal shells.
- npm audit reports 6 dependency advisories. No forced audit fix was applied because it would make broad dependency changes during the scaffold.

## Verification

- `npm run db:generate` - passed
- `npm run lint` - passed
- `npm run test` - passed
- `npm run build` - passed
- `npm run test:e2e -- local-smoke` - passed

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Wave 2 can build on the app shell, Prisma client, local SQLite file, settings model, squad member model, and browser test harness. Settings persistence and full squad/calendar management are ready to implement.

---
*Phase: 01-local-foundation-and-squad-setup*
*Completed: 2026-06-02*
