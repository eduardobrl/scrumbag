---
phase: 01-local-foundation-and-squad-setup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - eslint.config.mjs
  - vitest.config.ts
  - playwright.config.ts
  - prisma/schema.prisma
  - prisma/seed.ts
  - data/.gitkeep
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/api/squad-members/route.ts
  - src/app/api/health/route.ts
  - src/components/app-shell.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/features/squad/member-quick-create.tsx
  - src/features/squad/member-list.tsx
  - src/lib/db.ts
  - src/lib/navigation.ts
  - src/lib/settings.ts
  - src/lib/squad.ts
  - src/lib/validation.ts
  - tests/squad-member.test.ts
  - tests/e2e/local-smoke.spec.ts
  - README.md
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
  - APP-04
  - APP-05
  - SQUAD-01
  - SQUAD-02
user_setup: []
must_haves:
  truths:
    - "A fresh checkout can install dependencies and run Squad Planner on localhost with `npm run dev`."
    - "The app renders a desktop-first shell with navigation links for Dashboard, Releases, Features/Stories, Backlog, Sprints, Squad, Reports, Assistant AI, and Settings."
    - "The global header shows active release context, release status placeholder, capacity placeholder, and an Assistant AI entry point even before a release exists."
    - "Creating a squad member from the UI writes to SQLite and the member remains visible after page refresh."
    - "SQLite data lives under `./data/squad-planner.db` and Prisma uses that local file by default."
  artifacts:
    - path: "package.json"
      provides: "Local install/start/build/test scripts"
      contains: "dev"
    - path: "prisma/schema.prisma"
      provides: "SQLite models for Phase 1 persistence"
      contains: "model SquadMember"
    - path: "src/app/layout.tsx"
      provides: "Root app shell mount"
      min_lines: 20
    - path: "src/components/app-shell.tsx"
      provides: "Side navigation and global header"
      min_lines: 80
    - path: "src/features/squad/member-quick-create.tsx"
      provides: "Interactive member creation UI"
      min_lines: 50
    - path: "src/app/api/squad-members/route.ts"
      provides: "Real database read/write route for squad members"
      exports:
        - GET
        - POST
  key_links:
    - from: "src/features/squad/member-quick-create.tsx"
      to: "/api/squad-members"
      via: "client fetch for member creation"
      pattern: "fetch\\('/api/squad-members'"
    - from: "src/app/api/squad-members/route.ts"
      to: "prisma.squadMember"
      via: "Prisma create/findMany calls"
      pattern: "prisma\\.squadMember\\.(findMany|create)"
    - from: "prisma/schema.prisma"
      to: "./data/squad-planner.db"
      via: "SQLite datasource URL from DATABASE_URL"
      pattern: "provider = \"sqlite\""
---

<objective>
Create the walking skeleton for Squad Planner as a local Next.js app with SQLite persistence and one real user-visible write/read loop.

Purpose: Prove the full local stack before later phases add releases, stories, capacity, reports, MCP, and AI.
Output: A runnable Next.js project, Prisma SQLite schema, app shell navigation/header, member quick-create UI, API route, tests, and local run documentation.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-foundation-and-squad-setup/01-SKELETON.md
@spec.md
@telas.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the local app and persistence baseline</name>
  <files>package.json, package-lock.json, next.config.ts, tsconfig.json, postcss.config.mjs, eslint.config.mjs, vitest.config.ts, playwright.config.ts, prisma/schema.prisma, prisma/seed.ts, data/.gitkeep, src/lib/db.ts, src/lib/settings.ts, src/lib/validation.ts, README.md</files>
  <read_first>spec.md, telas.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create a Next.js App Router TypeScript project in the existing repo without moving `.planning`. Add scripts: `dev`, `build`, `lint`, `test`, `test:e2e`, `db:generate`, `db:migrate`, and `db:seed`. Configure Prisma datasource `db` with provider `sqlite` and default `DATABASE_URL="file:./data/squad-planner.db"` in the local environment docs. Define Phase 1 models: `SquadMember` with `id`, `name`, `roleType` enum `FULL_TIME | INTERN`, `active`, timestamps; `Absence` with `memberId`, `type` enum `VACATION | DAY_OFF`, start/end dates, notes; `Holiday` with date/name; `AppSettings` with `workingHoursFullTime`, `workingHoursIntern`, `standardDayHours`, `mcpHost`, `mcpPort`, `mcpEnabled`, timestamps. Add `src/lib/db.ts` exporting a singleton Prisma client. Add `src/lib/settings.ts` with defaults: full time 8, intern 6, standard day 8, MCP host `localhost`, MCP port 3333, enabled false. Document `npm install`, `npm run db:migrate`, and `npm run dev` in README.</action>
  <verify>npm install; npm run db:generate; npm run build</verify>
  <acceptance_criteria>
    - `package.json` contains scripts named `dev`, `build`, `lint`, `test`, `test:e2e`, `db:generate`, `db:migrate`, and `db:seed`.
    - `prisma/schema.prisma` contains `provider = "sqlite"`, `model SquadMember`, `model Absence`, `model Holiday`, and `model AppSettings`.
    - `src/lib/settings.ts` contains default values `8`, `6`, `8`, `localhost`, and `3333`.
    - `README.md` documents opening `http://localhost:3000`.
  </acceptance_criteria>
  <done>The project installs, Prisma generates, and the build command runs against the scaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Build the desktop app shell with all Phase 1 navigation targets</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, src/components/app-shell.tsx, src/components/ui/badge.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/lib/navigation.ts</files>
  <read_first>telas.md, .planning/REQUIREMENTS.md, src/app/layout.tsx, src/app/globals.css</read_first>
  <action>Implement a quiet desktop-first operational layout. `src/lib/navigation.ts` must export exactly these nav labels and hrefs: Dashboard `/`, Releases `/releases`, Features/Stories `/features`, Backlog `/backlog`, Sprints `/sprints`, Squad `/squad`, Reports `/reports`, Assistant AI `/assistant`, Settings `/settings`. `AppShell` must render a fixed side navigation, a global header, active release text `No active release`, status badge `Not configured`, capacity badge `Capacity: --`, and an Assistant AI button/link. Create placeholder pages only where needed for nav to avoid broken routes, with copy that states the section is planned for a later phase without adding fake functionality. Keep cards radius at 8px or less and avoid marketing hero layout.</action>
  <verify>npm run lint; npm run build</verify>
  <acceptance_criteria>
    - `src/lib/navigation.ts` includes all nine required nav labels.
    - `src/components/app-shell.tsx` contains `No active release`, `Not configured`, `Capacity: --`, and `Assistant AI`.
    - Browser navigation to each route returns a rendered page instead of a 404.
    - The first screen is the operational dashboard shell, not a landing page.
  </acceptance_criteria>
  <done>The user can navigate through the full v1 shell from localhost without broken routes.</done>
</task>

<task type="auto">
  <name>Task 3: Add the first real SQLite-backed squad member interaction</name>
  <files>src/app/api/squad-members/route.ts, src/app/page.tsx, src/features/squad/member-quick-create.tsx, src/features/squad/member-list.tsx, src/lib/squad.ts, tests/squad-member.test.ts, tests/e2e/local-smoke.spec.ts</files>
  <read_first>spec.md, telas.md, prisma/schema.prisma, src/lib/db.ts, src/components/app-shell.tsx</read_first>
  <action>Create `src/lib/squad.ts` with functions `listSquadMembers()` and `createSquadMember(input)` using Prisma. Validate that name is required and roleType is `FULL_TIME` or `INTERN`; default active to true. Implement `GET` and `POST` in `/api/squad-members` using those functions and JSON responses. Add a dashboard section that renders current members and a client component form with name input, role segmented/select control, active toggle, and save button. On save, call `fetch('/api/squad-members', { method: 'POST' })`, then refresh the visible member list. Add Vitest coverage for validation and a Playwright smoke test that opens localhost, creates member `Ana`, reloads, and still sees `Ana`.</action>
  <verify>npm run test; npm run build; npm run test:e2e</verify>
  <acceptance_criteria>
    - `src/app/api/squad-members/route.ts` exports `GET` and `POST`.
    - `src/lib/squad.ts` uses `prisma.squadMember.findMany` and `prisma.squadMember.create`.
    - The UI can create a `FULL_TIME` or `INTERN` member without leaving the dashboard.
    - The Playwright test proves persistence by reloading after creation.
  </acceptance_criteria>
  <done>A local user can create a squad member in the browser and see persisted SQLite data after refresh.</done>
</task>

</tasks>

<verification>
- [ ] `npm install` succeeds from a fresh checkout.
- [ ] `npm run db:generate` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test` succeeds.
- [ ] `npm run test:e2e` creates and reloads a persisted squad member on `http://localhost:3000`.
</verification>

<success_criteria>
- APP-01 through APP-05 are substantively covered by a runnable local app, localhost browser access, navigation/header shell, and SQLite persistence.
- SQUAD-01 and SQUAD-02 have a thin but real create/list path for members and role type.
- No auth, remote DB, external integration, or network exposure outside localhost is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-foundation-and-squad-setup/01-01-SUMMARY.md`
</output>
