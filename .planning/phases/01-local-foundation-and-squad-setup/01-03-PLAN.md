---
phase: 01-local-foundation-and-squad-setup
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/api/absences/route.ts
  - src/app/api/holidays/route.ts
  - src/app/api/squad-members/[id]/route.ts
  - src/app/squad/page.tsx
  - src/features/squad/absence-form.tsx
  - src/features/squad/absence-table.tsx
  - src/features/squad/holiday-form.tsx
  - src/features/squad/holiday-table.tsx
  - src/features/squad/member-form.tsx
  - src/features/squad/member-table.tsx
  - src/features/squad/squad-summary.tsx
  - src/lib/capacity-summary.ts
  - src/lib/squad.ts
  - tests/squad-calendar.test.ts
  - tests/e2e/squad.spec.ts
autonomous: true
requirements:
  - APP-05
  - SQUAD-01
  - SQUAD-02
  - SQUAD-03
  - SQUAD-04
  - SQUAD-05
user_setup: []
must_haves:
  truths:
    - "The Squad screen supports creating, editing, activating, and deactivating squad members."
    - "Each squad member records either full-time or intern schedule and shows the corresponding hours/day from settings defaults."
    - "The user can register vacations and day-off absences for members with start date, end date, type, and notes."
    - "The user can register holidays that apply to all active members."
    - "The Squad screen shows active member count, estimated daily gross capacity, future absences, holiday count, and absence impact placeholders ready for sprint capacity."
    - "Members, absences, and holidays persist in SQLite after reload."
  artifacts:
    - path: "src/app/squad/page.tsx"
      provides: "Full Squad management route"
      min_lines: 40
    - path: "src/lib/squad.ts"
      provides: "Member, absence, and holiday CRUD helpers"
      min_lines: 120
    - path: "src/lib/capacity-summary.ts"
      provides: "Phase 1 gross daily capacity and future calendar summary"
      min_lines: 40
    - path: "src/app/api/absences/route.ts"
      provides: "Absence create/list API"
      exports:
        - GET
        - POST
    - path: "src/app/api/holidays/route.ts"
      provides: "Holiday create/list API"
      exports:
        - GET
        - POST
    - path: "src/app/api/squad-members/[id]/route.ts"
      provides: "Member update API"
      exports:
        - PATCH
  key_links:
    - from: "src/features/squad/absence-form.tsx"
      to: "/api/absences"
      via: "client fetch for absence creation"
      pattern: "fetch\\('/api/absences'"
    - from: "src/features/squad/holiday-form.tsx"
      to: "/api/holidays"
      via: "client fetch for holiday creation"
      pattern: "fetch\\('/api/holidays'"
    - from: "src/lib/capacity-summary.ts"
      to: "src/lib/settings.ts"
      via: "daily capacity uses persisted/default working hours"
      pattern: "getOrCreateSettings"
---

<objective>
Complete Phase 1 squad and calendar management on top of the walking skeleton.

Purpose: Capture the local data future capacity calculations depend on: active members, schedules, absences, holidays, and daily capacity summary.
Output: Full Squad route, member edit/activation flow, absence and holiday CRUD, summary metrics, validation tests, and browser persistence checks.
</objective>

<execution_context>
@C:/Users/eduar/scrumbag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/eduar/scrumbag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-foundation-and-squad-setup/01-SKELETON.md
@.planning/phases/01-local-foundation-and-squad-setup/01-01-SUMMARY.md
@spec.md
@telas.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand squad data access and member editing</name>
  <files>src/lib/squad.ts, src/app/api/squad-members/[id]/route.ts, src/features/squad/member-form.tsx, src/features/squad/member-table.tsx, tests/squad-calendar.test.ts</files>
  <read_first>src/lib/squad.ts, src/app/api/squad-members/route.ts, prisma/schema.prisma, spec.md, telas.md</read_first>
  <action>Extend `src/lib/squad.ts` with `updateSquadMember(id, input)` and validation for required name, valid role type, and boolean active flag. Implement `PATCH /api/squad-members/[id]` to update name, `roleType`, and `active`. Build `member-form.tsx` and `member-table.tsx` for the Squad route: list members with name, jornada label `Full time` or `Intern`, hours/day, status badge Active/Inactive, and edit controls. Deactivation must set `active=false`; do not hard-delete members.</action>
  <verify>npm run test -- squad-calendar; npm run build</verify>
  <acceptance_criteria>
    - `src/app/api/squad-members/[id]/route.ts` exports `PATCH`.
    - Member table shows active and inactive members.
    - Deactivating a member updates `active` to false without deleting the row.
    - Tests cover invalid empty member name and deactivation.
  </acceptance_criteria>
  <done>Members can be created, edited, activated, and deactivated with local persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Add absence and holiday CRUD flows</name>
  <files>src/lib/squad.ts, src/app/api/absences/route.ts, src/app/api/holidays/route.ts, src/features/squad/absence-form.tsx, src/features/squad/absence-table.tsx, src/features/squad/holiday-form.tsx, src/features/squad/holiday-table.tsx, tests/squad-calendar.test.ts</files>
  <read_first>spec.md, telas.md, prisma/schema.prisma, src/lib/squad.ts</read_first>
  <action>Add `listAbsences`, `createAbsence`, `listHolidays`, and `createHoliday` helpers. Validate absence member exists, absence type is `VACATION` or `DAY_OFF`, and end date is greater than or equal to start date. Validate holiday date and required name. Implement `GET` and `POST` routes for `/api/absences` and `/api/holidays`. Build forms and tables on the Squad screen for absences and holidays using the labels from `telas.md`: member, type, start date, end date, notes, date, name. Store dates consistently as ISO date strings or Date objects normalized to date-only display; use one convention across API and UI.</action>
  <verify>npm run test -- squad-calendar; npm run build</verify>
  <acceptance_criteria>
    - `/api/absences` and `/api/holidays` export `GET` and `POST`.
    - Absence validation rejects end date before start date.
    - Holiday validation rejects empty name.
    - The Squad page displays newly created absences and holidays after reload.
  </acceptance_criteria>
  <done>Vacations, day offs, and holidays can be registered and persisted locally.</done>
</task>

<task type="auto">
  <name>Task 3: Show Phase 1 capacity/calendar summary metrics</name>
  <files>src/lib/capacity-summary.ts, src/features/squad/squad-summary.tsx, src/app/squad/page.tsx, tests/squad-calendar.test.ts, tests/e2e/squad.spec.ts</files>
  <read_first>spec.md, telas.md, src/lib/settings.ts, src/lib/squad.ts, src/app/squad/page.tsx</read_first>
  <action>Create `src/lib/capacity-summary.ts` to calculate Phase 1 summary metrics: active member count; daily gross capacity hours using full-time and intern hours from settings; count of future absences where end date is today or later; count of holidays; and an absence impact summary grouped by absence type. Render summary cards on `/squad` for `Active members`, `Daily capacity`, `Future absences`, and `Holidays`. Add a clearly labeled `Absence impact by sprint` placeholder that says sprint impact appears after Phase 2 creates sprints; do not invent sprint data in Phase 1. Add Playwright coverage that creates a member, absence, and holiday, reloads `/squad`, and sees the summary counts update.</action>
  <verify>npm run test; npm run build; npm run test:e2e -- squad</verify>
  <acceptance_criteria>
    - `src/lib/capacity-summary.ts` uses settings values instead of hard-coded role hours.
    - `/squad` shows active member count, daily capacity, future absences, holidays, and absence impact placeholder.
    - No Phase 2 release or sprint models are required for the Phase 1 summary to work.
    - The e2e test proves member, absence, holiday, and summary persistence after reload.
  </acceptance_criteria>
  <done>The Squad screen provides all Phase 1 squad/calendar data and summary metrics required for future sprint capacity.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `npm run test:e2e -- squad` succeeds.
- [ ] Manual browser check at `http://localhost:3000/squad` can create/edit/deactivate members and create absences/holidays.
</verification>

<success_criteria>
- SQUAD-01 through SQUAD-05 are covered by persisted member, absence, holiday, and summary flows.
- APP-05 remains true for squad/calendar data across reloads.
- No release/sprint implementation leaks into Phase 1 beyond explicit placeholders for future impact summaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-foundation-and-squad-setup/01-03-SUMMARY.md`
</output>
